# ダンジョンを掘る魔女 仕様書

最終更新: 2026-06-01
追記内容: 共通画面フェード/クロスフェード、デバッグ起動モードのタイトル前起動追加、紙芝居形式オープニング、仮Title画面、初回導入ダンジョン、採掘開始時のWorldLoading段階化生成、Objectsタブ画像番号列対応、オブジェクトPNG共通描画導入、obj画像ホットリロード対応、1px固定アウトライン対応、ダンジョン小型テンプレート生成、鉱石を含む地形ノイズ強化、虫とりアミのリング接触捕獲アイテム化/捕獲吸収演出、ダンジョンミニマップ、レベル上限100とレベル別最大HPテーブル、レベルアップ演出強化/ジングル共通処理、回復パルス/アイテム使用ログ、AudioEngine基盤、採掘道具セーフティ、壊れたアイテム表示、共通タブUI画像、拠点でのアイテム使用無効化、アイテム捨て機能、ダンジョンイベント発見処理、ダンジョンイベント管理、戦闘系ダンジョンイベント、掘削・属性ギミック系ダンジョンイベント、魔女救助・依頼系ダンジョンイベント、魔女NPC会話、ダンジョンイベント統合調整、拠点/ルネの家の画像ベース描画、オプション設定UI、プレイヤー足音SE、リングアイテム衝突SE、操作説明入力アイコン、固定仮想解像度/レターボックス表示、テストプレイスクリーンショットPNG保存、デバッグ用弾テスト
今回の追記: 回復パルス/アイテム使用ログ、レベルアップ演出強化/ジングル共通処理、みねうちの剣、非致死接触効果、操作説明入力アイコン、整理/整列ショートカット、起動ロード段階化、Google Sheet取得のテストプレイ限定化、固定仮想解像度表示、テストプレイスクリーンショットPNG保存、デバッグ用エフェクトテスト、デバッグ用弾テスト、帰還系ステータス表示のテストプレイ限定化、操作説明マウス表記整理、下部操作説明の表示抑制、D1敵経験値調整、固定敵ノードの敵出現重み参照、敵当たり判定の個別円集合化、当たり判定編集画面のObjects対応、敵当たり判定の方向別編集、当たり判定編集の全方向コピー/左右反転貼付

## 概要

`ダンジョンを掘る魔女` は、独自の軽量2Dゲームエンジン上で動く、2D見下ろし型の採掘アクション・スペルリング武器ローグライトです。

このリポジトリでは、完成版の商用ゲームではなく、ゲーム性を検証できる最小プレイ可能版と、本制作へ進めるためのエンジン土台を実装しています。

## 世界観・あらすじ・ステージ構成 暫定

この章は、ゲーム本制作に向けたシナリオ・ステージ設計の暫定方針です。実装済み仕様ではなく、今後のステージ定義、敵、アイテム、演出、UIテキスト、ボス設計の基準として扱います。

詳細なキャラクター設定、会話方針、情報開示順、エンディング演出は `シナリオ設計書.md` を正とします。仕様書側では、実装判断に必要な世界観・ステージ・ボス・演出要件だけを要約します。

### 作品トーン

世界観の因果関係は真面目に組みます。守護星、魔女の歴史、魔導具廃棄層、主人公の能力、モニカが同行できない理由、ラスボスの構造は、後から見ても筋が通るようにします。

一方で、イベントの絵面、会話、道具の振る舞い、村人の反応はゆるく、漫画的に過激で構いません。

目標は `ゆるいノリなのに、よく見ると世界観がきちんとしているゲーム` です。避けるべきなのは、世界観が雑なだけの悪ふざけです。

物語の核は次の一文です。

```text
強く命令して壊す魔法では救えないものを、道具を回して外側から丁寧に助ける。
```

### 基本世界観

主人公は見習い魔女です。火、雷、召喚、回復、結界などの強い現代魔法はほとんど使えません。ただし、物を自分の周囲に回転させる `スペルリング` だけは異常に得意です。

スペルリングでは、スコップ、松明、拾った道具、宝、魔導書、盾、武器、場合によっては捕獲した敵までも周囲に回して使えます。主人公にとっては、強い魔法を直接撃つのではなく、道具を回して掘り、照らし、守り、攻撃することが唯一の得意分野です。

村には、小さな守護星がありました。守護星はただ夜を照らす星ではありません。大昔の魔女たちが、地底からあふれる濁った魔力を鎮めるために作った、小さな人工星です。

守護星は、地脈から少しずつ魔力を吸い上げ、それをやわらかな光に変えて空へ逃がしていました。だから村には魔物が近づきにくく、夜も暗くなりすぎませんでした。

守護星は `硬い盾` ではありません。魔力を受け止めて跳ね返す装置ではなく、魔力を流し、薄め、逃がす `調律装置` です。

そのため、自然に湧く魔力や地脈の濁りには強いです。一方で、現代魔女が使う強い命令魔法とは相性が悪いです。

強い魔法には命令があります。火炎魔法には `燃えろ`、結界魔法には `閉じろ`、攻撃魔法には `砕け` という命令が乗っています。それが守護星に直接流れ込むと、守護星の古い術式と衝突し、内部で術式が割れ、砕ける危険があります。

つまり、守護星は弱いのではありません。守るための対象が違います。

主人公のスペルリングは、守護星に直接命令を流し込みません。スコップで掘り、松明で照らし、盾で守り、拾った道具を回し、外側から少しずつ問題を解きます。その地味な方法だけが、守護星を壊さずに救える手段です。

### 魔女の歴史

かつての魔女たちは、自然や地脈の流れに寄り添う魔力を使っていました。

古い魔法は、魔力に強い命令を与えるものではありませんでした。流れを読み、そっと動かし、土や星や水の性質に合わせ、道具を媒介にして少しずつ導く技術でした。

例:

- 火を爆発させるのではなく、種火を長く保つ
- 岩を砕くのではなく、割れ目を見つけて少しずつ開く
- 水を支配するのではなく、流れ道を作る
- 星を動かすのではなく、星が動きたい方向を邪魔しない

この時代に、地底から濁った魔力が湧き出す問題が起きました。古代魔女たちは、それを強い魔法で押さえつけませんでした。地底の魔力を少しずつ吸い上げ、薄め、光に変えて空へ逃がす装置を作りました。それが守護星です。

やがて人類の争いや文明発展の中で、魔女の魔法は変化しました。自然に寄り添う魔法は、戦場や大規模開発では遅すぎました。そこで魔女たちは、魔力に明確な命令を込めるようになりました。

例:

- 燃えろ
- 凍れ
- 切れ
- 守れ
- 閉じろ
- 撃て
- 砕け
- 従え

これが現代魔法の原型です。現代魔法は強く、速く、便利です。火力、結界、召喚、採掘、輸送、建築、医療、照明など、魔女社会は大きく発展しました。

しかし副作用がありました。命令を持った魔力は、壊れた魔導具や失敗作にも残ります。命令だけが残った道具は、役目を失っても動き続けます。

失敗した魔法道具、壊れた魔導具、戦争や開発で使い捨てられた道具は、地下へ捨てられ続けました。それらが積もってできた層が `魔導具廃棄層` です。

魔導具廃棄層は、単なるガラクタ置き場ではありません。現代魔女文明の負債が積み重なった場所です。

道具には命令魔法の残骸が残っています。

例:

- `燃えろ` と命令されたまま燃え続けようとするロウソク
- `守れ` と命令されたまま誰もいない通路を守る盾
- `開け` と命令されたまま自分自身を開け続ける宝箱
- `まっすぐ飛べ` と命令されたのに曲がり続ける失敗杖

表面上はゆるく笑える道具にします。しかし根には、命令魔法の残骸が地下を汚染しているという設定があります。

### 守護星落下の原因

守護星は、長年にわたり魔導具廃棄層の汚染を処理し続けていました。

本来処理するはずだったのは、自然に近い地脈の濁りです。しかし時代が進むにつれ、地下には命令魔法の残骸を含んだ失敗魔導具が増えていきました。

守護星は何百年もそれを吸い上げ、薄め、光に変え続けていました。その結果、守護星の内部には古い命令の残骸がこびりつき、限界に近づいていました。

守護星祭の日、主人公のスペルリング事故が起きます。

ただし、主人公が全部悪いわけではありません。守護星はすでに限界でした。主人公の事故は、最後のひと押しです。

この構図により、主人公には責任感が生まれますが、物語全体は `主人公が全部悪い` という重い話にはしません。

### 主人公

主人公の名前は `ルネ` です。

ルネは強い魔法が苦手な見習い魔女です。

火、雷、召喚、回復、結界などの現代魔法はほとんど得意ではありません。火球を撃とうとすると温かい石が出る、回復魔法を使うと傷ではなく靴ひもが直る、召喚魔法を使うと呼んでいないスコップが来る、くらいのゆるさで構いません。

一方で、物を自分の周囲に回すスペルリングだけは異常に得意です。

ルネは落ちこぼれではありません。今の時代の魔法体系に合っていないだけです。ルネの魔力は、古代魔女の自然に寄り添う魔力に近いものとして扱います。

主人公の欠点は、すぐ道具に頼ることです。手で取ればいい物もスペルリングで取ります。掃除もスペルリング、片付けもスペルリング、運搬もスペルリングで済ませようとします。この癖が守護星祭の事故につながります。

しかし最後には、その欠点が長所になります。序盤では `また道具を回して失敗した` ことが事件のきっかけになります。終盤では `道具を回すことしかできない` からこそ、守護星を壊さず救えます。

主人公の動機は段階的に変化させます。

```text
序盤: 自分のせいかもしれないから、何とかしたい
中盤: 地下を見て、これは自分だけの失敗ではなく、魔女たち全体が積み残してきた問題だと知る
終盤: 罪悪感ではなく、自分のやり方で星を助けたいと思う
```

### モニカ

モニカは主人公の幼なじみの魔女です。

主人公とは正反対に、現代魔法が非常に得意です。火、雷、氷、結界、解析、回復、召喚などを高い水準で使えます。村の期待を背負う優秀な魔女です。

モニカは主人公を見下しません。二人は仲良しです。モニカは主人公のスペルリングが普通ではないことを理解しており、本気で尊重しています。

モニカは優秀ですが、今回の問題には直接触れられません。モニカの魔力は強すぎます。守護星に近づくと、命令魔法の強い魔力が守護星やチコリに負担をかけます。

序盤で、モニカが一度は同行しようとします。しかし一歩近づいたり、結界を張ったりした瞬間に、チコリが苦しそうに点滅し、守護星の欠片にヒビが入りかけます。

そこでモニカは、自分が直接行けないことを理解します。モニカは無能ではありません。むしろ非常に有能です。ただし、今回だけは強すぎることが弱点になります。

ゲーム上、モニカは基本的に拠点上のNPCとして配置されます。近づいて話すと、状況補足や次の目的の仮メッセージを表示します。

ただし万能な案内役にはしません。魔導具廃棄層の奥や落星の眠る地底では、命令魔力のノイズが強すぎて観測しきれない場面を作ります。主人公が現地で見て、掘って、判断しなければならない状況を残します。

最後に、モニカは空への魔法の道を作ります。モニカの強い魔法は、守護星に直接触れられません。しかし、地底から空へ続く光の道、上昇気流、結界の外枠なら作れます。

モニカが道を作り、主人公がスペルリングで守護星を運びます。主人公だけでは星を空へ戻す大きな力が足りません。モニカだけでは星を壊さず運べません。二人の役割が合わさって初めて守護星を戻せます。

### チコリ

チコリは、守護星の欠片から生まれた小さな星の子です。

守護星が落ちる瞬間、ほんの小さな欠片だけがルネの帽子やフードの中に落ちます。その欠片が、小さな生き物のように動き始めます。

見た目の方向性:

- もちっとした星形の小動物
- 小さい光のしっぽがある
- 飛ぶというより、ふよふよ浮く
- ルネの帽子やポケットに隠れる
- 怖いものを見るとチカチカ点滅する
- 星くずを食べる
- 強い魔力に近づくと具合が悪くなる

チコリは流暢に喋らせません。

```text
ちこ
ぴ……
ちかちか！
……ほし
```

この程度でよいです。

チコリは説明役ではありません。世界観説明はモニカや拠点の大人が担当します。チコリは感情と反応を担当します。

チコリは、守護星の感情や状態を伝える存在です。守護星本体は喋りません。しかしチコリは、光り方や動きで守護星の状態を伝えます。

- 怖がっている
- 痛がっている
- 逃げたがっている
- 主人公に近づきたがっている
- モニカの魔力を怖がっている
- 魔導具廃棄層で気分が悪くなる

チコリがいることで、守護星が単なる目的物ではなく、感情のある存在として伝わります。

エンディングでは、チコリは一度、守護星本体へ戻ったように見せます。プレイヤーには別れだと思わせます。しかしエンディング後、ルネの帽子やフードの中で小さな光がともります。チコリは残っています。

チコリは守護星の欠片でありながら、主人公と旅をしたことで、主人公の小さな相棒として独立した存在になったものとして扱います。

### 開幕イベント

開幕は `守護星祭` です。

守護星祭は、村が守護星へ感謝する年に一度の儀式です。モニカは儀式の中心メンバーです。強い魔法で魔法陣や結界を制御しています。村の大人たちはモニカを信頼しています。

主人公は雑用係です。

主人公の仕事:

- 松明を並べる
- スコップを片付ける
- 儀式台の周りを掃除する
- 余計な物を回さない

最後の注意だけやけに強く言われます。

儀式中、星型の飾りか儀式具が落ちそうになります。主人公は反射的にスペルリングを使います。

スコップ、松明、鍋、椅子、村長の帽子、なぜか干してあった靴下などが一斉に回ります。

モニカが即座に結界を張ります。大人たちも魔法で止めようとします。

しかし強い魔法が守護星に反応し、守護星はさらに不安定になります。

最後に、主人公のスコップが儀式装置の足や固定具を一本だけ正確に削ります。

守護星が、静かに落ちます。

全員が無言になります。

数秒後、村長が言います。

```text
……今、星、落ちた？
```

タイトル。

```text
ダンジョンを掘る魔女
```

この導入は、ギャグの絵面で重大事件を起こすためのものです。軽く見せながら、主人公、モニカ、チコリ、守護星、スペルリング事故を一気に立てます。

### あらすじ

守護星祭の日、見習い魔女であるルネは儀式の雑用係をしていました。ルネは強い魔法が苦手ですが、物を周囲に回すスペルリングだけは異常に得意です。

儀式中、ルネは反射的にスペルリングを使い、スコップや松明や謎の道具まで一斉に回してしまいます。その事故が最後のひと押しとなり、もともと限界だった守護星は地底へ落ちます。

守護星が落ちた穴からは、黒い土、星くず、魔物、そして壊れた魔法道具が湧き出します。村の魔女たちは強い魔法で穴を塞ごうとしますが、地中の星が命令魔法に反応して砕けかけたため、強引な魔法は使えなくなります。

ルネの帽子やフードには、守護星の欠片から生まれた小さな星の子 `チコリ` が落ちてきます。チコリは守護星の方向や状態に反応します。

優秀な幼なじみの魔女 `モニカ` は同行しようとしますが、彼女の強すぎる魔力がチコリを苦しめるため、直接は行けません。

ルネは、自分のせいかもしれないという責任感と、自分のスペルリングでしか安全に近づけないという状況から、スコップと松明を回して地底へ潜ります。

最初は村の真下の `星くずのダンジョン` で落下跡を追い、次に魔女たちの失敗作が積み重なった `魔導具廃棄層` へ入り、最後は地底なのに星空のように歪んだ `落星の眠る地底` へ向かいます。

目的は、敵を倒すことではありません。落ちた守護星を壊さず救い出し、村の光を取り戻すことです。

最後は、モニカが空への魔法の道を作り、ルネがスペルリングで守護星を中心に包み、スコップ、松明、盾、宝、魔導書などを星の周囲に回して、星を守るかごのようにして空へ運びます。

### ステージ全体方針

ストーリー面は3つ、フリー面は1つ用意します。

| 種別 | 面名 | 役割 | 主体験 | 雰囲気 |
| --- | --- | --- | --- | --- |
| ストーリー面1 | 星くずのダンジョン | 序盤 | 掘る、照らす、進む | 自然の地下、暗いが怖すぎない冒険 |
| ストーリー面2 | 魔導具廃棄層 | 中盤 | 拾った道具を回す、ビルドを作る | 魔法ガラクタ、壊れたおもちゃ箱、地底の粗大ごみ置き場 |
| ストーリー面3 | 落星の眠る地底 | 終盤 | 総合力、星の救出 | 地底宇宙、星核、結晶化した魔導具 |
| フリー面 | 不可思議の迷宮 | ローグライク | 初期状態からの高難度試練 | 地底・宇宙・廃棄場・記憶が混ざった異次元 |

3つのストーリー面は、単なる難易度違いにしません。見た目、敵、アイテム、プレイ判断を明確に変えます。

- 面1: 自然の地下。土、根、鉱石、星くず。
- 面2: 人工物の地下。廃棄魔法道具、壊れた装置、ガラクタ。
- 面3: 超自然の地下。星、星核、結晶、宇宙的な空洞。
- フリー面: 全部が壊れて混ざった異次元。ストーリーで得た知識と操作技術を試す場。

### ボス一覧

| ボス | 登場 | 本質 | 戦闘 | 役割 |
| --- | --- | --- | --- | --- |
| 星くずモグラ | 星くずのダンジョン | 星くずを食べた地底生物 | 潜行、突進、背中の結晶狙い | 掘削、光源、逃げ道作りの基本確認 |
| 廃品殻獣ジャンクラブ | 魔導具廃棄層 | ガラクタを甲羅にした甲殻生物 | ガラクタリング、ハサミ、ガラクタ飛ばし、弾き返しで転倒、腹部攻撃 | 面2の道具ビルド、盾、反射、重い武器の確認 |
| 星封殻アストラグナ | 落星の眠る地底 | 守護星を封じる回転外殻 | 封印パーツ5個破壊、ダウン中に外殻掘削、星に触れて救出 | 掘って解除するラスボス |
| 星脈竜 | 不可思議の迷宮の深層 | 星脈と魔導廃棄物から生まれた巨大竜 | ブレス、飛行奇襲、突進、属性地形効果 | 無限ローグライクの深層目標 |

### ストーリー面1: 星くずのダンジョン

#### 役割

序盤の面です。基本操作、掘削、暗闇、光源、敵処理、経験値、レベルアップをプレイヤーに覚えさせます。

#### 設定

村の真下に広がる浅い坑道です。落ちた守護星が通った跡として、土の中に星くずが混じっています。全体は暗いものの、土は柔らかく、松明の光で少しずつ道が見えます。

ここは `暗いけれど怖すぎない地下冒険` として設計します。色の基調は土色、青白い星くず、小さな鉱石、木の支柱、根っこです。

#### 主体験

プレイヤーに、自分で道を作っている感覚を与えます。

スコップが当たった場所を掘る。松明が回って見える範囲が変わる。敵が出ても、リングに当てれば倒せる。序盤では、複雑なビルドよりも `掘る、照らす、進む` の気持ちよさを優先します。

#### 情報開示

第1面では、深い設定を説明しません。

明かすこと:

- 守護星は本当に地底へ落ちた
- 強い魔法で触ると危険
- チコリが星の方向へ反応する
- 主人公のスペルリングなら安全に近づける可能性がある

主人公は、自分の事故を取り返すために潜ります。

#### 主なギミック

- 柔らかい土が多い
- 星くずが埋まった小部屋
- 掘ると敵が出る地形
- 松明の光で見つけやすい宝
- 初期リング半径とリング速度でも扱いやすい通路

#### 敵案

- 小さい土虫
- 星くずに寄ってくる羽虫
- 掘ると逃げる小型モンスター
- 地面から短距離突進するモグラ系の敵

敵は弱めにします。この面ではプレイヤーを殺しにいくより、掘削と戦闘がつながっていることを理解させます。

#### アイテム案

- 基本スコップ
- 松明
- 軽い石
- 欠けたツルハシ
- 小さなランタン
- 星くずの欠片

序盤アイテムは、効果を複雑にしすぎません。リングに乗る物が増えるだけでも楽しさが出る段階です。

#### ステージ目的

星がさらに深く沈んだ痕跡を見つけます。最後に `星くずの裂け目` を発見し、次の層へ降ります。

#### ボス案: 星くずモグラ

`星くずモグラ` は、落ちた守護星の通り道に残った星くずを食べて巨大化したモグラ系モンスターです。背中や鼻先に青白い星くず結晶が生えており、暗い坑道内でぼんやり光ります。

位置づけ:

- ストーリー面1 `星くずのダンジョン` のボス
- 序盤の基本操作確認ボス
- 掘る、照らす、逃げ道を作る、リングを当てる、突進を避けることを試す

戦闘概要:

- 地中潜行と突進が主軸
- 潜る前に小ジャンプし、着地と同時に地中へ潜って消える
- 潜行後は毎回ランダムな地中待機を挟み、出現予定地点に土煙を出して位置を予告する
- 土煙地点からその場で大ジャンプして飛び出す
- 着地後、約30f待ってから主人公のいた方向へ突進する
- 突進方向は突進開始時に固定し、途中追尾はしない
- 突進中は柔らかい土を破壊して進む
- 岩壁、硬い岩壁、鉱石壁に衝突すると硬直する
- プレイヤーが狭い通路ばかり掘っていると逃げづらい
- 広い空洞や逃げ道を掘っていると避けやすい

弱点:

- 背中の星くず結晶
- 通常可視状態でも、背中側にある星くず結晶が弱点になる
- 壁にぶつかった後の硬直中は動きが止まり、背中の結晶を狙いやすい
- 小ジャンプ潜行中、地中潜行中、飛び出しジャンプ中は攻撃対象にしない

実装:

- `EnemySystem` の共通ボスアクションシーケンスで再現する
- `Submerge`、`Telegraph`、`Jump`、`LandingDelay`、`Charge`、`Stun`、`Recover` の共通フェーズを使う
- 星くずモグラは `stardust_mole` パターンとして、小ジャンプ潜行、0.90〜1.80秒のランダム地中待機、土煙予告、その場大ジャンプ、30f待機、突進、壁衝突硬直を組み合わせる
- 弱点は共通のボス弱点判定で扱う。星くずモグラは、可視かつ無敵でない間、向いている方向の背中側に `stardust_crystal` 弱点を持つ
- リングアイテムまたは反射弾の命中円が背中結晶の弱点円に重なると、`crystalDamageMultiplier` による弱点ダメージになる
- この共通フェーズは、今後のボスでも予告、突進、硬直、ジャンプ系の行動へ流用する

ストーリー上の意味:

- 守護星がさらに深く沈んでいることを示す最初のボス
- 撃破後、星くずの裂け目が開き、次の `魔導具廃棄層` へ進む

### ストーリー面2: 魔導具廃棄層

#### 役割

中盤の面です。アイテムビルド、リング装着、道具ごとの個性、敵の攻撃パターン変化を見せます。

#### 設定

昔の魔女たちが、失敗した魔法道具を地底へ捨ててきた廃棄層です。

壊れた魔導書、割れた水晶、片方だけのブーツ、しゃべりかけてくる盾、燃え尽きないロウソク、古い杖、魔法の釘、星読みコンパスなどが地層に埋まっています。

ここは `地底のガラクタ魔法市`、`壊れたおもちゃ箱`、`魔女の粗大ごみ置き場` の雰囲気にします。面1の自然洞窟とは明確に変えます。

#### 主体験

この面の主役は、拾ったものを回す楽しさです。

プレイヤーは、スコップと松明だけでなく、盾、鉄球、魔導書、ランタン、壊れた杖、変な宝などをリングに組み込みます。

道具には、次のような個性を持たせます。

- 重いが攻撃力が高い
- 光るが攻撃力は低い
- 弾を防ぐ
- 軌道を安定させる
- 速度を上げる
- 硬い地形に強い
- 隠し宝に反応する

#### 情報開示

第2面で、世界観の本質が少し見えます。

明かすこと:

- 地下には昔の魔女たちが捨てた失敗魔導具が大量にある
- それらには命令魔法の残骸が残っている
- 失敗魔導具と地底の濁った魔力が混ざり、地下を汚染している
- 守護星は長年それを処理し続けていた

主人公は、自分だけが原因ではないと知ります。ただし、それは責任が消えるという意味ではありません。むしろ、魔女たち全体の問題を見てしまいます。

ここで主人公の動機は変わります。

```text
自分の失敗を取り返したい
↓
このまま放っておけない
```

#### 主なギミック

- 壊れた魔法道具が埋まった地層
- 掘ると道具か敵が出るガラクタ塊
- 弾を撃つ敵と、盾系アイテムの有用性
- 探知アイテムで見つける隠し部屋
- 磁石や鉄球など、重量・材質を活かすアイテム配置

#### 敵案

- 飛び回る壊れた魔導書
- 盾だけが歩いている敵
- 歯車虫
- ガラクタをまとった地底生物
- 壊れた杖から生まれた弾撃ち敵

敵も `回転` や `道具` と関係させます。普通のスライムだけで構成すると、この面の意味が薄くなります。

#### アイテム案

- ひび割れた魔導書
- 古い盾
- 鉄球
- 磁石
- 壊れたランタン
- 星読みコンパス
- 魔法の釘
- 回りたがる皿
- しゃべる盾
- 曲がった杖

ふざけたアイテムもこの面なら成立します。ただし、効果はDB上で整理し、通常効果・軌道効果・特殊タグの役割を混同しないようにします。

#### ステージ目的

星が廃棄魔導具を暴走させている原因を突き止めます。魔導具廃棄層の奥で、命令魔法の残骸と地底生物が混ざった異常生態系を確認します。

#### ボス案: 廃品殻獣ジャンクラブ

`廃品殻獣ジャンクラブ` は、魔導具廃棄層に住みついた巨大甲殻生物です。カニ、ヤドカリ、地底虫を混ぜたような生物で、背中に壊れた盾、歯車、鉄球、魔導書、ランタン、曲がった杖などを甲羅としてまとっています。

位置づけ:

- ストーリー面2 `魔導具廃棄層` のボス
- 旧案 `廃棄魔導炉・ガラクタリング` は廃止し、生物寄りのボスに変更する
- `ガラクタリング` はボス名ではなく、ジャンクラブの攻撃名・防衛行動名として扱う
- 魔導具廃棄層のテーマである `捨てられた魔法道具が、地底で別の生態系を作っている` を体現する

設定:

- 守護星の力を浴びたことで、背中のガラクタが浮き上がり、周囲を不安定に回るようになった
- プレイヤーのスペルリングに似ているが、技術ではなく本能的な防衛反応である
- 巣を守り、光るものや使えそうなガラクタを集める
- 主人公の道具や星くずを甲羅素材と見なして襲う

主な攻撃:

1. ガラクタリング

- 背中のガラクタを浮かせ、自分の周囲に回す防衛行動
- 盾、鉄球、歯車、魔導書、ランタンなどが回る
- 主人公のリングと違い、軌道は不安定
- 本体を守りつつ、近づくプレイヤーを妨害する

2. ハサミ攻撃

- 近距離攻撃
- プレイヤーが懐に入りすぎると巨大なハサミで挟む
- 密着し続けるだけの攻略を防ぐ

3. ガラクタ飛ばし

- 遠距離攻撃
- 背中や周囲のガラクタを弾のように飛ばしてくる
- 盾、反射、リング投げなどで弾き返せる余地を作る

弱点:

- 腹部
- 通常時は甲羅とガラクタに守られており、腹部には攻撃しにくい

ひっくり返し条件:

- ガラクタリング中のガラクタを壊す
- ガラクタリング中のガラクタを弾き返す
- ガラクタ飛ばしを弾き返して本体に当てる
- 重い武器や盾系アイテムでガラクタを押し返す
- これらで一定量の転倒値を与えると、ジャンクラブがひっくり返る

転倒時:

- 腹部が露出する
- 腹部へ大ダメージが通る
- 一定時間後に起き上がる
- 起き上がり後、再びガラクタリングやガラクタ飛ばしを使う

実装方針:

- `Enemies` の `junk_crab`、またはステージ2の既定ボスID `junk_crab` から `boss_sequence` の `pattern=junk_crab` として専用FSMへ接続する
- `junk_crab` がDB未定義でも、フォールバック定義として `廃品殻獣ジャンクラブ` を生成し、ステージ2ボスとして動作させる
- FSMは `ガラクタリング待機`、`ハサミ予告`、`ハサミ攻撃`、`ガラクタ飛ばし予告`、`ガラクタ飛ばし`、`転倒`、`復帰` で構成する
- ガラクタリング中はボス周囲にボス所有のガラクタ当たり判定を複数持たせる。ガラクタは通常敵としては扱わず、捕獲、経験値、ミニマップ、通常ドロップの対象にしない
- ガラクタを壊す、重い武器や盾系アイテムで殴る、反射した `junk_chunk` を当てることで転倒値を増やす
- 通常時は甲羅防御として受けるダメージを軽減し、転倒中は腹部弱点円に命中した場合だけ弱点ダメージになる。腹部以外へ当たった場合は通常ダメージとして扱う
- ガラクタ飛ばしは敵弾 `junk_chunk` を使う。既存のガード・反射処理でプレイヤー側の弾になった `junk_chunk` は、ジャンクラブへ大きな転倒値を与える
- `boss_sequence` のパラメータで `topple`、`debris`、`debrisHp`、`guardSeconds`、`orbitSpeed`、`guardMoveSpeed`、`throwProjectile`、`throwCount`、`throwDamage`、`throwSpread`、`throwSpeedMultiplier`、`throwRadiusScale`、`shellDamageMultiplier`、`toppledDamageMultiplier`、`toppledSeconds`、`recoverSeconds` を調整できる。`toppledDamageMultiplier` は腹部弱点命中時の倍率として扱う

削除する旧要素:

- 廃棄炉・処理装置としての設定は使わない
- 無機質な処理音声風の喋り方は使わない
- ガラクタ拾いによる甲羅修復は現時点では不要
- 巣穴呼びによる雑魚召喚は現時点では不要
- 突進を壁にぶつけて腹部露出する仕様は使わない

ストーリー上の意味:

- 魔導具廃棄層の異常生態系を見せるボス
- 昔の魔女たちが捨てた魔法道具を、地底の生き物が甲羅として利用している
- 面2のテーマ `捨てられた魔法道具が、地底で別の生態系を作っている` を体現する

### ストーリー面3: 落星の眠る地底

#### 役割

終盤の面です。掘削、光源管理、リングずらし、軌道投げ、ビルド、敵処理、逃げ道作り、硬い地形の掘り方を総合的に使わせます。

#### 設定

守護星の本体が沈んだ最深部です。

ここは普通の洞窟ではありません。星の力で地形が歪み、地底なのに宇宙のように見える空洞です。岩は黒く、鉱石は青白く光り、星核岩や結晶化した魔導具が周囲を覆っています。

面3は `地底なのに宇宙っぽい場所` として設計します。単なる深い洞窟にすると弱いので、星空、結晶、黒い地面、青白い光を使います。


#### 主体験

面1と面2で学んだ要素の集大成です。

- 掘削して道を作る
- 松明や光源アイテムで見える範囲を管理する
- リング中心ずらしで危険地形を安全に削る
- 軌道投げで敵や障害物へ踏み込む
- ビルドで火力、照明、防御、探知を選ぶ
- 硬い地形をどこから崩すか判断する
- 敵を倒すだけでなく、逃げ道を掘って生き残る

#### 情報開示

第3面で、守護星の正体が分かります。

明かすこと:

- 守護星は自然の星ではなく、古代魔女が作った人工星
- 地底の魔力を吸い上げ、薄め、光に変えて空へ逃がす調律装置
- 守護星は命令魔法の残骸を何百年も処理し続けて限界だった
- 主人公のスペルリングは、古代魔女の自然に近い魔力系統と近い

#### 主なギミック

- 硬い星核岩
- 光る星脈による進行方向の誘導
- 星の結晶に覆われた廃棄魔導具
- 暗闇でだけ動く敵
- 光に反応して寄ってくる敵
- 硬い地形の裏にある星の外殻片

#### 敵案

- 星の光をまとった地底虫
- 結晶化したガラクタ魔物
- 光に反応して寄ってくる敵
- 暗闇でだけ動く敵
- 周囲に小さな結晶片を回す敵

終盤では、単にHPが高い敵よりも、プレイヤーの操作判断を揺さぶる敵を重視します。ただし、リング操作不能や過度な妨害でプレイヤーの主体験を壊さないようにします。

#### アイテム案

- 星核の欠片
- 重力石
- 反重力ランタン
- 星読みの杖
- 軌道を安定させる結晶
- 軌道を広げる結晶
- 星脈スコップ
- 結晶盾

ここで、スペルリングの最終強化や、複数リング前提の遊びと噛ませます。

#### ステージ目的

落ちた守護星を見つけ、壊さずに掘り出します。ただし守護星は、星封殻に包まれ、地上への魔力流出を防ぐために閉じ込められています。

#### ラスボス案: 星封殻アストラグナ

`星封殻アストラグナ` は、中央に落ちた守護星を封じている巨大な封印殻です。悪意ある魔王ではなく、守護星を危険な高魔力体として封じ、地上への魔力流出を防ごうとする古い安全装置です。

位置づけ:

- ストーリー面3 `落星の眠る地底` のラスボス
- 通常の敵ではなく、地形ボス・採掘ボスとして扱う
- HP0で倒すボスではなく、外殻を掘り抜いて守護星に触れることでクリアする

本体構造:

- 中央に守護星がある
- その周囲を巨大な外殻ブロック群が包み込んでいる
- 外殻は通常マップとは独立した、ボス専用の回転タイル構造とする
- 外殻ブロックは通常マップの地形タイルとは混ぜない
- 外殻はゆっくり回転している
- 外殻は `掘れる地形そのものがボス` になっている
- 外殻は通常時、防御が異常に高く、破壊困難

プレイヤーの目的:

- 外殻を掘り抜き、中央の守護星に触れて救出する
- 強い魔法や単純破壊ではなく、掘削によって星を壊さず救い出す

封印パーツ:

- 封印パーツは5個
- 結晶のような見た目
- 本体または外殻と魔導管でつながっている
- ウネウネ動いたり、外殻周辺を移動したりする
- 封印パーツが生きている間、外殻の高防御が維持される

基本ループ:

1. 封印パーツ5個が生存し、外殻は高防御になる
2. 封印パーツが外殻を少しずつ修復する
3. プレイヤーが封印パーツ5個をすべて破壊する
4. アストラグナが一定時間ダウンする
5. ダウン中は外殻の防御が下がり、外殻ブロックへ通常ダメージが通る
6. プレイヤーはダウン中に外殻を掘る
7. ダウン終了後、封印パーツ5個が完全復活する
8. 復活するたびに封印パーツの最大HPが下がる
9. 外殻を掘り抜いて中央の守護星まで到達する
10. 守護星にプレイヤーが触れるとクリア
11. 星に力がみなぎり、外殻を内側からすべて破壊して救出成功となる

封印パーツ復活:

- ダウン終了時、封印パーツ5個は完全復活する
- ただし復活するたびに最大HPにデバフがかかる
- 最終的に初期最大HPの50%程度まで低下する

HP倍率案:

| 状態 | 最大HP倍率 |
| --- | --- |
| 初回 | 100% |
| 1回復活後 | 85% |
| 2回復活後 | 70% |
| 3回復活後 | 60% |
| 4回復活以降 | 50% |

外殻修復:

- 封印パーツが1個以上生存している間、時々、破壊済み外殻ブロックを修復する
- 修復対象は、破壊済み外殻ブロックから選ぶ
- 修復は一定間隔で発生し、一度に生存封印パーツ数ぶんまで外殻ブロックを復旧する
- 生存している封印パーツ数が多いほど、1回の修復タイル数が多くなる
- ダウン中は外殻修復を行わない

修復量案:

| 生存封印パーツ数 | 1回の修復タイル数 |
| --- | --- |
| 5個 | 5タイル |
| 4個 | 4タイル |
| 3個 | 3タイル |
| 2個 | 2タイル |
| 1個 | 1タイル |
| 0個 | 修復なし |

修復制限:

- プレイヤーが重なっている場所は修復しない
- プレイヤーのすぐ近くは修復しない
- 中央の星に触れる直前のマスを即時封鎖しない
- ダウン中は修復しない

実装方針:

- `Enemies` の `astragna`、またはステージ3の既定ボスID `astragna` から `boss_sequence` の `pattern=astragna` として専用FSMへ接続する
- アストラグナ本体は通常ボスと同じ `Enemy` として生成し、ボス前後会話、BGM、撃破演出、ステージクリア遷移は既存の `EnemyEventType::BossDeath` 経路へ合流する
- 外殻ブロックと封印パーツは `BossActionRuntime::astragna` の子ランタイムとして保持し、通常 `TileMap` の地形タイル、掘削ドロップ、隠し敵、ミニマップ開通とは混ぜない
- 外殻はボス中心からのローカル角度と半径を持つ専用ブロックとして扱い、毎フレーム `rotationAngle` を加えて回転位置を算出する
- 封印パーツは5個固定で、リング接触またはプレイヤー反射弾の命中円が封印パーツ円に重なった時にHPを削る
- 封印パーツがすべて壊れると `Downed` に入り、ダウン中だけ外殻の掘削倍率を通常値へ戻す
- 外殻ブロックはリングアイテムの `target=terrain` の `dig` / `dig_hard` / `dig_multi` 効果から掘削ダメージを計算する。通常時は `sealedShellDamageMultiplier`、ダウン中は `downedShellDamageMultiplier` を掛ける
- 封印パーツが1個以上生存している `Sealed` 中だけ、破壊済み外殻ブロックを生存パーツ数ぶんまで周期修復する。プレイヤー近傍のブロックは修復対象から外す
- ダウン終了時は封印パーツを完全復活させ、復活回数に応じて最大HPを `100% / 85% / 70% / 60% / 50%` へ下げる
- プレイヤーが外殻の隙間を抜けて中央の守護星へ触れると `Rescued` になり、HP0撃破ではなく救出成功として `BossDeath` を発行する
- `boss_sequence` のパラメータで `rotationSpeed`、`sealHp`、`sealOrbitRadius`、`sealRadius`、`shellHp`、`outerShellRadius`、`innerShellRadius`、`shellRadius`、`sealedShellDamageMultiplier`、`downedShellDamageMultiplier`、`downedSeconds`、`repairSeconds`、`repairSafeRadius`、`guardianRadius` を調整できる

不採用要素:

- ラスボス攻撃としての重力歪曲は不採用
- ラスボス攻撃としてのリング操作妨害は不採用
- リング速度、リング半径、ずらし距離を乱す攻撃はラスボスには入れない
- 周囲にアイテムや残骸を回して攻撃するボスにはしない
- ラスボスは `回す敵` ではなく、`掘って解除する封印殻` とする

勝利条件:

- 外殻を掘り抜いて中央の守護星に触れること
- HP0で倒すボスではない
- 最後は攻撃ではなく救出で決着する

### フリー面: 不可思議の迷宮

#### 役割

ローグライク用のフリー面です。ストーリー面を遊んで知識と技術を身につけたプレイヤーが挑む、難関試練として扱います。

毎回レベル1、初期状態から開始します。地形、宝、敵、星くず、出口、ボス、イベント部屋は毎回再生成します。

#### 設定

`不可思議の迷宮` は現実の地下ではありません。落ちた星と魔導具廃棄層が干渉して生まれた異次元の坑道です。

星の記憶、捨てられた魔法道具、掘られた地形、倒した敵の残響が混ざり、毎回構造が変わります。

#### なぜ毎回レベル1・初期状態なのか

この面は現実の坑道ではなく、星が見せる試練です。

持ち込んだ装備、レベル、強化はすべて星の外側に置いていかれます。入れるのは、主人公本人の知識と操作技術だけです。

この設定により、ローグライク上の初期化をゲーム都合ではなく世界観上のルールとして説明します。

#### 雰囲気

見た目は、地底と宇宙と廃棄場が混ざった場所です。

- 地面は黒い土
- 背景には星が流れている
- 土を掘ると、岩ではなく古い記憶の破片が出る
- 松明の光が丸く照らすが、その外側は洞窟ではなく星空のような闇
- 道具が地面に埋まっているのではなく、空間に半分刺さっている
- 星くず、魔導廃棄物、結晶、敵の残響が同じ場所に混ざる

ここはストーリー面よりも整合性を少し崩してよいです。重要なのは、特別な挑戦場としての異次元感と高難度感です。

#### プレイ構造

基本構造は次の通りです。

1. 浅層: 基本資源を集める
2. 中層: ビルド方針が決まる
3. 深層: 敵密度と地形難度が上がる
4. 深層ボス: 星脈竜系ボス
5. 脱出または記録更新

完全ランダムにしすぎると運ゲーになります。ランダム生成でありながら、最低限の資源、複数の進行ルート、リスクと報酬の選択が成立するようにします。

#### 特殊ルール: 星の歪み

階層進行ごとに、空間ルールが変わります。

じっくり探索させるため、時間経過だけでは歪みを更新しません。同じ深度に長くいても、プレイヤーを急かす目的の強制更新は行いません。

現在の仮実装では、不可思議の迷宮の深度 `1` から歪みを1つ発生させ、より深い深度へ進んだ時だけ再抽選します。同じ歪みは連続しないようにします。

仮実装する歪み:

- `星明かりが遠のく`: 光源範囲が狭くなる
- `星硬化`: 地形が硬くなる
- `残響湧き`: 掘削時の敵スポーン率が上がる

通常面とは違う緊張感を出すため、星の歪みはフリー面固有のフロア効果として扱います。効果の種類、数値、深度ごとの強さは今後さらに調整します。

#### 特殊ルール案: 持ち込み禁止、記憶だけ持ち込み

装備もレベルも持ち込めません。ただし、ストーリーで学んだ知識は使えます。

プレイヤーが問われる知識は次のようなものです。

- どの敵を先に倒すべきか
- どのアイテムをリングに入れるべきか
- 掘る方向をどう決めるか
- 光源をどう使うか
- 敵を湧かせすぎない掘り方
- 逃げ道を残す掘り方
- 重いアイテムと軽いアイテムの扱い
- 探知アイテムや防御アイテムの価値判断

この面では、プレイヤーに `上達したから深く潜れる` と感じさせることを重視します。

持ち帰り:

- 成功帰還時は、ラン中に得たアイテム、素材、お金をすべて持ち帰れます。
- 死亡時は、ラン開始時の所持状態へ戻し、ラン中取得品は星間に消失します。
- ラン中レベル、経験値、星の歪み、探索中マップ、敵状態は持ち帰り対象にしません。

#### 特殊部屋案

ランダム生成の中に、記憶に残る特殊部屋を入れます。不可思議の迷宮では最低5部屋を生成し、5種が最低1個ずつ出るようにします。

- 道具だけが大量に回っている部屋
- 敵が眠っているが、宝を取ると起きる部屋
- 光が届かないが、宝の位置だけ見える部屋
- 壁が星くずでできていて、掘ると敵か宝が出る部屋
- 自分の過去のビルドを模した敵が出る部屋
- 松明の光が逆に敵を呼ぶ部屋
- 床ではなく星空を掘っているように見える部屋
- 廃棄道具が空間に刺さっており、引き抜くとリングに入る部屋

#### ローグライク専用深層ボス案: 星脈竜

`星脈竜` は、落ちた守護星の魔力、古い魔導廃棄物、地中の鉱脈が長い時間をかけて竜の形になった存在です。普通のドラゴンではなく、不可思議の迷宮の深層にだけ現れる巨大な星脈の化身です。

位置づけ:

- フリー面 `不可思議の迷宮` の深層ボス
- 旧案 `星間試練体・ミラースペル` は廃止する
- プレイヤーのリングを真似する敵にはしない
- 無限ローグライクの長期目標として扱う

不可思議の迷宮での扱い:

- 不可思議の迷宮は、死ぬまで無限に潜れるローグライク専用ダンジョンとして扱う
- 道中では星くずモグラやジャンクラブなど、ストーリーボスの残響・再登場版も出る
- それらを乗り越え、かなりの距離を掘り進めた時に、星脈竜が現れる
- 空を飛ぶというより、星空のような地底空間を泳ぐ巨大竜として扱う
- 戦闘上は、飛行、奇襲、ブレスなどのドラゴンらしい行動をシンプルに使う

戦闘コンセプト:

- 複雑なギミックボスではなく、純粋に強いボス
- ただし「ダンジョンを掘る魔女」らしく、地形、掘削、光源判断と関係させる
- ブレスや奇襲に対して、壁を掘って逃げ道を作る、岩を遮蔽物にする、光源で予兆を読むなどの攻略余地を持たせる

共通行動:

1. ブレス

- 前方広範囲の属性攻撃
- 色によって属性が変わる
- 地形にも影響を与える

2. 飛行・奇襲

- 一時的に飛び上がり、位置を変えて急襲する
- 着地点は光、影、地面の揺れなどで予告する

3. 爪・噛みつき

- 近距離高威力攻撃
- 密着し続ける攻略を防ぐ

4. 突進

- 地上または空間を滑るように突進する

5. 咆哮

- 周囲への圧力攻撃
- ノックバックや短時間の行動阻害を与えてもよい

「ダンジョンを掘る魔女」らしい最低仕様:

- ブレスは地形を焦がす、凍らせる、毒化する、帯電させるなど、属性ごとに地形へ影響する
- プレイヤーは壁を掘って逃げ道を作れる
- 一部の硬い岩はブレスを防ぐ遮蔽物になる
- 光源で飛行奇襲やブレスの予兆を読みやすくする
- 深層ほど地形が硬くなり、準備不足だと逃げ場を作りにくくなる

色違い・属性違い:

星脈竜は無限ローグライクの深度に応じて色違い・属性違いで再登場する。

- 緑竜: 毒、酸、風寄り
- 赤竜: 火
- 青竜: 氷
- 銀竜: 雷
- 金竜: 光、高威力万能寄り
- 虹竜: 全属性、最強個体

#### 専用リザルト: 星間記録

不可思議の迷宮では、通常のステージクリア/ゲームオーバーとは別に専用リザルト `星間記録` を表示します。

表示項目:

- 結果: `帰還成功` / `死亡` / `星脈竜撃破`
- 到達深度: `深度 7/9` の形式と、到達距離
- 撃破数
- 掘削数。掘ったタイルの総数です。
- 入手したアイテム数、素材量、お金
- 持ち帰り結果。成功時は `すべて持ち帰り`、死亡時は `ラン取得品は星間に消失` と表示します。
- スコア。成功時だけ、到達深度、到達距離、撃破数、掘削数、入手量、お金から算出します。
- ハイスコア。成功時のスコアが過去最高を超えた場合に更新します。

死亡時は記録として結果を表示しますが、スコア更新対象にはしません。

色ごとの地形影響:

- 緑竜: 毒沼・毒霧を残す
- 赤竜: 土や木を燃やし、火床を残す
- 青竜: 地形を凍らせ、掘削しにくくする
- 銀竜: 鉱石や金属ガラクタを伝って雷が走る
- 金竜: 強烈な光で暗闇を消すが、予兆や攻撃範囲が見えにくくなる
- 虹竜: 複数属性を連続で切り替える

虹竜:

- 超深層でのみ出現する最強個体
- 到達できるプレイヤーは極めて少ない想定
- ただし開発コストをかけすぎない
- まずは既存ドラゴン行動の高速化、全属性化、HP・攻撃力・耐性上昇、専用演出少し程度でよい
- 本格的な専用行動は、ゲームバランスが成立してから検討する

星脈竜はストーリー本編のラスボスではありません。ストーリー本編は `星封殻アストラグナ` による守護星救出で締めます。星脈竜は、ストーリー後または高難度挑戦として長期的に戦う相手です。

### エンディング方針

ラスボス `星封殻アストラグナ` を掘り抜いた後、主人公は守護星を見つけます。しかし守護星は弱っていて、自力では空へ戻れません。

モニカが地上から大魔法で、地底から空へ続く光の道を作ります。ただし、命令魔法が守護星に直接触れると砕けるため、モニカは星には触れません。

主人公はスペルリングを使い、守護星をリングの中心に入れます。スコップ、松明、盾、宝、魔導書などが守護星の周囲を回り、星を守るかごのようになります。

主人公はモニカの作った光の道を進み、守護星を空へ戻します。

チコリは一度、守護星本体へ戻ります。別れたように見えます。

しかし最後に、ルネの帽子やフードの中で小さな光がともります。チコリはルネのそばに残っています。

### ステージ設計上の注意

#### 1. 地下ステージの単調化を避ける

全ステージが地下なので、地層の違いだけで差別化すると弱くなります。各面は、別ジャンルのダンジョンに見えるくらい雰囲気を変えます。

- 面1: 土と星くずの自然洞窟
- 面2: 壊れた魔法道具の廃棄場
- 面3: 地底宇宙と結晶空洞
- フリー面: すべてが壊れて混ざった異次元坑道

#### 2. 採掘理由を常に保つ

主人公は、宝探しのためだけに掘るのではありません。落ちた守護星を壊さず救出するために掘ります。

強い命令魔法では星が砕けるため、主人公のスペルリングが必要になります。この理屈を、あらすじ、会話、チュートリアル、ボス戦、ラスト演出で一貫させます。

#### 3. スペルリングと世界観を分離しない

スペルリングは単なる攻撃手段ではありません。

- 掘る
- 照らす
- 守る
- 攻撃する
- 探知する
- 拾った道具を活かす
- 捕獲した敵を使う
- 星を壊さず周囲だけを削る
- 守護星をリングの中心に入れ、周囲の道具で包んで運ぶ

このゲームの世界では、主人公のスペルリングこそが問題解決手段です。

#### 4. ラスボスの決着は破壊だけにしない

ラスボスは倒す対象であると同時に、守護星を閉じ込める安全装置でもあります。最後は、敵を完全に破壊するより、星の周囲を掘って救い出す方がこのゲームに合います。

`星封殻アストラグナ` は、守護星を封じる外殻です。悪意のある敵ではなく、古い安全装置として扱います。主人公はアストラグナを壊すのではなく、守護星が出られる道を掘り抜きます。
## 現在実装済みの内容

- C++20 + SDL3 + CMake のプロジェクト構成
- SDL3ウィンドウ、レンダラー、メインループ、delta time
- キーボード/マウス入力の抽象化
- プレイヤー移動とカメラ追従
- プレイヤー歩行時の足元砂埃と、拠点外/ルネの家/ダンジョンで切り替わる足音SE
- プレイヤーの壁衝突
- 32x32タイル単位のチャンク式地下マップ
- 地形種別: 空洞、土、岩、鉱石
- 配列ベースの地形管理
- プレイヤー周辺チャンクのみの生成/描画
- ステージ別地形タイルシートによる床・壁画像描画
- スコップと松明の円形のスペルリング
- リング上アイテムの描画に、視覚演出のみの浮き沈みアニメーション（sin波）を適用
- リング上アイテムの地面影を追加（濃さは主人公の影と同一）
- スコップによる地形掘削
- 基本暗闇と、光源半径内だけを描画する円形視界
- 敵のオブジェクトプール
- 掘削で空洞化したタイルからの敵スポーン
- 壁を貫通しない敵移動
- プレイヤー周辺の空洞タイルを使ったBFS追跡
- 敵の接触ダメージ
- リングアイテムによる敵へのダメージ
- 敵撃破による経験値獲得
- レベルアップ時の3枚カード強化UI
- クリック可能なUI基盤と、UIクリック消費によるワールド入力抑止
- 共通タブUIは `assets/UI_tubs.png` の上半分を非選択、下半分を選択状態として描画し、縦方向の1px区切りで3分割して横幅可変にする。上下の状態分割は画像高さの半分で行うため、横方向のガイド線は不要。タブ同士は共通描画側で少し隙間を空け、画像は入力用タブ矩形から約18px外側へはみ出して描画する。ホバー中または選択中のタブは枠線ではなく同じ倍率で少し拡大して表示する。
- 縦並びタブUIも共通タブUIと同じ `assets/UI_tubs.png` を使い、左ラベルと右側の値テキストを持つ一覧として描画できる。選択・ホバー時の拡大、未選択/選択画像の使い分け、画像の外側はみ出し量は共通タブUIのルールに合わせる。
- 共通選択テーブルUIは、ヘッダー、複数列、スクロール、行/列選択、クリック選択を担当します。セル内の固有描画は各画面側に任せ、操作設定のような行列型UIで再利用します。
- 画面モード管理: `OpeningKamishibai` / `EndingKamishibai` / `Title` / `Base` / `WorldLoading` / `Playing` / `PauseMenu` / `Inventory` / `Ring` / `ObjectImageScaleEdit` / `AudioCueEdit` / `LevelUp` / `GameOver` / `StageClear` / `AstralResult`
- 紙芝居形式オープニング、仮Title画面、拠点画面、一時停止メニュー、ショートカットHUD、アイテム画面、リング画面、レベルアップ画面、ゲームオーバー画面、ステージクリア画面
- `Player::applyDamage()` 経由のHP減少、最後のダメージ原因記録、死因つきゲームオーバー表示
- プレイヤーの現在HPが最大HPの70%未満になると画面端に赤いグラデーションビネットを表示し、20%以下に近づくほど赤みと脈動を強める。被ダメージ時はダメージ量に応じて短時間だけ透明度を上乗せし、HUDより背面のスクリーンスペース演出として描画する。
- 被ダメージ後にHPが最大HPの70%未満のピンチ状態で生存している場合、またはその被ダメージで70%未満に入った場合、`se.player.pinch` を鳴らす。連続被弾や毒などのTickダメージによる鳴りすぎは `audio_manifest.tsv` の `cooldown_ms` で抑制する。
- プレイヤーHP0時のゲームオーバー遷移、簡易リザルト表示、通常ステージ所持品保持リトライ
- 8個目ワープポイント到達後の深部ボス出現、ボス撃破によるステージクリア、簡易シナリオ表示、次ステージ解禁
- 8列×3行のショートカットとアイテム画面
- アイテム画面のスロットコマンドは `使用する` / `リングへ` / `保護` / `捨てる` を表示する。拠点から開いたアイテム画面では `使用する` を半透明表示にして選択不可にする。
- `捨てる` はダンジョン中と拠点で実行できる。重要アイテム、保護中の個体アイテム、装備中の杖は捨てられない。ダンジョン中は実行後もアイテム画面を開いたままにし、プレイヤーが現在向いている方向へ既存のワールドドロップジャンプ軌道で投げる。落下地点は約310px先を基準に約40pxランダムにずらす。実行時は右側ダンジョンログに `[アイコン]アイテム名を捨てた` と表示する。
- 拠点で `捨てる` を選んだ場合は、アイテム画面の上に確認ウィンドウを重ね、`[アイコン]アイテム名を捨てますか？` と `（捨てたアイテムは再入手できません）` を表示する。確定した場合はワールドドロップを生成せず、リュックから永久に削除する。
- 捨てられたアイテムは一時ワールドドロップとして扱う。拾い直した場合は通常の拾得処理に戻るが、拠点帰還、ダンジョン状態保存、セーブ再起動では残さない。
- 最大3リング前提のリング管理画面
- 右クリック長押しによるリング中心の前方ずらし
- アクティブリングだけを投げるリング投げ。投げ中は他リングは通常どおりプレイヤー周囲に残り、リング切替は帰還まで保留する。投げたリングは進行方向側に切れ目を作り、投げ開始時のリング上の切れ目位置から線状へ滑らかに変形して発射される。到達点で減速してUターンし、戻り中はリング上の反対側の位置へ収束しながら元のリング形状へ戻る。
- FPS、敵数、アクティブチャンク数などのデバッグ表示
- `assets/fonts/craftmincho.otf` を使った日本語UIテキスト表示
- `data` / `assets` 配下の変更検知による自動リロード
- `data/game_balance.cfg` によるランタイムバランス調整
- Googleスプレッドシートの公開CSV取得
- Googleスプレッドシート `Objects` タブのアイテム定義読み込み
- 鉱石タイル破壊時のObjects DB由来アイテムドロップ、ワールド表示、接触拾得、アイテム画面表示
- メイン経路沿いの小型テンプレート生成による、鉱石壁、隣接2マス宝/敵ポケット、露出敵つき報酬、木箱アルコーブ、鉱脈の高頻度配置
- 地形生成の後段ノイズによる、土/岩/硬岩/鉱石/小空洞の1マス単位の混在
- リング装着アイテムへのObjects DBパラメータ反映
- リング装着アイテムの攻撃力、ダメージ種別、掘削力、耐久力、重さによる戦闘/掘削/速度補正
- 起動時の `OpeningKamishibai` → `Title` → `IntroTutorialDungeon` → `Base` フロー、拠点からの採掘開始、拠点限定の1ファイルセーブ/ロード基盤
- テスト起動用 `run_game.bat`
- コード変更を監視して自動ビルド/再起動する開発用 `dev_auto_reload.bat`

## ダンジョン情報密度生成

ダンジョンは、通路と大部屋だけでなく、画面内に複数の小さな発見が入る密度を目指します。目安として、1280x720表示時の1画面に小型テンプレート由来の見どころが約2個入る頻度を基準にします。

小型テンプレートは `DungeonLayout` のメイン経路進行度に沿って決定し、既存の報酬ノード、宝箱ノード、木箱ノード、敵ノード、タイル上書きを組み合わせて構成します。テンプレート自体はセーブ用の新しい永続データを増やさず、生成時に既存ノードと `TileMap` の上書きへ展開します。

`EnemyPlacementType::Exposed` の敵ノードは、ダンジョン生成時点では指定タイル付きの予約として保持し、プレイヤー光源または標準光源の範囲に入る少し手前で `EnemySystem` へ実体化します。実体化時は通常スポーンの近傍探索を使わず、ノードのタイル中心へ固定配置し、スポーン演出待機も付けません。これにより、小型テンプレートの小空洞に置かれた敵は、光源範囲に入った時点で最初からその空洞内の敵として見え、攻撃、接触、捕獲なども通常の敵と同じ処理を使います。

実装済みテンプレート:

- `OreNeedle`: 柔らかい壁の中に1マスだけ実鉱石を置く。掘ると通常の鉱石ドロップが出ます。
- `OreVein`: 4〜5マス程度の鉱脈を壁内に置く。見た目だけの鉱石ではなく、すべて `TileType::Ore` として扱います。
- `DoublePocketTreasure`: 隣接2マスの小空洞を作り、片方に宝箱/お金/月のカケラのいずれか、もう片方に露出敵または木箱を最初から見える状態で置きます。
- `BaitAndAmbush`: 見える位置にキラキラ壁または鉱石壁を置き、その奥の小空洞に露出敵を最初から見える状態で置きます。報酬は小さめにします。
- `CrateAlcove`: 小さな空洞に木箱と小銭を置き、どちらも最初から見える状態で通路脇の寄り道を増やします。

鉱石デコは使いません。鉱石に見えるものは必ず `TileType::Ore` で、掘削時に既存の鉱石ドロップ処理へ接続します。見た目だけで報酬が出ない鉱石表現は、プレイヤーの期待を裏切るため採用しません。

地形タイルは、経路距離と特殊部屋による大きな地層グループを維持しつつ、後段ノイズで1マス単位の混在を加えます。土壁の中に岩や鉱石、小空洞が混ざり、岩壁の中にも土、硬岩、鉱石、小空洞が混ざります。これにより、同じタイルが広く固まり続ける見た目を避け、掘る前の画面情報量を増やします。

## 地形タイル画像描画

ダンジョン地形は、`TileMap` の既存チャンク管理、掘削、衝突、敵経路探索を変更せず、描画時だけステージ別タイルシートへ差し替えます。画像が読み込めない場合やサイズが不足している場合は、従来の色矩形描画へフォールバックします。既存の掘削ヒビ表示は画像タイルの上に重ねて描画します。

使用する画像は `assets/tiles/tile_1.png` から `assets/tiles/tile_4.png` です。対応は `stage_01_stardust` が `tile_1.png`、`stage_02_junk_magic` が `tile_2.png`、`stage_03_star_core` が `tile_3.png`、`stage_04_astral_mine` が `tile_4.png` です。各画像は48pxセルの5列x10行スプライトシートとして扱います。1行目は床、2〜4行目は土壁、5〜7行目は岩壁、8〜10行目は鉱石壁です。既存の `HardRock` は専用行がないため岩壁行を使います。

`Empty` は床タイルを描画します。床の `col0` は基本床、`col1`〜`col4` はワールドタイル座標ハッシュによる固定ランダム差分です。`Dirt`、`Rock`、`Ore`、`HardRock` は床を下地に描いた上で、対応する壁ブロックを描画します。

壁タイルは上下左右と斜め4方向の空洞判定で選びます。優先順位は内角、外角、単純な辺、基本タイルです。基本タイル相当で輪郭判定が不要な場合だけ、壁の装飾1/2を低確率で座標固定ランダム選択します。

1枚の48pxセルだけで表現できない複合形状は、タイルを24px四方の4象限に分けて描きます。たとえば同じタイル内で左外角と右外角が同時に必要な場合は、左側象限に左外角由来の部分、右側象限に右外角由来の部分を貼り、上辺・左右辺・基本部分を融合して描画します。これにより地形データを増やさず、描画専用のオートタイル合成として扱います。

## ビルド方法

### 必要環境

- C++20対応コンパイラ
- CMake 3.22以上
- SDL3

SDL3は次のどちらかで使用できます。

- `external/SDL` にSDL3のソースを置く
- vcpkgなどで `SDL3Config.cmake` を見つけられる状態にする
- `external/SDL3-prebuilt` に同梱されたプリビルドSDL3を使う

現在の標準手順は、同梱のプリビルドSDL3を使う次のコマンドです。

```powershell
.\build_game.bat
```

またはCMakeプリセットを直接使います。

```powershell
cmake --preset windows-release
cmake --build --preset windows-release
```

ビルド成果物は `%LOCALAPPDATA%\MajoShovel` 配下に出力します。Dropbox配下の同期ロックを避けるため、リポジトリ直下にはビルド成果物を置かない方針です。

### vcpkgを使う場合

```powershell
cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=C:/path/to/vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build build --config Release
.\build\Release\MajoShovel.exe
```

### `external/SDL` を使う場合

```powershell
git clone https://github.com/libsdl-org/SDL external/SDL
cmake -S . -B build-nopch -DCMAKE_DISABLE_PRECOMPILE_HEADERS=ON
cmake --build build-nopch --config Release --target MajoShovel
.\build-nopch\Release\MajoShovel.exe
```

この環境では、Visual Studio BuildTools + 日本語を含むパスでSDL3のプリコンパイル済みヘッダー生成が失敗したため、`-DCMAKE_DISABLE_PRECOMPILE_HEADERS=ON` を指定してビルド確認しました。

## 起動方法

ビルド後、プロジェクト直下の次のbatを実行します。

```powershell
.\run_game.bat
```

`run_game.bat` は以下の順に実行ファイルを探します。

- `build-nopch\Release\MajoShovel.exe`
- `build\Release\MajoShovel.exe`
- `build-nopch\Debug\MajoShovel.exe`
- `build\Debug\MajoShovel.exe`

開発中にコード変更へ自動追従したい場合は、次のbatを使います。

```powershell
.\dev_auto_reload.bat
```

`dev_auto_reload.bat` は初回ビルド後にゲームを起動し、コード変更時は実行中のゲームを止めずに再ビルドし、成功時だけ新しい実行コピーへ切り替えます。

### 画面解像度と表示スケール

ゲーム内部の仮想画面は `1280x720` 固定です。ワールドカメラ、UIレイアウト、ロード画面、マウス座標、クリック/ドラッグ判定はすべてこの仮想画面座標を基準にします。

ウィンドウサイズやボーダレスフルスクリーン時の実表示サイズは、仮想画面全体の拡大表示先としてだけ扱います。SDL3の logical presentation を `LETTERBOX` で使い、実画面が16:9でない場合は上下または左右に黒帯を出します。横だけ、または縦だけを伸ばす非等倍拡大は行いません。

オプションの画面サイズはウィンドウ外形のサイズを変更する設定です。設定値に応じてテキスト、UI、キャラクター、背景、エフェクトは同じ比率で拡大縮小され、ゲーム内レイアウトそのものは変わりません。

### テストプレイスクリーンショット

`--test-play` 付きのテストプレイ起動中に `PrintScreen` を押すと、現在のゲーム画面をPNGとして保存します。通常起動ではこの入力をゲーム側のスクリーンショット機能として扱いません。

保存対象は固定仮想画面の `1280x720` です。ウィンドウサイズやボーダレスフルスクリーンの実表示サイズには依存せず、レターボックスの黒帯やデスクトップ全体は含めません。

保存先は `%LOCALAPPDATA%\MajoShovel\screenshots` です。`LOCALAPPDATA` を取得できない環境では `.local\MajoShovel\screenshots` を使います。

ファイル名は `majo_shovel_YYYYMMDD_HHMMSS_mmm.png` です。同名ファイルが既にある場合は `_001` から `_999` までの連番を付けます。保存要求、成功、失敗はデバッグログへ出力します。

### 起動ロード

アプリ起動時は、SDLウィンドウとRendererを作成した直後に最初のロード画面フレームを描画します。その後、音声初期化、音声マニフェスト、固定アセット、ゲームデータ初期化、ホットリロード設定、デバッグ起動モード適用を `App` の起動ロードステップとして1フレームずつ進めます。

`Game` 側の初期化も、Sheet設定、バランス、Objects、Stages、Enemies、セーブ、拠点レイアウト、画像倍率、オープニング、StoryEvent、初期画面開始に分割して進めます。これにより、ウィンドウ表示後に長時間イベント処理へ戻らない状態を避けます。

通常起動ではGoogleスプレッドシート取得を行いません。`--test-play` 付きのテストプレイ起動時だけ、`data/google_sheet_source.cfg` の `enabled=true` を有効として公開CSVを取得します。通常起動では同設定がtrueでも無視し、ローカル/フォールバックデータで起動を継続します。

## 自動リロード

自動リロードには2種類あります。

1. ゲーム内データのランタイムリロード
2. コード変更時の開発用ビルド/再起動

### ゲーム内データのランタイムリロード

実行中、ゲームは次のディレクトリを監視します。

- `data`
- `assets`

監視対象内のファイルが追加、削除、更新された場合、ゲームは変更を検知して自動リロードします。

現在のリロード内容:

- `data/game_balance.cfg` を再読み込みし、RuntimeBalanceへその場で反映
- `data/google_sheet_source.cfg` 変更時はシート設定を読み直し、RuntimeBalanceの取得元へ反映
- `assets/majo.png`, `assets/UI_window1.png`, `assets/UI_messageWindow.png`, `assets/UI_window2.png`, `assets/UI_buttons.png`, `assets/UI_tubs.png`, `assets/fonts/craftmincho.otf` を再読み込み
- `assets/taties/tatie_*.png` は任意PNGキャッシュを無効化し、次回描画時に再読み込み
- 読み込み成功時もプレイヤー、地形、敵、スペルリング、レベル状態は維持
- 画面上に短時間 `Hot reload: <変更ファイル>` 表示

読み込みに失敗した場合は、現在のバランス値または既存テクスチャを維持します。

Googleスプレッドシート由来のObjects、Stages、Enemies DBは、`--test-play` 付きのテストプレイ起動時のみ起動ロード中に読み込みます。実行中の `data` / `assets` リロードでは、DB全体の再取得や既存敵の定義ポインタ差し替えは行いません。

### ランタイム調整できる値

`data/game_balance.cfg` では、次のような値を実行中に変更できます。

- プレイヤー速度、プレイヤー半径
- 光源半径
- リング半径、リング速度
- リングずらし距離
- リング投げのクールダウン、速度、距離、最大時間、戻り速度
- 土、岩、鉱石のHP
- 敵の速度、半径、HP、経験値
- 掘削イベントカウンタのminimum/guarantee値
- 敵のソフト上限数
- レベルアップに必要な経験値係数
- 1ステージ中に保持するワールドドロップ上限数
- 収集術式の吸引半径、Lvごとの伸び、吸引加速度、同時吸引数
- 宝箱のお金/素材追加枠確率
- 宝箱報酬量のステージ倍率、深度倍率、宝箱種別倍率
- 木箱のお金/追加報酬発生確率

例:

```text
player_speed=180
light_radius=200
spell_ring_radius=72
enemy_speed=70
enemy_min_dug_tiles=16
enemy_guarantee_dug_tiles=30
world_drop_limit_per_stage=300
collection_pull_radius_base=56
collection_pull_radius_per_level=32
collection_pull_acceleration=1800
collection_pull_limit=24
dig_money_min_dug_tiles=6
dig_money_guarantee_dug_tiles=12
dig_item_min_dug_tiles=10
dig_item_guarantee_dug_tiles=30
loot_money_chance=0.30
loot_material_chance=0.30
loot_stage_multiplier_ST=1.0
loot_stage_multiplier_JK=1.2
loot_stage_multiplier_SC=1.5
loot_stage_multiplier_AS=1.0
loot_depth_multiplier_1=1.0
loot_depth_multiplier_2=1.3
loot_depth_multiplier_3=1.7
loot_depth_multiplier_AS_1=1.0
loot_depth_multiplier_AS_2=1.1
loot_depth_multiplier_AS_3=1.2
loot_depth_multiplier_AS_4=1.4
loot_depth_multiplier_AS_5=1.7
loot_depth_multiplier_AS_6=2.0
loot_depth_multiplier_AS_7=2.4
loot_depth_multiplier_AS_8=2.9
loot_depth_multiplier_AS_9=3.5
loot_grade_multiplier_C=1.0
loot_grade_multiplier_R=1.8
loot_grade_multiplier_S=3.0
crate_money_chance=0.30
crate_bonus_chance=0.30
enemy_mana_drop_chance=0.10
enemy_moon_fragment_chance=0.01
boss_mana_drop_chance=0.80
boss_moon_fragment_chance=0.15
ore_material_min=1
ore_material_max=3
```

保存すると、実行中のゲームが自動で再読み込みします。

## Googleスプレッドシート連携

### 目的

Googleスプレッドシート上で調整・管理するデータを、テストプレイ起動時に公開CSVとして取得します。通常起動では取得せず、ローカル/フォールバックデータで起動します。

現在は次の4種類を読み込みます。

- バランス値: 既存の `RuntimeBalance` へ反映
- `Objects` タブ: アイテム定義を `ObjectCatalog` / `ItemRegistry` へ読み込み
- `効果コード一覧` タブ: 効果コード定義を `ObjectCatalog::effectCodes` へ読み込み
- `特殊タグ一覧` タブ: 特殊タグ定義を `ObjectCatalog::specialTags` へ読み込み

### 設定ファイル

設定は `data/google_sheet_source.cfg` に記述します。

```text
enabled=true
spreadsheet_id=1TP16zcNKS4RDdf7UryeePqIzxBftLTj-xFxDADmByZc
gid=0
objects_sheet=Objects
stages_sheet=Stages
```

- `enabled`: `true` の場合のみスプレッドシートを参照します。
- `spreadsheet_id`: GoogleスプレッドシートURL内のIDです。
- `gid`: バランス値として読むシートのgidです。
- `objects_sheet`: アイテム定義を読むタブ名です。現在は `Objects` を想定します。
- `stages_sheet`: ステージ定義を読むタブ名です。現在は `Stages` を想定します。

スプレッドシートは認証なしの公開CSVとして取得します。そのため、対象シートはリンクを知っているユーザーが閲覧できる状態、または公開状態にしておく必要があります。

### 読み込みタイミング

スプレッドシートは `--test-play` 付きの起動時に一度だけ読み込みます。通常起動では `data/google_sheet_source.cfg` の `enabled=true` が設定されていても取得しません。

起動後にスプレッドシートを編集しても、実行中のゲームには反映されません。反映するにはゲームを再起動します。

`data/game_balance.cfg` は引き続き実行中リロードに対応します。スプレッドシート読み込みに失敗した場合、バランス値はローカルの `data/game_balance.cfg` にフォールバックします。

### バランス値シート

バランス値シートはCSVの1列目をキー、2列目を値として読みます。ヘッダー行 `key,value` はあっても構いません。

例:

```csv
key,value
player_speed,150
light_radius,200
spell_ring_radius,48
enemy_hp,5
```

キー名は `data/game_balance.cfg` と同じ名前を使います。

### Stagesタブ

`Stages` タブは、従来分けていたステージ基本情報と生成パラメータを統合したステージDBです。`StageParams` タブは作らず、ゲーム起動時に `StageCatalog` として読み取り、保持、検証、ログ確認までを行います。現段階では `DungeonLayout` 生成、地形生成、ワープポイント配置、特殊部屋配置へ接続します。敵・報酬・アイテム出現の本格制御は各DB側のステージ別重みを使います。

`Stages` タブは1行目を列見出しとして扱い、列名ベースで読み込みます。列順が変わっても動作し、必須列がない場合や数値変換に失敗した場合は `StageCatalog::validationWarnings` に警告を追加して、可能な範囲で読み込みを継続します。取得またはパースに失敗した場合はゲーム起動を継続し、デフォルト4ステージへフォールバックします。

| 列名 | 型 | 意味 |
|---|---|---|
| `ステージID` | 文字列 | `StageCatalog::stagesById` のキーです。空IDの行は無視します。 |
| `ステージ名` | 文字列 | ログや将来の表示に使う名称です。 |
| `種別` | 文字列 | ストーリー、ローグライクなどの分類です。 |
| `表示順` | int | ステージ一覧用の並び順です。数値変換失敗時は `999` です。 |
| `実装状態` | 文字列 | 実装・調整状態のメモです。 |
| `生成プロファイル` | 文字列 | コード側の生成プリセットを選ぶキーです。DBに内部比率を直接持たせません。 |
| `地形プロファイル` | 文字列 | コード側の地形プリセットを選ぶキーです。DBに内部比率を直接持たせません。 |
| `ゴール距離タイル` | int | 開始地点からゴールまでのおおよそのタイル距離です。既定値は `320` です。 |
| `迂回度` | double | 主経路の曲がりや寄り道の強さです。既定値は `0.30` です。 |
| `分岐密度` | double | 分岐経路の量を表す調整値です。既定値は `0.25` です。 |
| `空洞幅倍率` | double | 空洞幅プリセットへの倍率です。既定値は `1.00` です。 |
| `地形硬度倍率` | double | 地形硬度プリセットへの倍率です。既定値は `1.00` です。 |
| `ワープポイント数` | int | ワープポイント配置数です。既定値は `0` です。 |
| `特殊部屋数` | int | 特殊部屋配置数です。既定値は `0` です。 |
| `ボス敵ID` | 文字列 | ステージボスとして出現させる `Enemies.ID` です。空欄時はステージIDから既定値へフォールバックします。 |

既知の生成プロファイルキーは `natural_cave`、`junk_layer`、`star_core`、`astral_rogue` です。既知の地形プロファイルキーは `soft_stardust`、`junk_mixed`、`hard_star_core`、`chaos_astral` です。未知キーはクラッシュさせず警告として扱います。

`astral_rogue` またはローグライク扱いのステージでは、`特殊部屋数` が5未満でも生成側で最低5部屋へ補正します。通常生成で使う特殊部屋は `OreRoom`、`CoinRoom`、`TreasureRoom`、`EnemyRoom` です。`SafeCavern` は休憩空洞イベント削除に合わせて通常生成ローテーションから外します。

検証では、ステージIDの空欄と重複、ステージ名の空欄、表示順の数値変換、各数値の範囲、生成プロファイルと地形プロファイルの空欄および未知キーを確認します。アイテムID、報酬ID、素材ID、宝ID、光源倍率は `Stages` から読みません。

フォールバック時は `stage_01_stardust`、`stage_02_junk_magic`、`stage_03_star_core`、`stage_04_astral_mine` の4ステージを登録します。起動ログには読み込み成功/失敗、フォールバック使用有無、ステージ数、各ステージID・ステージ名・生成プロファイル・地形プロファイル、検証警告を出します。

既定のボス敵IDは、`stage_01_stardust=stardust_mole`、`stage_02_junk_magic=junk_crab`、`stage_03_star_core=astragna`、`stage_04_astral_mine=star_vein_dragon` です。

#### Stages読み取り結果の現在の利用範囲

現時点で `Game` は `StageCatalog` をメンバーとして保持し、選択中ステージ用に `currentStageId` を持ちます。初期値は `stage_01_stardust` です。`currentStageId` が `StageCatalog` に存在しない場合は表示順が最小のステージへフォールバックし、それも存在しない場合はコード内の `stage_01_stardust` 相当の定義を使って起動を継続します。

現在の利用範囲は、`currentStageId` と解決済み `StageDefinition` の保持、ダンジョン入口の行き先選択、起動ログ、F1デバッグ表示、`DungeonLayout` 生成、`TileMap` 地形生成、ワープポイント配置、特殊部屋配置、ボス敵ID解決です。確認できる項目はステージID、ステージ名、種別、生成プロファイル、地形プロファイル、ゴール距離タイル、地形硬度倍率、ワープポイント数、特殊部屋数、ボス敵IDです。既存の数値 `current_stage` はGameOver、StageClear、セーブ互換のため維持します。

`StageDefinition` のゴール距離タイル、生成プロファイル、地形プロファイル、迂回度、分岐密度、空洞幅倍率、地形硬度倍率、ワープポイント数、特殊部屋数は、採掘開始時のダンジョン生成入力として使います。`ボス敵ID` はボス出現時に `EnemySystem` へ渡します。ローグライク種別または `astral_rogue` 生成プロファイルのステージは、通常ステージ状態保存やワープポイント再入場の対象外として扱います。

### Objectsタブ

`Objects` タブは1行目を列見出しとして扱い、列名ベースで読み込みます。読み込んだ各行は正規化済みの `ObjectDefinition` に変換し、`ObjectCatalog` に保持します。参照用に `ItemRegistry` と `ObjectCatalog::objectsById` も作成し、ID検索、カテゴリ検索、タグ検索ができます。

現在読む列:

| 列名 | 型 | 説明 |
| --- | --- | --- |
| `ID` | 文字列 | アイテムID。必須、一意です。 |
| `名前` | 文字列 | アイテム名。必須です。 |
| `カテゴリ` | 文字列 | 想定カテゴリ内の値です。 |
| `説明文` | 文字列 | UIや説明文用の本文です。 |
| `レア度` | 整数 | 1〜10です。 |
| `価格` | 整数 | 0以上です。 |
| `通常効果` | 文字列 | `none` またはEffectSpec文字列です。カテゴリ `杖` では通常使用効果ではなく杖装備効果として扱います。 |
| `軌道効果` | 文字列 | `none` またはEffectSpec文字列です。 |
| `攻撃力` | 整数 | 0以上です。通常の接触ダメージ量として扱います。 |
| `ダメージ種別` | 文字列 | 想定ダメージ種別内の値です。 |
| `掘削力` | 整数 | 0以上です。 |
| `耐久力` | 整数 | -1以上です。-1は壊れない扱いです。 |
| `重さ` | 小数 | kg単位の重さ。0以上です。 |
| `画像番号` | 整数 | 0以上です。`obj_画像番号.png` の画像ファイル番号として使います。0は画像なし扱いです。 |
| `地面角度` | 小数 | 地面ドロップ、アイテム画面、アイコン表示、基本プレビューで使う画像回転角度です。度数法で、空欄は0度です。 |
| `リング角度` | 文字列 | リング上で使う画像角度挙動です。`fixed`、`outward`、`forward`、`spin:速度`、`forward:+180` などを指定できます。空欄は`fixed`です。 |
| `特殊タグ` | 文字列 | 区切り文字でタグ配列に変換します。 |
| `効果テキスト` | 文字列 | 表示用の効果説明です。 |
| `ST_C1` などの宝箱抽選重み列 | 数値 | 任意列です。宝箱抽選の出現率制御専用で、効果発動や特殊タグ判定には使いません。 |

任意の宝箱抽選重み列は次の54列です。列が存在しない場合、空欄、未定義、数値変換失敗は0扱いです。重み0以下のアイテムは、その宝箱抽選では抽選対象外です。

```text
ST_C1 ST_R1 ST_S1 ST_C2 ST_R2 ST_S2 ST_C3 ST_R3 ST_S3
JK_C1 JK_R1 JK_S1 JK_C2 JK_R2 JK_S2 JK_C3 JK_R3 JK_S3
SC_C1 SC_R1 SC_S1 SC_C2 SC_R2 SC_S2 SC_C3 SC_R3 SC_S3
AS_C1 AS_R1 AS_S1 AS_C2 AS_R2 AS_S2 AS_C3 AS_R3 AS_S3
AS_C4 AS_R4 AS_S4 AS_C5 AS_R5 AS_S5 AS_C6 AS_R6 AS_S6
AS_C7 AS_R7 AS_S7 AS_C8 AS_R8 AS_S8 AS_C9 AS_R9 AS_S9
```

略号:

```text
ST = stage_01_stardust
JK = stage_02_junk_magic
SC = stage_03_star_core
AS = stage_04_astral_mine
C = 通常宝箱
R = レア宝箱
S = 超レア宝箱
```

深度ランクはST/JK/SCが1〜3、ASが1〜9です。列名解決ではこの範囲へクランプします。

`ObjectDefinition` の内部構造:

```cpp
struct ObjectLootWeights {
    std::unordered_map<std::string, double> byColumn;
};

struct ObjectDefinition {
    std::string id;
    std::string name;
    std::string category;
    std::string description;
    int rarity;
    int price;
    std::vector<EffectSpec> normalEffects;
    std::vector<EffectSpec> orbitEffects;
    int attackPower;
    std::string damageType;
    int digPower;
    int durability;
    double weightKg;
    std::vector<std::string> tags;
    std::string effectText;
    ObjectLootWeights lootWeights;
};
```

`ObjectCatalog::objects` は読み込み順の一覧、`ObjectCatalog::objectsById` は `key = ID` のMapです。IDが重複した場合は警告し、ID Mapでは最初に読み込んだ定義を保持します。

宝箱抽選重みは `ObjectDefinition::lootWeights` に保持します。現在ステージID、深度ランク、宝箱種別から `ST_C1` 形式の列名を解決し、その列の重みが1以上のアイテムだけを候補にします。この重み列は出現率制御専用です。特殊タグは分類・検索・判定用であり、特殊タグや重み列から直接効果は発動しません。

想定カテゴリ:

```text
掘削, 探索, 軌道, 宝, 回復, 強化, 弱体, 魔導書, 武器, 盾, 杖
```

想定ダメージ種別:

```text
none, physical, fire, ice, thunder, wind, earth, water, magic
```

#### 効果文字列

`通常効果` と `軌道効果` は次の形式で読みます。`none` は空の `EffectSpec` 配列になります。

```text
target:effect:value:duration
target:[effect1,effect2]:[value1,value2]:duration
packet1;packet2
```

例:

```text
player:heal:30:0
orbit:[orbit_gravity,orbit_power]:[0.85,1.15]:0
projectile:guard:1:0;projectile:reflect_physical_chance:40:0
```

カテゴリ `杖` の `通常効果` 欄だけは、通常使用効果ではなく杖装備効果として読みます。Objectsタブに杖専用列は追加しません。杖装備効果用targetは `equip_all`、`equip_ring1`、`equip_ring2`、`equip_ring3` です。これらはカテゴリ `杖` の `通常効果` 欄でのみ許可し、カテゴリ `杖` 以外や `軌道効果` 欄で使った場合は検証警告を出します。カテゴリ `杖` の `軌道効果` は、杖をリングへ乗せた時の従来どおりのリング搭載時効果として扱います。

内部構造:

```cpp
struct EffectSpec {
    std::string target;
    std::vector<std::string> effects;
    std::vector<double> values;
    double duration;
};
```

DB上では効果が1種類だけなら `[]` で囲みません。内部構造では `effects` と `values` は常に配列です。複数効果では `effects` と `values` の数が一致している必要があります。

通常の接触ダメージは `攻撃力` と `ダメージ種別` を正とします。軌道効果に `enemy:damage` は原則不要です。リング投げは軌道ごと投げるシステムなので、アイテム個別の投げ効果は作りません。

非致死接触武器は `通常効果=none`、`軌道効果=enemy:nonlethal_hit:1:0` として定義します。`nonlethal_hit` は通常使用では発動せず、リング上の該当アイテムが敵へ接触した時だけ、攻撃力、ダメージ種別、速度補正、会心、敵耐性、防御、正面ガードを反映した最終ダメージを、敵HPが1未満にならないよう丸めます。対象のHPが1の時はHPダメージ0になり、撃破、経験値、撃破ドロップは発生しません。

虫とりアミ系アイテムは `通常効果=none`、`軌道効果=enemy:capture_net:<捕獲率倍率>:<再判定秒数>` として定義します。`capture_net` は通常使用では発動せず、リング上の該当アイテムが敵へ接触した時だけ捕獲判定を行います。`value` は既存の捕獲率計算へ掛ける倍率、`duration` は同じアミが次に捕獲判定できるまでの秒数です。`duration=0` では既定値0.75秒を使います。判定を行った時点で耐久を1消費し、成功時は捕獲吸収演出を開始します。捕獲アイテムの所持品追加は、演出で敵がプレイヤーへ収納された後に確定します。

虫眼鏡は `通常効果=none`、`軌道効果=enemy:inspect_enemy:<観察強度>:<再判定秒数>` として定義します。`inspect_enemy` は通常使用では発動せず、リング上の虫眼鏡が敵へ接触した時だけモンスター詳細を記録します。初めて観察した敵は敵図鑑を `完全確認済み` に更新し、採掘画面上に `モンスター発見：<名前>`、`HP <値> / 攻撃 <値>`、説明文のポップアップを表示します。捕獲後アイテムの効果詳細は、この処理では開示せず、アイテム図鑑側の通常の効果発見ルールに任せます。

#### 効果パースと検証

`通常効果` と `軌道効果` は読み込み時に `EffectSpec[]` へパースします。

- `none` は空配列として扱います。
- `;` 区切りで複数パケットに分割します。
- 単一効果の `effect` は1要素の `effects` 配列に変換します。
- 単一値の `value` は1要素の `values` 配列に変換します。
- `[effect1,effect2]` と `[value1,value2]` は同じ要素数である必要があります。不一致の場合は読み込みエラーです。
- `value` と `duration` は数値へ変換します。数値変換できない場合は警告を出し、値は0として保持します。
- 効果コード一覧に存在しない `effect` がある場合は警告します。
- 効果コード一覧の `対応target` に含まれない `target` / `effect` の組み合わせがある場合は警告します。
- `equip_all`、`equip_ring1`、`equip_ring2`、`equip_ring3` はカテゴリ `杖` の `通常効果` 欄だけで許可します。それ以外の欄やカテゴリで使った場合は警告します。

検証警告は `ObjectCatalog::validationWarnings` に保持し、起動時ログへ一覧出力します。起動時ログには杖カテゴリ件数、杖の通常効果パース件数、equip系target検証警告件数も出力します。警告は実効果処理を行うものではなく、DB定義の整合性確認用です。

#### 特殊タグパースと検証

`特殊タグ` は読み込み時に文字列配列へ変換します。

- `,`、`;`、`|`、`、` を区切り文字として扱います。
- 各タグの前後空白をtrimします。
- 空タグは除外します。
- 特殊タグ一覧に存在しないタグがある場合は警告します。
- 特殊タグ一覧の `排他グループ` が同じタグを同一アイテムが複数持つ場合は警告します。
- 効果コード一覧の `効果コード` と同名のタグがObjects側にある場合は警告します。

特殊タグは分類、条件判定、検索、相性判定、演出SEルーティングのためのラベルです。タグだけでゲーム効果は発動しません。例えば `poison` タグだけで毒を付与せず、`shield` タグだけでガード処理をせず、`light_source` タグだけで照明処理をしません。実際の効果処理は `通常効果` / `軌道効果` の効果コードで行います。

#### 検証

読み込み時に最低限、次を検証します。

- `ID` が空でない
- `ID` 重複がない
- `名前` が空でない
- `カテゴリ` が想定カテゴリ内
- `レア度` が1〜10
- `価格`、`攻撃力`、`掘削力`、`重さ` が0以上
- `耐久力` が-1以上
- `ダメージ種別` が想定値内
- `通常効果`、`軌道効果` が `none` または正しいEffectSpec形式
- 複数効果では `effects` と `values` の数が一致する
- 効果コード一覧に存在しない効果コードは警告する
- `対応target` と一致しない `target` / `effect` の組み合わせは警告する
- equip系targetがカテゴリ `杖` の `通常効果` 欄以外で使われていない
- 特殊タグ一覧に存在しないタグは警告する
- 同じ排他グループの特殊タグが同一アイテムに複数ある場合は警告する
- 効果コードと同名の特殊タグがObjects側にある場合は警告する

数値列は読み込み時に数値型へ変換します。数値変換できない場合、または範囲外の場合は警告し、既定値を入れて読み込みを継続します。現在の既定値は `レア度=1`、`価格=0`、`攻撃力=0`、`掘削力=0`、`耐久力=0`、`重さ=0.0`、`画像番号=0`、`地面角度=0` です。

現在は読み込んだ内容を `ObjectCatalog` / `ItemRegistry` として保持し、起動時ログへ件数と簡易検索結果を出力します。鉱石タイル破壊時のワールドドロップ、拾得、インベントリ内のObjects DBアイテム表示、リング描画、最小セットの通常効果使用へ接続済みです。ショップ処理は今後の実装対象です。

オブジェクト画像は `ObjectImageRenderer` の共通関数を経由して描画します。`画像番号` から `assets/objects/obj_番号.png` を解決し、描画先ごとに与える最大サイズへアスペクト比維持で縮小表示します。標準ではスケールに依存しない1px黒アウトラインを付けて描画します。実際の縮小、Nearestフィルタ、アウトライン、選択中アウトラインの描画処理は `ScaledImageRenderer` に集約し、Objects以外の画像でも同じ経路を使います。アイテムとして表示する場合は `ItemImageRenderer` を入口にし、通常Objectsは `ObjectImageRenderer`、捕獲敵アイテムは `EnemyImageRenderer` の敵スプライト切り出しへ振り分けます。地面ドロップ、アイテム画面、インラインアイコン、デバッグの基本プレビューは `地面角度` を適用して描画します。リング上の描画だけは `リング角度` を適用し、外向き、進行方向、自転などの挙動を個別に使い分けます。さらにリング上では、上下の浮遊揺れと同じ周期の小さなCos角度揺れを最終表示角度へ加えます。

Objects DBに属さないワールド用アイコンは `WorldIconRenderer` で意味付きIDへ集約し、`assets/others/img_番号.png` から読み込みます。対応は `img_1=お金小額`、`img_2=お金中額`、`img_3=お金高額/所持金アイコン`、`img_4=宝箱`、`img_5=宝箱開封`、`img_6=レア宝箱`、`img_7=レア宝箱開封`、`img_8=超レア宝箱`、`img_9=超レア宝箱開封`、`img_10=木箱`、`img_11=古木の建材`、`img_12=強化鉱石`、`img_13=月のカケラ`、`img_14=魔力のしずく`、`img_15=ワープポイント`、`img_16=ワープ案内図の台座`、`img_17=ワープ案内図の浮遊地図`、`img_18=巣穴`、`img_19=光る岩`、`img_20=受電石/未通電`、`img_21=荷物`、`img_22=未点火の焚き火`、`img_23=重い岩`、`img_24=受電石/通電中` です。お金ドロップは量に応じて小額/中額/高額を選び、素材ドロップ、上部HUD素材カウンタ、画像化済みダンジョンイベント代表物も同じアイコン経路で描画します。埋まり魔女のガレキは専用 `others` 画像を持たず、そのダンジョンの岩壁タイルとして描画・掘削します。

デバッグの編集ツール `画像サイズ編集` は `Objects` / `Others` のタブを持ちます。`Objects` では `assets/objects/obj_番号.png` を使うObjects DBアイテムの個別表示倍率、`Others` では `assets/others/img_番号.png` を使うワールド用アイコンの個別表示倍率を編集します。保存先は `data/object_image_scale.cfg` で、現行形式は `MAJO_IMAGE_SCALE_V2`、行種別は `object <objectId> <scale>` と `other <worldIconKey> <scale>` です。旧形式 `MAJO_OBJECT_IMAGE_SCALE_V1` の `scale <objectId> <scale>` も読み込み互換として扱います。

デバッグの編集ツール `当たり判定編集` は `Enemies` / `Objects` タブを持ちます。`Enemies` では敵ごとの攻撃対象判定、`Objects` ではリング上アイテムごとの攻撃判定を編集します。保存先は `data/hitboxes.cfg` で、現行形式は `MAJO_HITBOX_V2`、行形式は `enemy <enemyId> circle <offsetX> <offsetY> <radius>`、方向別の `enemy <enemyId> <down|left|right|up> circle <offsetX> <offsetY> <radius>`、`object <objectId> circle <offsetX> <offsetY> <radius>` です。敵の方向未指定行は共通判定として扱い、方向別定義がない向きは共通判定へフォールバックします。1対象・1方向につき最大8個の円を持てます。未定義の敵は従来の敵半径1円、未定義のリング上アイテムは従来の `hitRadius` 1円へフォールバックします。捕獲してリングに乗せた敵は `Objects` タブには表示せず、リング上でも `object captured_*` 判定ではなく元の敵IDの `enemy` 判定をそのまま使います。読み込み時に古い `object captured_*` 判定があれば無視します。編集UIは単体コピー/貼付と敵専用の全方向コピー/貼付を分け、左右反転貼付も選べます。全方向コピーは定義済みの共通/方向別プロファイルだけを保持し、全方向貼付では貼付先の敵方向プロファイルをコピー元の構成へ置き換えます。左右反転の全方向貼付では左/右プロファイルを入れ替え、各円のXオフセットを反転します。旧形式 `MAJO_HITBOX_V1` と `data/enemy_hitboxes.cfg` / `MAJO_ENEMY_HITBOX_V1` は読み込み互換として扱い、保存時は新形式へ出力します。

ワールドドロップでは、最初の候補カテゴリを `掘削`、`探索`、`回復`、`武器`、`盾`、`宝` に限定します。候補DB内にドロップ系特殊タグが存在する場合は、カテゴリに加えて `drop`、`dig_drop`、`drop_dig`、`ore_drop`、`treasure_drop`、`mining`、`loot`、`treasure`、`ドロップ`、`掘削ドロップ`、`宝` のいずれかのタグを持つアイテムだけを候補にします。該当タグがDB側にまだ存在しない場合は、初期データとの互換性を優先してカテゴリのみで候補化します。`no_drop`、`nodrop`、`shop_only`、`ショップ専用` タグを持つアイテムは除外します。

ドロップ抽選は `レア度` を重みとして使います。現時点では `レア度` が低いほど出やすく、高いほど出にくい扱いです。Objects DBが空、不正ID、候補なしの場合はクラッシュさせず警告ログを出して処理を継続します。

採掘道具セーフティとして、壁を壊した直後にリング上・インベントリ内・プレイヤー近くのワールドドロップのいずれにも、使用可能な採掘道具が存在しない場合は、保険として `item_shovel` をワールドドロップ生成します。使用可能な採掘道具は、Objects DB のカテゴリが `掘削` で、壊れていないアイテムとします。生成位置は壊れた壁タイル付近の安全な落下地点を選び、出現演出は壊れた壁タイルを起点にしたジャンプドロップとして扱います。生成時には右側ログで壁からスコップが出たことを通知します。連続生成による増殖を避けるため、生成後は一定時間のクールダウンを置きます。インベントリ内に採掘道具がある場合は、リングから外しているだけなのでセーフティ生成は行いません。

### 効果コード一覧タブ

`効果コード一覧` タブは1行目を列見出しとして扱い、列名ベースで読み込みます。読み込んだ各行は `EffectCodeDefinition` に変換し、`ObjectCatalog::effectCodes` に `key = 効果コード` の `std::unordered_map` として保持します。

現在の公開シートでは移行前のタブ名 `#効果コード` も使われているため、読み込み時は `効果コード一覧` を優先し、取得またはパースできない場合に `#効果コード` へフォールバックします。

現在読む列:

| 列名 | 型 | 説明 |
| --- | --- | --- |
| `効果コード` | 文字列 | Mapのキーです。空の場合、その行は無視します。 |
| `表示名` | 文字列 | 人間向けの表示名です。 |
| `分類` | 文字列 | 回復、バフ、採掘、軌道補正などの分類です。 |
| `使用可能欄` | 文字列 | `通常効果` / `軌道効果` など、DB上で使える欄の説明です。 |
| `対応target` | 文字列配列 | カンマ区切りで配列に変換します。 |
| `value意味` | 文字列 | EffectSpecの `values` の意味です。 |
| `duration意味` | 文字列 | EffectSpecの `duration` の意味です。 |
| `重複ルール` | 文字列 | 同じ効果が複数ある場合の設計メモです。 |
| `実装状態` | 文字列 | 未実装、使用中、疑似コードなどの状態です。 |
| `備考` | 文字列 | 補足メモです。 |

内部構造:

```cpp
struct EffectCodeDefinition {
    std::string code;
    std::string displayName;
    std::string category;
    std::string usableField;
    std::vector<std::string> targets;
    std::string valueMeaning;
    std::string durationMeaning;
    std::string stackingRule;
    std::string implementationState;
    std::string note;
};
```

このシートは効果コードの定義・説明を参照しやすくするためのDBです。HP回復、毒、麻痺、眠り、バフ、デバフ、属性、採掘、防御、反射、軌道補正などの実処理はまだ実装しません。

### 特殊タグ一覧タブ

`特殊タグ一覧` タブは1行目を列見出しとして扱い、列名ベースで読み込みます。読み込んだ各行は `SpecialTagDefinition` に変換し、`ObjectCatalog::specialTags` に `key = 特殊タグ` の `std::unordered_map` として保持します。

現在の公開シートでは移行前のタブ名 `#特殊タグ` も使われているため、読み込み時は `特殊タグ一覧` を優先し、取得またはパースできない場合に `#特殊タグ` へフォールバックします。

現在読む列:

| 列名 | 型 | 説明 |
| --- | --- | --- |
| `特殊タグ` | 文字列 | Mapのキーです。空の場合、その行は無視します。 |
| `表示名` | 文字列 | 人間向けの表示名です。 |
| `分類` | 文字列 | アイテム種別、素材、属性、挙動補助などの分類です。 |
| `用途` | 文字列 | タグの設計用途です。 |
| `対象カテゴリ` | 文字列配列 | カンマ区切りで配列に変換します。 |
| `排他グループ` | 文字列 | 同時付与を避けたいグループ名です。 |
| `関連効果コード` | 文字列配列 | カンマ区切りで配列に変換します。 |
| `実装状態` | 文字列 | 未実装、使用中、未使用などの状態です。 |
| `備考` | 文字列 | 補足メモです。 |

内部構造:

```cpp
struct SpecialTagDefinition {
    std::string tag;
    std::string displayName;
    std::string category;
    std::string usage;
    std::vector<std::string> targetCategories;
    std::string exclusiveGroup;
    std::vector<std::string> relatedEffectCodes;
    std::string implementationState;
    std::string note;
};
```

このシートは特殊タグの定義・説明を参照しやすくするためのDBです。タグ単体による実際の効果発動、属性処理、ステート処理、バフ処理、排他制御は行いません。現在は排他グループの衝突を検証警告として検出し、演出用途としてリングアイテム衝突SEのルーティングに参照します。

### 文字化け対策

- 仕様書、設定ファイル、C++ソースはUTF-8で保存します。
- MSVCビルドでは `/utf-8` を指定します。
- 日本語UI表示は `assets/fonts/craftmincho.otf` をGDI+で読み込み、UTF-8文字列から描画します。
- Googleスプレッドシートから取得するCSVはUTF-8として扱います。
- 文字化けが発生した場合は、PowerShellやエディタがShift_JISとして開いていないか確認します。

### コード変更時の開発用リロード

`dev_auto_reload.bat` を使うと、C++コード変更時に外部ウォッチャーが自動で再ビルドし、成功時だけゲームを短く再起動します。

監視対象:

- `src`
- `data`
- `assets`
- `CMakeLists.txt`

処理手順:

1. 変更を検知
2. 約0.7秒待って連続保存をまとめる
3. 実行中のゲームは `.dev-run` 配下のコピーから動かしたままにする
4. `cmake -S . -B build-nopch -DCMAKE_DISABLE_PRECOMPILE_HEADERS=ON` を必要時に実行
5. `cmake --build build-nopch --config Release --target MajoShovel` を実行
6. ビルド成功時だけ実行中のゲームを停止し、新しいコピーを起動
7. ビルド失敗時は現在のゲームを止めず、次の保存を待つ

`data` と `assets` は実行中プロセス内で軽量ホットリロードします。`data/game_balance.cfg` はワールドやウィンドウを作り直さずにRuntimeBalanceへ反映し、`data/hitboxes.cfg` は敵・リング上アイテムの当たり判定カタログへ反映します。旧互換用の `data/enemy_hitboxes.cfg` も変更検知時に読み込み対象へ含めます。`assets/majo.png`, `assets/UI_window1.png`, `assets/UI_messageWindow.png`, `assets/UI_window2.png`, `assets/UI_buttons.png`, `assets/UI_tubs.png`, `assets/fonts/craftmincho.otf` はSDLウィンドウを維持したまま再読み込みします。`assets/objects/obj_*.png`、`assets/others/img_*.png`、`assets/tiles/tile_1.png`〜`assets/tiles/tile_4.png`、`assets/taties/tatie_*.png` は任意PNGキャッシュを無効化し、次回描画時に再読込します。読み込みに失敗した画像は既存テクスチャを維持するか、地形タイルでは従来の色矩形描画へフォールバックします。

これはC++バイナリをプロセス内で差し替えるホットリロードではありません。現在の段階では、安全で単純な「実行コピーを使い、成功ビルド後だけ再起動する」方式を採用しています。

## 操作方法

- `WASD` / 矢印キー: 移動
- マウス位置: 向き
- `C`: アクティブリングを投げる
- 右クリック長押し: リング中心を前方へずらす
- `Q` / `E`: ショートカットカーソル左 / 右
- マウスホイール: ショートカットカーソル左 / 右
- `1`〜`3`: リングプリセット1〜3を呼び出す。未登録の場合は何も変更せずメッセージを出す
- `4`〜`8`: 表示中ショートカット枠を直接選択
- `Tab`: ショートカット行切替
- `F`: 選択中アイテムを使用
- `R`: 選択中アイテムをアクティブリングに乗せる
- `T`: アイテム画面と収納箱画面では並び替え、リング画面では装着アイテムの整列
- `Shift+R`: リング画面で装着アイテムを可能な分だけ全部取る
- 虫とりアミ系アイテムは通常使用できません。リングへ乗せ、リング上のアミが敵に当たった時だけ捕獲判定を行います。
- `G`: アイテム画面でアイテムをつかむ / 置く
- `Z` / `X`: アクティブリング切替
- `Esc`: 一時停止メニュー
- `I`: アイテム画面
- ゲームパッド左スティック: 移動 / UI項目移動
- ゲームパッド右スティック: 向き / 照準
- ゲームパッド `A` / South: 決定 / 選択中アイテムを使用
- ゲームパッド `B` / East: 戻る / キャンセル
- ゲームパッド `Y` / North: アイテム画面
- ゲームパッド `X` / West: アイテム画面と収納箱画面では並び替え、リング画面では装着アイテムの整列
- ゲームパッド `LT` 長押し: リング中心をずらす。右スティック入力中はその方向へずらし、入力なしなら現在向いている方向へずらす
- ゲームパッド `RT`: アクティブリングを投げる
- ゲームパッド `LB` / `RB`: アクティブリング切替
- ゲームパッド十字キー左 / 右: ショートカットカーソル左 / 右
- ゲームパッド十字キー上: ショートカット行切替
- ゲームパッド十字キー下: 選択中アイテムをアクティブリングに乗せる
- ゲームパッド `LT` + 十字キー下: リング画面で装着アイテムを可能な分だけ全部取る
- ゲームパッド `Start`: 一時停止メニュー
- ゲームパッド `Back` / View: 選択中アイテムの保護切替
- ゲームパッド左右スティック押し込み: アイテム画面 / リング画面でアイテムをつかむ / 置く
- 拠点画面中の `WASD` / 矢印キー: 拠点マップ上を移動
- 初回の `OpeningKamishibai` 中のクリック、`Enter`、`F`、`Esc`、`Space` など: スキップ、ページ送り、早送りは不可。全ページを `duration` 通りに自動再生する
- 2回目以降の `OpeningKamishibai` 中の左クリック、`Enter`、`F`: 確認なしで紙芝居を即終了し、`Title` へ進む。ページ送りではない
- `OpeningKamishibai` 中のテキストなしページ: 入力待ちは行わず、`duration` で自動進行する
- 仮 `Title` 画面中の左クリック、`Enter`、`F`: `Base` へ進む。`Esc` では終了しない
- 拠点画面中の `Enter`: 近くの施設を調べる。複数施設が範囲内にある場合は、魔女の向き方向にある施設を優先し、向き方向に候補がない場合は距離が一番近い施設を優先
- 拠点画面中の左クリック: 近くにいる状態で、マウスカーソルが施設画像の表示矩形上にある施設だけを調べる。遠距離クリックでは調べられない
- 拠点画面中の `Esc`: 通常の `PauseMenu` を開く
- 拠点画面中の右クリック、`I`、`R`、`Q` / `E`、`Tab`、`1`〜`8`: 採掘用入力としては処理しない
- 一時停止メニュー中の `W` / `S` または上下キー: 項目移動
- 一時停止メニュー中の `F` または `Enter`: 決定
- 一時停止メニュー中の左クリック: 決定
- 一時停止メニュー中の `Esc` または右クリック: 戻る
- レベルアップ画面中の `A` / `D`、左右キー、`Q` / `E`: カード選択
- レベルアップ画面中の `F` / `Enter`: 選択中カードを決定
- レベルアップ画面中の `1` / `2` / `3`: 強化カードを直接決定。ただし画面上の操作説明には表示しません。
- レベルアップ画面中の左クリック: 強化カードを決定
- レベルアップ結果表示中の `F` / `Enter`: 結果を閉じる
- レベルアップ画面中の `Esc`: 閉じない。選択必須
- アイテム画面の24枠をクリック: カーソル移動
- アイテム画面中の `Esc`: 戻る。つかみ中はキャンセル
- 拠点から開いたアイテム画面では、アイテムの「使用する」コマンドは半透明表示で選択不可
- リング画面中の `Z` / `X`: 解禁済みリングが複数ある場合のリング切替
- リング画面中の `1`〜`3`: リングプリセット1〜3を呼び出す。リング直接選択には使わない
- リング画面中の `Shift+1`〜`Shift+3`: 現在の全リング配置をプリセット1〜3へ登録する
- リング画面中の左クリック: リング上の装着アイテムを選択
- リング画面中の左ドラッグ: リング上の装着アイテムを円周上で移動
- リング画面中の `Q` / `E`: 装着アイテム選択
- リング画面中の `A` / `D` または左右キー: 選択中アイテムを5度単位で角度調整
- リング画面中の `F` / `Enter`: 詳細確認
- リング画面中の `R`: 選択中アイテムを外す
- リング画面中の `T`: 装着アイテムを等間隔に整列する
- リング画面中の `Shift+R`: 装着アイテムを可能な分だけ全部取る
- リング画面中の `G`: 装着アイテムをつかむ / 置く
- リング画面中の `Esc`: 戻る。つかみ中はキャンセル
- ゲームオーバー画面中の `W` / `S` または上下キー: 項目移動
- ゲームオーバー画面中の `F` / `Enter`: 決定
- ゲームオーバー画面中の左クリック: 決定
- ゲームオーバー画面中の `Esc`: 無効。死亡を取り消して `Playing` には戻れません。
- ステージクリア画面中の `W` / `S` または上下キー: 項目移動
- ステージクリア画面中の `F` / `Enter`: 決定
- ステージクリア画面中の左クリック: 決定
- `F1`: デバッグ表示の切り替え

## 画面モード

- 新規開始時の起動フローは `OpeningKamishibai` → `Title` → `IntroTutorialDungeon` → `Base` です。紙芝居は毎回タイトル前に再生します。`Title` の決定入力時に `intro_tutorial_completed` があれば `Base` へ進み、なければ導入ダンジョンへ進みます。
- `OpeningKamishibai`: 紙芝居形式オープニング。ゲーム進行を止め、TSV定義に従ってページを自動再生します。
- `Title`: 仮タイトル画面。正式タイトル画面へ差し替えやすい暫定画面として扱い、`intro_tutorial_completed` 未完了では導入ダンジョンへ、完了済みでは `Base` へ進みます。
- `IntroTutorialDungeon`: 新規開始時だけ入る導入専用ダンジョンです。通常ステージ選択やセーブ復元対象には含めません。
- `Base`: 歩ける拠点マップ。導入ダンジョン脱出後、またはセーブ読込後に表示され、プレイヤー移動、施設調べ、通常メニュー表示だけを解釈します。
- `WorldLoading`: 採掘開始前の準備画面。ゲーム進行を止め、ダンジョン新規生成、ノード配置、初期チャンク読み込みを数フレームに分けて進めます。
- `Playing`: 通常画面。移動、向き、リング投げ、ショートカット、アイテム使用を解釈します。
- `PauseMenu`: 一時停止メニュー。ゲーム進行を止め、`Playing` から開いた場合は `Playing`、`Base` から開いた場合は `Base` に戻ります。
- `Inventory`: アイテム画面。ゲーム進行を止め、アイテム選択、使用、リング追加を解釈します。ただし `Base` から開いた場合は、使用コマンドを無効化し、リング追加や保護変更などの管理操作だけを扱います。
- `Ring`: リング画面。ゲーム進行を止め、リング関連操作を解釈します。
- `ObjectImageScaleEdit`: デバッグ用の画像サイズ編集画面。`Objects` / `Others` タブを切り替え、選択中カードを上下キーまたはホイールで拡大縮小し、`Ctrl+S` で `data/object_image_scale.cfg` へ保存します。
- `EnemyHitboxEdit`: デバッグ用の当たり判定編集画面。`Enemies` / `Objects` タブを切り替え、選択した敵またはリング上アイテムの円判定をプレビュー上でドラッグ移動、ホイールでサイズ変更、`A` で円追加、`Delete` で削除、`Ctrl+C` / `Ctrl+V` で単体コピー/貼付、`Ctrl+Alt+V` で単体の左右反転貼付、`Ctrl+Shift+C` / `Ctrl+Shift+V` で敵の全方向コピー/貼付、`Ctrl+Shift+Alt+V` で全方向の左右反転貼付、`Ctrl+Z` / `Ctrl+Y` で編集のUndo/Redo、`Ctrl+S` で `data/hitboxes.cfg` へ保存します。`Enemies` では `共通` / `下` / `左` / `右` / `上` の方向ボタンを持ち、敵プレビューは選択中方向の歩行アニメーションで表示します。
- `AudioCueEdit`: デバッグ用の音声差し替え画面。`BGM編集` は `assets/audio/bgm/`、`効果音編集` は `assets/audio/se/` のWAVを一覧化し、選択したcue IDの `path` を差し替えて `audio_manifest.tsv` へ保存します。
- `LevelUp`: レベルアップ演出とレベルアップ選択。ゲーム進行を止め、演出中は入力を選択へ流さず、演出完了後に選択肢入力を解釈します。
- `GameOver`: プレイヤーHPが0以下になった後の画面。ゲーム進行を止め、死因、リザルト表示、リトライ操作を解釈します。`Esc` では `Playing` に戻れません。
- `StageClear`: ボス撃破後のクリア結果画面。ゲーム進行を止め、次ステージ解禁、クリア記録、拠点帰還操作を解釈します。ボス後会話はこの画面ではなく、ボス撃破共通フロー内で先に再生します。

## 紙芝居形式オープニング

- 紙芝居データは `data/opening_kamishibai.tsv` で定義します。1行目はヘッダーで、列は `id`、`image`、`text`、`duration`、`effect`、`note` です。
- `data/opening_kamishibai.tsv` は既存の `data` ホットリロード対象です。編集するとページ文言、画像、`duration`、`effect` の変更が反映されます。
- `text` 内の `|` は表示時に改行へ変換します。TSV内に実改行は入れません。
- 画像の命名規則は `assets/opening/op_1.png` 〜 `assets/opening/op_8.png` です。`op_8.png` は紙芝居最後のタイトル用画像として扱います。
- 画像ファイルが存在しない場合でもクラッシュせず、仮背景を表示し、警告ログを出して続行します。
- `duration` が不正、TSVパースが不正、ページが空の場合もクラッシュさせず、既定値またはフォールバックページで続行します。読み込み件数と警告はログへ出します。
- 初回再生済みかどうかは通常セーブとは別の軽量メタ保存で管理します。保存先はWindowsでは `%LOCALAPPDATA%/MajoShovel/meta.dat`、`LOCALAPPDATA` が取得できない場合は実行時カレントディレクトリの `meta.dat` です。
- 保存値は `openingEverWatched=true/false` です。紙芝居が最後まで再生された時点で `openingEverWatched=true` を保存します。
- `openingEverWatched=true` でも紙芝居自体は毎回タイトル前に再生します。違いは入力で即終了できるかどうかだけです。
- 初回再生時は、クリック、`Enter`、`F`、`Esc`、`Space` などすべての入力でスキップ、ページ送り、早送りを行いません。全ページを `duration` 通りに自動再生します。
- 2回目以降は、左クリック、`Enter`、`F` のいずれかで確認なしに紙芝居を即終了し、`Title` へ進みます。ページ送りではありません。
- テキストなしページも入力待ちせず、`duration` で自動進行します。
- 画像は画面全体を埋めるcover描画にします。画面全体を埋めるように拡大し、はみ出す部分は中央基準でトリミングします。
- ページ遷移は約0.8秒のクロスフェードです。前画像は alpha 1→0、次画像は alpha 0→1 で合成します。
- テキストがあるページでは、画像のクロスフェード完了後に本文をフェード表示します。本文背面の暗色帯は表示しません。
- 紙芝居本文のテキストサイズはページや画面幅で変えず、一定サイズで表示します。
- 本文は左右余白を十分に取り、中央揃えの白文字と黒い影で表示します。
- `effect=flash` はページ開始直後に白フラッシュを画面全体へ短時間重ねます。
- `effect=shake_dark` は守護星落下ページ用として、軽い画面揺れと後半の控えめな暗転を行います。
- `effect=title_fade` は紙芝居最後のタイトル用ページとして、画像またはロゴのフェードイン感を出します。正式な `Title` 本画面ではありません。
- 文字揺れ、Mesh歪み、紙芝居ページごとの専用BGM/SEは未実装です。共通の仮Title BGMは `AudioEngine` で再生します。

## 仮Title画面

- `Title` は正式タイトル画面までの仮画面です。
- `op_8.png` または紙芝居の `title_fade` ページ画像の余韻に合う簡易表示を出します。画像がない場合は仮背景で続行します。
- 左クリック、`Enter`、`F` で共通画面遷移の黒フェードを開始します。`intro_tutorial_completed` 未完了では導入ダンジョンへ進み、完了済みでは `Base` へ進みます。
- `Esc` では終了しません。
- 後続で本タイトル画面に差し替えやすいよう、`OpeningKamishibai` とは別の画面モードとして扱います。

## 共通画面遷移

- 画面遷移用に共通の黒フェードイン/フェードアウトとクロスフェードを扱います。
- `Title` から導入ダンジョンまたは `Base` へ進むときは、`Title` を黒へフェードアウトし、黒画面中に遷移先へ切り替え、遷移先をフェードイン表示します。未完了データで導入ダンジョンへ入る場合は、フェードイン完了後に1秒間の操作不能ウェイトを挟んでから落下直後イベントを開始します。
- 拠点UIから採掘開始して `Base` からダンジョンへ進むときは、`Base` を黒へフェードアウトし、黒画面中に `WorldLoading` の段階化生成を進め、`Playing` 開始後にダンジョン画面をフェードイン表示します。
- クロスフェードでは、遷移開始時点の画面をスクリーンショットとして保持し、切り替え後の画面の上に重ねて徐々に透明化します。
- 拠点の魔女の拠点とルネの家の切り替えは、黒フェードではなく共通クロスフェードを使用します。
- フェード中は画面固有の入力処理を行いません。二重決定、移動、メニュー操作などはフェード完了後から受け付けます。
- フェードとクロスフェードは画面描画の最後に共通オーバーレイとして重ねます。個別画面の描画処理には専用フェードを直書きしません。
- 共通画面遷移では `se.transition` を再生します。遷移先に応じたBGM切り替えは `AudioEngine` のID指定APIから呼び出します。

## プレイヤー歩行演出と足音

- プレイヤー歩行中は、歩行アニメーションの着地フレームで足元の砂埃と足音を同じイベントとして発火します。砂埃と足音を別タイマーで管理せず、見た目と音の同期を優先します。
- 着地フレームは歩行フレーム `3` と `6` を使います。歩行中に同じフレームへ留まっている間は連続発火せず、フレームが切り替わった時だけ発火します。
- 足音は床種別ごとにcueを切り替えます。魔女の拠点の屋外は `se.footstep.base_outdoor`、ルネの家の中は `se.footstep.home`、ダンジョン内は `se.footstep.dungeon` を使います。
- 左右の足で別素材は持たず、同一cueを再生します。左右差はピッチで表現し、片足は少し低め、もう片足は少し高めにします。さらに毎回わずかなランダムピッチ揺らぎを加えます。
- 足音のピッチ変更は `AudioEngine::playSe` の `pitchScale` で行います。足音用に別cueを量産せず、AudioEngine側の汎用SEピッチ再生を使います。
- 仮SE素材は `tools/generate_placeholder_audio.ps1` で生成します。足音用の仮素材は `assets/audio/se/footstep_base_outdoor_placeholder.wav`、`assets/audio/se/footstep_home_placeholder.wav`、`assets/audio/se/footstep_dungeon_placeholder.wav` です。

## リングアイテム衝突SE

- スペルリング上のアイテムが地形または敵に当たった時、`DiggingSystem` / `EnemySystem` は音を直接鳴らさず `RingImpactSoundEvent` を発行します。`Game::update` がイベントを集約し、`RingImpactSound` の解決処理で `se.impact.*` cueへ変換して再生します。
- 衝突SEはObjects DBとEnemies DBの特殊タグを参照します。タグは素材、質感、形状、構造の分類として使い、ダメージ、状態異常、採掘性能などのゲーム効果は発動しません。
- 地形側はコードで対象タグを補います。土は `terrain,dirt,soft`、岩は `terrain,rock,stone,hard,rugged`、鉱石は `terrain,ore,stone,metal,hard,rugged`、硬い岩は `terrain,rock,stone,hard,rugged,heavy` として扱います。
- 代表ルールは、土 + `scoop` で `se.impact.dirt.scoop`、土 + `blade` で `se.impact.dirt.blade`、土 + `pointed` で `se.impact.dirt.pointed`、岩/鉱石 + `metal` で `se.impact.rock.metal`、岩/鉱石 + `pointed` で `se.impact.rock.pointed` です。
- 敵や捕獲後アイテムとの接触では、`slime` 同士を `se.impact.slime.slime`、`soft` 同士を `se.impact.soft.soft`、硬いものと `soft` を `se.impact.hard.soft`、金属同士を `se.impact.metal.metal`、石/ゴツゴツ + 金属を `se.impact.stone.metal` に寄せます。
- アイテム固有の質感は衝突対象より優先できます。`crisp` はリンゴ系の `se.impact.crisp`、`book` は魔導書系の `se.impact.paper.book`、`trinket,metal` は鍵・銀貨系の `se.impact.metal.trinket`、`hollow,resonant` はベル/風鈴系の `se.impact.resonant.*`、`cloth/fluffy` は布団系、`bristle/mesh` はほうき・虫とり網系、`glass` はガラス系に使います。
- 同一フレームで複数アイテムが接触した場合は、同じcueを重複再生しないようにまとめ、優先度の高いものから地形3件、敵4件までに制限します。再生時は `volumeScale` と `pitchScale` をcueごとに調整し、さらに小さなランダムピッチを加えます。
- `audio_manifest.tsv` には `se.impact.*` を登録し、仮SE素材は `tools/generate_placeholder_audio.ps1` で `assets/audio/se/impact_*_placeholder.wav` として生成します。正式音源へ差し替える場合もcue IDは維持します。

## 初回導入ダンジョン

- 導入ダンジョンは `stage_00_intro_tutorial` としてコード内で扱う新規開始専用の短いダンジョンです。`StageCatalog` には通常ステージとして登録せず、拠点の行き先選択、ワープポイント、通常ダンジョン状態保存には混ぜません。
- タイル画像と地形プロファイルはステージ1相当の `soft_stardust` を使います。レイアウトは短い固定導線にし、開始地点、厚さ約5タイルのガレキ壁、短い暗所導入、明るい敵部屋、任意宝箱2個、出口手前の短い通路、出口を含めます。敵部屋後の独立した宝箱部屋は置きませんが、2個目の宝箱周辺だけは出口へ向かう途中の小部屋として扱います。
- 導入中だけ初期装備をリングから外した状態で開始します。落下直後の会話後、まず近くに落ちていたスコップだけを既存の採掘開始時リング回し始め演出でスペルリングへ乗せます。演出中は移動できません。松明は暗い通路で「ポケットに入っていた」ものとして追加でリングへ乗せます。どちらも拾う操作は要求しません。
- 導入ダンジョン中にリングへ乗せるチュートリアル用スコップは、導入中だけ耐久消費をロックします。通常ダンジョンのスコップ、松明、宝箱から得たアイテムの耐久消費は通常どおり扱います。
- 導入ダンジョン中だけ、プレイヤーHPはダメージで1未満になりません。敵接触、敵攻撃、弾、毒など `Player::applyDamage()` を通るHP減少はHP1で止め、導入ダンジョン終了後は通常どおりHP0でゲームオーバーになります。
- 落下直後の会話は `intro_tutorial:fall` のStoryEventで再生します。内容は「こ、ここは…？」「私、もしかして、守護星を…？」「とにかく、脱出しないと！」「でも、ガレキが壁になって、囲まれちゃった…」「あ、ちょうど近くにスコップがある。これをスペルリングで…」です。スコップ演出後に `intro_tutorial:shovel_ready` で「よし！これなら、なんとかなりそう」を表示します。
- 開始地点は右方向へ開いた小さな崩落ポケットにします。プレイヤー初期位置は開始地点から右へ2タイルずらします。左・上・下は浅い凹みを残しつつ崩落壁で塞ぎ、探索方向として目立たせません。開始ポケットはリングを回しながら動きやすいよう縦方向へ少し余裕を持たせます。開始地点と開始地点からX方向へ3タイル進んだ位置には、同じ大きさの導入用光源を置きます。その先に厚さ約5タイルのガレキ壁を置きます。右の掘削壁は中央ルートを土多めにし、外側ほど岩と硬い岩を増やします。壁材はseedと相対座標から決まる固定ノイズでばらつかせますが、正面中央の突破ルートは必ずスコップで進めるよう保証します。従来のように20タイル程度掘らないと進めない壁厚にはしません。
- 暗い通路に入ると `intro_tutorial:torch_found` で「うう…暗い場所だ…」「そうだ！たしか、松明がポケットに入ってたはず！」「これに火をつけて…」を表示し、その後松明をリングへ追加します。松明演出後に `intro_tutorial:torch_ready` で「よし！これなら暗い道も進めそう」を表示します。暗い通路は長く引っ張らず、松明追加後すぐ次の明るい敵部屋を目指せる程度に短くします。
- 導入中はリング中心ずらし、リング投げ、虫とりアミ、リング装備のチュートリアルを発火しません。画面下の操作ヘルプからも虫とりアミ、中心ずらし、リング投げは外します。虫とりアミのリング接触捕獲判定も導入中は発生させません。
- 導入中の通常操作ヘルプは画面最下部ではなくショートカットHUDの上部に表示し、文字サイズは通常と同じ2で描画します。通常時の文言は移動とEscメニューだけにし、F使用とIリュックは表示しません。出口付近の専用プロンプトでは `F/Enter 拠点へ帰還` を表示します。リング状態HUDは導入中だけ表示せず、クリック対象や図鑑ポップアップの回避矩形にも含めません。
- 通路の先の明るい敵部屋にスライムを1体固定で出します。スライム本体は部屋の少し手前から見えるように早めにスポーンしますが、導入中だけ自動視認では発見状態にせず、プレイヤーが敵部屋へ入った時点でランタイムID指定により強制的に発見状態へ切り替えてから遭遇イベントを開始します。スライムは敵部屋の中央付近から端へ離れすぎないよう、導入中だけ移動リーシュを設定します。遭遇時はまず「うわ！モンスターだ！」を表示し、その後スライムへカメラフォーカスしてから「グヘヘヘ！襲ってやるぜ～！」を表示します。スライム発言後はカメラをプレイヤーへ戻してから「こっちに来ないで～！」を表示します。カメラ移動中はメッセージを表示せず、文字送り入力でも進行しません。スライムの発言は本文に「モ：」を入れず、キャラ名欄を「スライム」にします。スライム発言では立ち絵を表示しません。
- 敵部屋外周は上下を壁で囲ってよいですが、メイン通路または枝通路が通る接続口は外周岩帯で塞がないようにします。導入スライム部屋では右方向の出口が必ず通れる状態になり、外周の硬い岩で囲まれ続ける状態にはしません。
- スライム遭遇イベント後、スライムを倒すまでの間は、導入スライム部屋周辺の地形ダメージを一時的に無効化します。これにより、周囲の壁を掘って戦闘を迂回する進行はできません。スライム撃破後は通常どおり掘削できます。
- スライムを倒すと `intro_tutorial:enemy_defeated` で「やった！倒せた！」「私、戦闘は苦手だから…モンスターがいたら、ぶつけて倒すことにしようっと」を表示します。その会話が閉じた直後、敵のいた場所に1個目の宝箱をジャンプドロップ演出で出してから `intro_tutorial:chest_found` で「あれ？なんだろう」を表示します。
- 導入ダンジョンの宝箱は2個固定です。1個目はスライム撃破位置に出現し、中身はObjectsアイテム `item_coin_bag` の金貨袋固定です。2個目は出口へ向かう道中の明るい小部屋へ右寄せで配置し、小部屋の入口側にいるキノコを倒した直後に、その場でジャンプする宝箱出現演出として出現します。中身はObjectsアイテム `item_iron_sword` の鉄の剣固定です。どちらも任意で、無視しても出口へ進めます。開けてアイテムを入手した場合は「やったあ！ルネはお宝を手に入れた！」「…って、そんなこと言ってる場合じゃないか」に続き、初回入手通知を閉じた後のリング装備状態で分岐し、リング装備済みなら「一応、これも回しながら行こうっと」、未装備なら「…これって、スペルリングに乗せたらどうなるんだろう？」を表示します。
- 出口手前で `intro_tutorial:midway` を再生し、「出口はもう少しかな…？」「不安だけど、がんばって進むしかない…！」を表示します。出口部屋は導入ダンジョン専用の小さめの出口ポケットとして整形し、通常ステージの広いゴール空洞にはしません。出口部屋の照明は小さめの部屋サイズに合わせ、敵部屋より控えめな半径にします。出口から約5タイル以内に入ると `intro_tutorial:exit_found` を再生し、「あっ！出口かな！？急ごう！！」を表示します。帰還操作はさらに出口へ近づいてから有効にし、出口発見台詞の発火距離とは分けます。出口の帰還プロンプトは下部操作説明だけに表示し、右側のダンジョンログには表示しません。その後、出口を調べると拠点へ帰還し、`intro_tutorial_completed` をストーリーフラグに追加します。導入ダンジョン中は通常ダンジョン入口・ワープポイント用の帰還確認UIを出さず、この出口専用の完了処理だけを使います。続けて `intro_tutorial:base_return` の拠点会話で、守護星を探しに星くずのダンジョンへ向かう任務を受けます。

## 拠点画面

- ゲーム起動時は必ず `OpeningKamishibai` から開始し、`Title` を経由します。SaveDataの有無ではなく `intro_tutorial_completed` を見て、未完了なら導入ダンジョンに入り、脱出後に `Base` に入ります。完了済みなら `Title` の決定入力後に `Base` を表示します。導入ダンジョン実装前の進行済み既存セーブはロード時に完了済み扱いへ移行します。
- 拠点はルネの家の庭と、採掘口まわりに作られた臨時採掘拠点として扱います。
- 拠点は採掘ランの出発点、帰還地点、持ち帰り所持品/素材の保管地点、セーブ可能地点として扱います。セーブ操作はルネの家の中にある `日記` から行います。
- 拠点画面は選択式メニューではなく、`Base` モード内の歩ける2D見下ろし拠点マップです。魔女の拠点とルネの家の中を持ち、どちらも `Base` モードとして扱います。背景と施設は画像ベースで描画し、移動範囲、衝突、調査距離、拠点編集は矩形ベースで扱います。カメラは固定1画面です。
- 魔女の拠点背景は `assets/kyoten/map.png` を使います。ルネの家の中の背景は `assets/kyoten/map_house.png` を使います。
- 魔女の拠点の施設画像は `assets/kyoten/` 配下の `move.png`、`box.png`、`wagon.png`、`sagyodai.png`、`kyokaro.png`、`ring-kobo.png`、`house.png` を使います。ルネの家の中の家具画像は `本棚=books.png`、`日記/机=desk.png`、`ベッド=bed.png` を使い、背景画像の上に重ねて描画します。
- `Base` モード中はプレイヤー移動、施設調べ、通常メニュー表示だけを行います。
- 魔女の拠点とルネの家の中では、スペルリングを回転表示せず、スペルリング当たり判定、スコップ掘削、松明光源、敵、ドロップ、虫とりアミ、リング投げ、リング中心ずらし、照準によるプレイヤー/リング向き反映を行いません。
- 魔女の拠点とルネの家の中では、ショートカット使用、リング装着、アイテム使用を採掘中ワールド処理へ流しません。敵、地形、掘削、ドロップ、Projectile、経験値、レベルアップなどの採掘中ゲーム進行も更新しません。
- 拠点内プレイヤー座標と屋外/屋内の現在エリアはメモリ上で維持してよいものとします。ただしセーブファイルには保存しません。セーブから再開した時は魔女の拠点のルネの家のドア下部から開始し、ダンジョンから帰還した時は魔女の拠点のダンジョン入口上部から開始します。
- `Base` から `Esc` で通常の `PauseMenu` を開けます。`Base` から開いた `PauseMenu`、`Inventory`、`Ring` を閉じると最終的に `Base` へ戻ります。`Playing` から開いた場合は従来どおり `Playing` へ戻ります。
- `Base` から開いた `Inventory` では「使用する」コマンドを半透明の無効状態で表示し、決定しても発動しません。採掘中に開いた `Inventory` では従来どおり使用できます。
- インタラクト対象は `facilityId`、`displayName`、`rect`、`interactionRange`、`enabled`、`unlocked`、`onInteract` を持つ構造で配置します。魔女の拠点と屋内で対象リストを切り替えます。自動遷移用の隠し矩形も同じ施設リストに含め、拠点編集で移動・リサイズできるようにします。
- 魔女の拠点の配置一覧は `ダンジョン入口`、`収納箱`、`商人ワゴン`、`作業台`、`拠点強化炉`、`リング工房`、`モニカNPC`、`ルネの家`、隠し矩形 `ルネの家の入口` です。ルネの家は入口だけでなく家全体を調査対象として扱います。
- ルネの家の中の配置一覧は `本棚`、`日記`、隠し矩形 `屋外へ戻る出口`、`ベッド` です。`日記` は机画像 `desk.png` 上の調査対象として扱います。
- 施設やNPCの近くにいると画面下に `Enter: 〇〇を調べる`、または `Enter: モニカと話す` のようなヒントを表示します。`Enter` は範囲内の対象のうち魔女の向き方向にある施設を優先し、向き方向に候補がない場合は距離が一番近い施設を優先します。
- 調査可能距離内にある施設は、画像のアルファ形状に沿った1px白アウトラインを表示します。カーソルが施設画像に乗っている場合は黄色アウトラインへ変化します。アウトライン表示中は施設名を画像上部に中央揃えで表示し、6pxの黒半透明アウトラインを付けます。
- マウスクリックでは、マウスカーソルが対象画像の表示矩形上にあり、かつプレイヤーがその対象の `interactionRange` 内にいる場合だけ反応します。遠距離クリックでは反応しません。
- 魔女の拠点では、プレイヤーの足元が `ルネの家の入口` の隠し矩形へ下から入った時、自動で屋内へ移動します。ルネの家の中では、プレイヤーの足元が `屋外へ戻る出口` の隠し矩形へ上から入った時、自動で魔女の拠点へ戻ります。これらの隠し矩形は通常表示・通常クリック・通常Enter対象には出さず、拠点編集中だけ編集対象として表示します。
- 拠点内エリア切替やダンジョン帰還時の黄色い完了ステータス（`ルネの家に入りました`、`魔女の拠点に戻りました`、`帰還しました`、`脱出しました`、および帰還時自動保存の追記）は、`--test-play` 付きのテストプレイ起動時だけ表示します。通常起動では遷移、帰還、保存処理は実行しますが、この帰還系ステータスは表示しません。
- `ダンジョン入口` を調べると、出発地点選択として `入口`、`ワープポイント`、条件達成後の `ダンジョンを再生成する` を表示します。
- ダンジョン入口の行き先選択では解放済み通常ステージを表示します。ローグライクダンジョン「不可思議の迷宮」はステージ2解放以降で選べます。
- `入口` を選ぶと、保持中の通常ステージ状態がある場合はその地形、敵、未取得ドロップ、木箱、宝箱、月のカケラ、ワープポイント発見状態を維持したまま入口から再入場します。保持中の状態がない場合は新規生成します。
- `ワープポイント` を選ぶと、発見済みワープポイント一覧を2列カードで表示し、任意の発見済み地点から再入場できます。解放済みワープポイントがない場合は選択できません。発見済みワープポイント数の表示はウィンドウ中央揃えにします。
- `ダンジョンを再生成する` は、そのステージに配置された全ワープポイントを発見し、かつそのステージを1回以上クリアした後に選択できます。選択時は未回収ドロップが消えることと、ボス再戦用に地形も作り直すことを警告表示し、確認後に地形、敵、未取得ドロップ、木箱、宝箱、月のカケラ、ワープポイント配置を作り直します。未達成時は `全ワープ解放とクリア後に可能` と表示します。
- ダンジョン中の開始地点には、拠点へ戻るための `ダンジョン入口` を描画します。`ダンジョン入口` または発見済みワープポイントに近づくと `F/Enter 拠点へ帰還` の操作表示を出し、入力すると共通の帰還確認UIを表示します。
- ダンジョン中は画面左上、上部情報バーの下に透明背景の円形ミニマップを表示します。ミニマップは画像ウィンドウや四角いパネルではなく、プレイヤー中心のローカル表示とし、探索済みタイル、現在光が届いているタイル、プレイヤー向き、発見済みワープポイント、探索済み範囲内のアクティブ敵を表示します。未探索の地形、未発見ワープポイント、隠し報酬、未露出の敵ノードは表示しません。
- ミニマップの探索済み情報は、プレイヤー光源およびリングアイテム、魔法、ワープポイント、入口などの実描画用光源と同じ判定を使って記録します。掘削や爆発で開いたタイルは即座に探索済みの空洞として記録します。更新は毎フレーム総当たりせず、一定間隔またはプレイヤーが別タイルへ移動した時に行います。
- 通常ステージの保持中ダンジョン状態には、ミニマップの探索済み情報も含めます。拠点帰還、発見済みワープポイントからの再入場、リトライスナップショット復元では探索済みミニマップを維持します。ステージ再生成時はミニマップも新規状態へ戻します。
- セーブデータには通常ステージの `dungeon_seed` / `dungeon_warp_point` と同じステージ状態として、探索済みミニマップセルを保存します。アプリ再起動後も探索済み情報を復元します。ローグライク扱いのステージや敵テスト用ダンジョンは通常ダンジョン状態保存の対象外とし、ミニマップ探索済み情報も混ぜません。
- 保持中の通常ステージ状態がない場合、または `ダンジョンを再生成する` を確定した場合は、即座に `Playing` へ入らず `WorldLoading` へ遷移します。`WorldLoading` 中は拠点画面に準備中表示を重ね、採掘中の入力とゲーム進行は処理しません。
- `WorldLoading` の表示は画面右下の小さな進捗ゲージのみとし、中央ウィンドウ背景や「現在どの初期化処理を実行中か」の詳細テキストは表示しません。
- `WorldLoading` は `Game::WorldBuildJob` として、状態初期化、UI初期化、ラン情報初期化、`DungeonLayout` 生成、ワープポイント配置、月のカケラ/報酬/宝箱/木箱/敵ノード配置、スペルリング初期化、開始地点周辺チャンク読み込み、最終反映を1フレームずつ進めます。
- `WorldLoading` 完了時に、拠点で保持していた所持品、リング装着、レベル、経験値を復元し、強化状態を再反映してから `Playing` へ遷移します。ワープポイント開始の場合は完了時に開始位置を選択したワープポイントへ移し、初期チャンクを読み込み、リトライ用スナップショットを作成します。
- 発見済みワープポイントはダンジョン入口から選択する構造にします。ワープポイント専用施設は作りません。
- 採掘開始時、拠点で保持している所持品とリング装着はランへ引き継ぎます。
- 採掘開始時、HPは全回復し、毒、鈍足などの一時状態は解除します。
- 採掘開始時はフェードイン完了後にスペルリングが中心から広がる演出を再生し、リング上アイテムは時間差でフェードイン表示します。スペルリングの通常中心はプレイヤー魔女の足元ではなく、自発光と同じ体中央付近のアンカーを使います。再入場時は前回の投げ/戻り状態や前回リング座標を持ち越さず、開始位置で通常リング状態へ戻してから演出を始めます。アイテム由来の照明は新規入場・再入場どちらでもフェード中や演出前には表示せず、アイテムのフェードイン位置に合わせて半径0からイージングで広げます。展開演出中もリング色は通常表示と同じ色相を維持し、黄色系など別色へ変化させません。初回ダンジョン会話が未再生の場合は、この演出が完了してから会話を開始します。
- `収納箱` は持ち帰った通常アイテムを長期保管する倉庫です。調べると倉庫画面を開きます。
- `商人ワゴン` では、宝や不要アイテムの売却と、基本商品の購入ができます。
- 所持金は拠点での売却収入と強化支払いに使う恒久リソースです。
- `拠点強化炉` では、お金と素材を使って拠点機能、商人機能、倉庫容量、施設解禁、プレイ性能強化を進めます。
- `作業台` では、修理とアイテム強化を行います。
- `本棚` は屋内専用です。ルネの家の中で調べるとアイテム図鑑、宝図鑑、敵図鑑を開きます。
- `日記` は屋内専用です。ルネの家の中で調べるとセーブ画面を開き、保存済みの記録概要を確認してから拠点セーブを上書きできます。初期選択は `保存する` で、決定1回で上書き保存します。記録概要はサブウィンドウ内に表示し、その下に確認中は `保存しますか？`、保存後は `保存しました。` を表示します。保存後は画面を閉じず、表示内容を現在の進行状況へ更新し、もう一度決定すると閉じます。
- `リング工房` は初期状態では未解禁で、拠点強化炉または将来のストーリー進行フラグで解禁します。未解禁時は拠点上に表示せず、解禁後はリング工房UIを開きます。
- `モニカNPC` は魔女の拠点に配置します。専用施設は置かず、近づいて話すと現時点では仮メッセージのみ表示します。
- 施設UIを閉じた場合は必ず拠点マップの `Base` に戻ります。ダンジョン入口で採掘開始を選んだ場合だけ、保持中ダンジョン再入場なら `Playing`、新規生成または再生成なら `WorldLoading` 経由で `Playing` へ移行します。
- 採掘開始時、強化状態をプレイヤー最大HPとスペルリング半径/速度へ反映します。
- 売却/強化/加工後はルネの家の中にある `日記` で所持金、所持品、倉庫、拠点強化状態を保存できます。
- `セーブ` は引き続き拠点でのみ実行できます。日記はルネの家の中にあります。採掘中、一時停止メニュー、アイテム画面、リング画面、レベルアップ画面、ゲームオーバー画面、ステージクリア画面ではセーブ操作を提供しません。
- 日記のセーブ画面には、前回保存データの軽い進行概要として `進行`、`ルネ`、`アイテム図鑑`、`モンスター図鑑`、`プレイ時間` を表示します。`進行` は現在選択中のダンジョンではなく、最新解放済みの通常ストーリーダンジョン名と発見済みワープポイント数を `ワープアイコン 1/4` の形で表示します。通常ストーリー3ステージをクリア済みの場合は `進行 ストーリークリア` と表示します。`ルネ` はプレイヤーLv、図鑑は現在のカタログ総数に対する記録済み割合、プレイ時間は `○時間○分` で表示します。
- セーブ先はWindowsでは `%LOCALAPPDATA%/MajoShovel/save.dat` を標準とします。`LOCALAPPDATA` が取得できない場合は実行時カレントディレクトリの `save.dat` を使います。

## 会話・メッセージUI

- 拠点NPC会話、ダンジョン中の通信、ステージ開始時・クリア時の短い会話は、共通の会話オーバーレイで表示します。
- 会話中はゲーム進行と通常入力を止め、`F`、`Enter`、左クリック、`Esc`、右クリックを文字送り入力として扱います。表示途中なら現在行を全文表示し、全文表示後なら次の行へ進めます。ただしダンジョン中のスペルリングは演出表示として回転と表示座標だけを更新し、攻撃判定、掘削、捕獲回復、投げ/戻り移動などのゲーム進行は発生させません。ワールドドロップと後出し宝箱のジャンプ出現だけは表示演出として会話中も進行させ、接触拾得、宝箱開封、吸引、敵・Projectile・地形などの進行は止めます。
- ダンジョン中の会話表示中、ストーリーイベント開始待ちのウェイト中、ストーリーイベントがキュー済みでまだ会話開始していないフレーム、ダンジョンフォーカスによるカメラ移動・ホールド中は、上部バーと会話メッセージ以外のUIを非表示にします。対象はミニマップ、HP/ステータス、ショートカットHUD、リング状態HUD、ダンジョンログ、下部操作ヘルプ、ワープ帰還UI、図鑑ポップアップなどです。ウェイトと会話の切り替わりでUIが1フレームだけ再表示されないよう、`pendingStoryTrigger_` と `pendingStoryTriggers_` が残っている間も同じ非表示状態を維持します。
- 上記のUI非表示中でも、敵消滅、アイテムドロップのジャンプ、後出し宝箱のジャンプ、壁破壊、ダメージポップアップ、魔法FX、地面ラインなどの表示演出は停止させません。プレイヤー移動、敵AI、Projectile、掘削、接触拾得、宝箱開封などのゲーム進行は止めたままにします。
- `はじめて入手した！`、レベルアップ選択、ワープ/入口の脱出確認など、モーダルUIが操作を占有する場面では下部のダンジョン操作ヘルプを表示しません。
- 会話中でも、画面内の主人公は待機状態のスプライトアニメーションだけを継続します。移動、向き変更、足音、当たり判定、HP変化などのゲーム進行は発生させません。
- 文字送り入力を押し続けた場合は、最初の入力後も約0.28秒間隔の低速な連続文字送りとして扱います。連続送りでも、表示途中の行はまず全文表示し、その後のリピートで次の行へ進みます。
- 本文は画面下部のメッセージウィンドウ内に表示します。通常会話のメッセージウィンドウは `assets/UI_messageWindow.png` を専用テクスチャとして使い、1280×720では画像の自然サイズ970×232pxで表示します。狭い画面では縦横比を維持して縮小します。本文は専用画像の本文基準位置から8px下げて表示し、操作説明は他UIと同じフッター位置から40px左へ寄せて表示します。
- 共通ウィンドウの開きアニメーション中は、完了10フレーム前まで本文、発言者名、立ち絵の表示開始を待機します。その後に発言者名と立ち絵をフェードインし、本文の1文字送りを開始します。
- 会話中は背景を暗くする画面全体の暗幕を重ねません。
- 本文は1文字ずつフェードイン表示します。文字位置は通常の文章描画と同じ字間になるよう、行全体のテキスト計測を基準に配置します。
- 発言者名は `assets/UI_messageWindow.png` 左上の名前プレート内に中央揃えで表示し、プレート中央から24px右へ寄せます。名前文字は白単色で表示し、黒アウトラインは付けません。
- 会話中は主人公と相手キャラクターの立ち絵を表示します。主人公は左側、モニカなどの相手キャラクターは右側に配置します。通常会話の立ち絵はメッセージウィンドウより先に描画してウィンドウの後ろ側へ重ね、画像下端をゲーム画面下端へ合わせます。表示高さは画面高の約96%を基準にし、元画像の縦横比を維持します。
- 正式立ち絵は `assets/taties/tatie_N.png` を使います。対応は `1=ルネ`、`2=チコリ`、`3=モニカ`、`4=村長`、`5=商人`、`6=加工職人` です。会話データ側で個別の `portraitPath` が指定された場合は、その行の指定を優先します。
- 立ち絵には枠とキャラ名を付けません。正式素材がない場合は仮立ち絵を表示します。立ち絵は表示時にフェードインし、会話終了時にフェードアウトして消します。フェードイン時は左側が左から右へ、右側が右から左へ24px分イージング移動し、フェードアウト時は逆方向へ戻します。
- 会話イベントは `data/story_events/*.story` から読み込みます。メタ行は `@event`、`@title`、`@trigger`、`@once`、`@repeat` を使います。`@title` はデバッグ一覧などで使う表示名で、省略時は `@event` IDを表示名として扱います。

## ダンジョンイベント共通カメラフォーカス

- ダンジョン内のギミック、特殊部屋、救助イベントなどの発見演出は、`Game` の共通フォーカス状態で扱います。状態は `Idle`、`MoveToTarget`、`Hold`、`PlayDiscoveryDialogue`、`ReturnToPlayer` を基本とします。
- フォーカス要求は `eventKind`、`focusWorldPos`、`discoveryStoryEventId`、`holdSecondsIfNoDialogue`、完了コールバックを受け取ります。すでにフォーカス中の場合、後発要求はキューせず無視し、ログに出します。
- フォーカス開始時に現在のカメラ位置を保存し、対象ワールド座標へ約0.7秒でイージング移動します。初回発見会話がある場合は対象位置で既存の共通会話オーバーレイを開始し、会話完了後にプレイヤー位置へ約0.7秒で戻します。会話がない場合は対象位置で約2秒ホールドしてから戻します。
- フォーカス中は既存会話中の停止処理と同じ考え方で `gameProgressPaused()` の停止対象に含めます。プレイヤー移動、敵更新、Projectile、掘削、リング当たり判定、ドロップ取得、HP減少などの採掘中ゲーム進行は止め、描画は通常通り行います。スペルリング、ワールドドロップのジャンプ出現、後出し宝箱のジャンプ出現は会話中と同じ演出表示更新だけを許可します。
- `focusWorldPos` が不正な場合、会話IDが空または未定義の場合、StoryEventが見つからない場合でもクラッシュさせません。会話なし扱いでホールドしてから通常カメラ追従へ戻します。
- `PauseMenu`、`GameOver`、`StageClear`、画面フェード、ワールド生成、ワープ帰還確認などの既存画面状態とは重ねて開始しません。デバッグ用に `game dungeon-focus test` コマンドでプレイヤー右側をフォーカスでき、F1デバッグ表示に現在のフォーカス状態を表示します。

## ダンジョンイベント発見処理

- ダンジョン内のギミック、特殊部屋、救助イベントは `DungeonEventKind` と `DungeonEventInstance` で表します。現在の種類は `sleeping_enemy_treasure`、`monster_swarm_room`、`nest_room`、`boss_monster_room`、`glowing_rock_room`、`electric_circuit_room`、`coin_room`、`warp_guide_map`、`buried_witch`、`lost_baggage_witch`、`item_request_witch`、`surrounded_witch`、`cold_witch_campfire`、`heavy_rock_witch` です。`safe_cavern` はイベント種類から削除します。
- `DungeonEventInstance` はID、種類、中心タイル、フォーカスタイル、発見半径、発見済み、完了済み、自発光半径、空洞半径、任意データを持ちます。現段階では `DungeonLayout` の特殊部屋、ワープ案内、ゴール、mainPath上の仮配置から生成し、後続の正式イベント配置へ差し替えられる構造にします。
- 未発見状態でも代表ギミック、NPC、部屋中心などは自発光します。自発光は既存の `LightSource` 配列へ追加し、プレイヤー光源、リング/魔法光源、入口、ワープポイントと同じ円形視界・ミニマップ探索判定へ統合します。半径は種類ごとに3〜5タイル程度を初期値とし、後で調整できるようにします。
- プレイヤーが `discoveryRadiusTiles` 内へ入ると、そのインスタンスを発見済みにして共通カメラフォーカスを要求します。ただし `coin_room` は見つけてもカメラフォーカスを要求せず、発見済み化、発見SE、ログだけを行います。同一インスタンスは、会話ありなしに関わらず一度発見したら再フォーカスしません。フォーカス中、会話中、リング開始演出、画面遷移、ワールド生成中は新規発見処理を開始しません。
- 初回発見会話はイベント種類ごとに1回だけ再生します。StoryEvent IDは `dungeon_discovery:<kind>`、既読フラグは `dungeon_discovery_seen_<kind>` とし、既存の `@once` / `storyFlags_` 保存を使います。デバッグ会話テストは従来通り既読フラグを立てません。
- 同じ種類の初回会話が未再生なら、対象へカメラ移動後に既存の共通会話オーバーレイで仮会話を再生します。既読済み、会話ID未定義、StoryEvent未読込の場合は会話なし扱いで約2秒ホールドしてからプレイヤーへ戻します。
- 発見済み/完了済みのインスタンス状態は、通常ステージの保持中ダンジョン状態、ワープポイントリトライスナップショット、セーブデータの `dungeon_event_instance` 行へ保存します。種類別の初回会話済み状態は既存StoryEventフラグとしてセーブをまたいで保持します。
- F1デバッグ表示には現在の共通カメラフォーカス状態と、プレイヤーに最も近い `DungeonEventInstance` のkind、ID、発見済み、完了済み、距離、総数を表示します。読み込み済みStoryEvent一覧には `data/story_events/dungeon_discovery_*.story` の仮発見会話が通常イベントと同じ扱いで出ます。

## ダンジョンイベント管理・生成・報酬

- ラン中のダンジョンイベントは `Game::DungeonEventSystem` が保持します。役割は `DungeonEventInstance` 一覧の生成、ランタイム保持、発見候補検索、自発光登録、完了状態の更新、デバッグ参照、保存/復元用配列の受け渡しです。個別ギミックの詳細挙動は今後 `DungeonEventKind` ごとの更新処理へ追加します。
- イベント種類の固定メタ情報は `DungeonEventDefinition` に集約します。定義テーブルは `kind`、ID文字列、表示名、カテゴリ、デバッグ配置可否、初回発見会話の有無、ステージ候補可否、未発見ワープポイント要求、高危険フラグ、発見半径、自発光半径、共通空洞プロファイルを持ちます。`GameDungeon.cpp` はこの定義を参照して生成・デバッグ・ログ・発見判定を行い、イベント追加時の一覧更新漏れを防ぎます。
- `DungeonEventInstance` はID、種類、中心タイル、フォーカスタイル、発見済み、完了済み、報酬生成済み、自発光半径、発見半径、生成済み参照ID、イベント固有パラメータを持ちます。現段階の参照IDはワールドドロップなど将来の追跡用で、独自のドロップ形式は作りません。
- ステージ1〜3の固定イベントは `DungeonEventDefinition` の固定配置表で管理し、各ステージを3区分に分けて配置します。`coin_room` は固定表に含めず、`DungeonLayout::specialRoomAnchors` の `CoinRoom` からランダムに生成します。`warp_guide_map` も固定表に含めず、未発見ワープポイントがある場合だけ低確率でmainPath上へ生成します。
- 固定配置表を持たないステージでは、従来通り `DungeonLayout::specialRoomAnchors` を参照して特殊部屋由来イベントを作ります。`TreasureRoom` は `sleeping_enemy_treasure`、`EnemyRoom` は `monster_swarm_room` / `nest_room`、`OreRoom` は `glowing_rock_room` / `electric_circuit_room`、`CoinRoom` は `coin_room` へ変換します。`SafeCavern` はイベントへ変換しません。`coin_room` は発見フォーカスを行わず、空洞保持と金貨ノード配置のみを担当し、専用会話は追加しません。
- ワープポイントがある通常ステージで、未発見ワープポイントが残っている場合のみ `warp_guide_map` を候補にします。配置点はmainPath上の案内図/石碑であり、未発見ワープポイントの正確なタイルには置きません。不可思議の迷宮のようなローグライク扱いでワープポイントがないステージではワープ案内図は出しませんが、特殊部屋由来イベントとmainPath上の仮イベントは出してよい扱いです。
- 固定配置表を持たないステージでは、`boss_monster_room` をmainPath後半の代表点へ補助配置できます。既存のボス出現地点、ワープポイントからのボスフロー、StageClear/GameOver境界は変更しません。イベント地形は共通空洞化で確保し、通路を塞ぐ当たり判定は追加しません。
- 固定配置表を持たないステージでspecialRoomAnchorsだけでは初期マーカーが不足する場合は、mainPathの中盤以降へ `sleeping_enemy_treasure`、`monster_swarm_room`、`nest_room`、`glowing_rock_room`、`electric_circuit_room`、魔女救助・依頼系6種、条件を満たす場合は `warp_guide_map` を補助配置します。スタート直後とゴール直前は避け、既存の小型テンプレート、ワープポイント配置、宝箱/木箱配置を破壊しない非ブロッキングマーカーとして扱います。
- 通常描画ではイベント中心だけを示す目印マーカーは出しません。ワープ案内図は `assets/others/img_16.png` の台座と `img_17.png` の浮遊地図を重ね、地図だけを上下に少し揺らして描画します。巣穴、発光岩、受電石、荷物、未点火の焚き火、重い岩は `assets/others/img_18.png`〜`img_23.png` のイベント用画像を優先し、画像がない場合だけ仮図形へフォールバックします。魔女NPCは当面は仮図形表示です。埋まり魔女のガレキは代表オブジェクト画像を持たず、ステージ別地形タイルシートの岩壁として表示します。F1デバッグ時のみ中心、発見半径、自発光半径をワールド上へ表示します。
- 完了判定は共通管理に置きます。`glowing_rock_room` は発光岩の全破壊、`electric_circuit_room` は受電石の全通電、`warp_guide_map` は案内表示の使用終了または対象ワープポイント発見で完了します。戦闘系イベントはそれぞれ敵全滅、巣穴破壊、親分撃破を完了条件にします。魔女救助・依頼系は埋まり除去、落とし物返却、条件アイテム提出、周囲敵撃破、焚き火点火、硬い岩破壊を完了条件にします。
- 報酬生成は `DungeonEventRewardRequest` 経由で行います。`ChestDrop` / `MultiChestDrop` は実際の `ChestNode` を共通の宝箱出現処理で生成し、素材ドロップ、お金ドロップは既存の `WorldDropSystem`、`safeLootLandingPosition`、ジャンプ演出へ接続します。イベント専用ドロップ形式は増やしません。未取得ドロップは既存の `dungeon_world_drop` 保存対象として扱います。
- 通常ステージのメモリ保持、ワープポイント到達時リトライスナップショット、セーブデータには、イベントの発見済み、完了済み、報酬生成済み、自発光/発見半径、生成済み参照ID、イベント固有パラメータを含めます。ローグライク扱いステージは既存方針通り通常ステージ状態保存対象外ですが、同一ラン中のイベント状態はランタイムで保持します。

## ダンジョンイベント共通空洞化

- `DungeonEventInstance` ごとに共通の空洞化処理を適用します。空洞化は `applyPlacementTerrainOverrides()` の一部として行い、特殊部屋由来イベント、mainPath補助イベント、デバッグ配置イベントが同じ処理を使います。
- 空洞形状は四角形ではなく、ダンジョンシード、イベントID、中心タイルから作る決定的ランダムなひとかたまりの形にします。中心、フォーカスタイル、報酬タイル、敵/ギミック/NPCの代表配置タイルは必ず空け、必要な点までは中心から線状に連結して孤立タイルを作りません。
- 空洞サイズはイベント種別のプロファイルで決めます。戦闘系は広め、掘削・属性ギミック系は中程度、魔女系とワープ案内図は小さめにします。`CoinRoom` は既存特殊部屋半径を参照しつつ中小規模にします。
- `buried_witch` は例外的に完全な大部屋へせず、小空洞だけを作ります。魔女周囲のガレキはイベントオブジェクトではなく、そのダンジョンの通常岩壁 `TileType::Rock` として配置します。見た目、HP、硬さ、掘削、破壊SE、地形編集保存は同じステージの岩壁と完全に同じです。
- 空洞化はラン生成時に決定的に再適用できるため、通常の地形上書きとして扱います。破壊済み発光岩、通電済み受電石などのイベントオブジェクト変化は `DungeonEventInstance` の固有パラメータで保持し、埋まり魔女のガレキ破壊は既存の地形編集保存で保持します。

## ダンジョンイベント 戦闘系

- `sleeping_enemy_treasure` は「眠る敵の宝物庫」です。発見後、部屋内に眠り状態の敵を複数体配置し、中央または奥に露出宝箱を置きます。眠り敵は宝箱開封で一斉に起きるほか、プレイヤー接触またはリング攻撃が当たった時点でも通常敵として起きます。宝箱開封後、イベント敵が全滅すると完了です。眠り中の敵は移動・攻撃せず、仮の `Z` 表示で区別します。
- `monster_swarm_room` は「大量発生の部屋」です。敵を出すたびに通常湧きと同じステージ/深度の敵出現重みで抽選し、複数種類が混ざってよい扱いにします。初期上限は8体程度とし、既存の敵ソフトキャップとスポーン位置探索を使って過密配置を避けます。イベント敵が全滅すると部屋奥に宝箱を1個出し、イベント完了にします。
- `nest_room` は「巣穴つぶし」です。発見後、部屋内に巣穴を1〜3個配置します。巣穴は自発光し、専用の巣穴画像で描画します。巣穴は一定間隔で敵を出し、敵種はスポーンごとに通常湧きと同じステージ/深度の敵出現重みで抽選します。巣穴ごとの同時出現上限は3体、イベント全体の同時出現上限は9体程度とし、敵が無限に溢れないようにします。巣穴はスペルリング接触でHPを削れ、HP0で破壊されます。破壊済み巣穴は追加スポーンせず、破壊時に周囲へ複数宝箱を出します。全巣穴破壊かつイベント敵全滅で完了です。
- `boss_monster_room` は「親分モンスター部屋」です。発見後、通常敵数体と親分モンスター1体を配置します。親分は専用敵IDではなく、通常湧きと同じ抽選で選んだ既存敵定義に倍率をかけたイベント強化個体です。初期値はHP2.5倍、接触ダメージ1.5倍、サイズ1.5倍、経験値3倍です。親分は通常ボスフローの `BossDeath` ではなく、イベント用の強化通常敵として扱い、撃破時に部屋奥へ宝箱を1個出してイベント完了にします。見た目はサイズ差と簡易オーラで区別します。
- 戦闘系イベントの敵は `EnemySystem::spawnEventEnemy` で生成します。敵IDは原則指定せず、通常湧きと完全に同じ `EnemySystem` 側のステージ/深度重み抽選を毎回使います。イベントIDとランタイム敵IDを保持し、死亡イベントから親分撃破や全滅判定へ戻します。眠り、親分、イベント所属は `Enemy` のランタイム状態として保持します。
- 戦闘系イベント状態は `DungeonEventInstance` の `encounterSpawned`、`activated`、`rewardSpawned`、`bossDefeated`、`bossEnemyRuntimeId`、`nestHoles`、生成済み参照IDとして保持します。メモリ上の通常ステージ保持とワープポイントリトライスナップショットでは、敵システム本体とこれらの状態を一緒に復元するため、破壊済み巣穴や撃破済み親分が同一ラン内で復活しません。
- 通常ステージSaveDataには、イベント発見済み、完了済み、報酬生成済み、報酬タイル、起動済み、親分撃破済み、巣穴HP/破壊済み/報酬生成済みを `dungeon_event_instance` のイベント固有パラメータへ含めます。アクティブ敵の個体状態そのものは既存方針通り通常SaveDataでは完全保存しないため、再起動後の未完了戦闘はイベント状態から再開または再生成する扱いです。旧SaveDataの選択敵IDは読み込み互換として無視してよい扱いです。

## ダンジョンイベント 掘削・属性ギミック系

- `glowing_rock_room` は「光る岩を全部壊す部屋」です。発見後、部屋内に発光岩を3〜5個配置します。発光岩は通常壁ではなく `DungeonEventInstance` 配下のイベントオブジェクトとして扱い、スペルリング接触時の攻撃力または掘削力でHPを削ります。すべて破壊すると部屋奥に宝箱を1個出し、イベント完了にします。破壊済みの岩は同一ラン、通常ステージ保持、リトライスナップショット、SaveData復元で復活しません。
- `electric_circuit_room` は「電気回路の部屋」です。発見後、受電石を2〜4個配置します。配線パズルにはせず、雷属性の接触で受電石を1個ずつ通電させます。すべて通電すると部屋奥に宝箱を1個出し、イベント完了にします。通電済み受電石は未通電と異なる発光色で描画します。
- 受電判定は、リングアイテムの `damageType == thunder`、雷魔法使用後の `magicAuraDamageType == thunder`、または対象アイテム/追加効果の `EffectSpec` に `cast_thunder` が含まれる場合に成立します。特殊タグの `thunder` 単体では効果を直接発動しません。将来、雷属性Projectileや捕獲敵行動をイベントオブジェクトへ通知する場合も、この雷接触判定へ入力を増やす方針です。
- `warp_guide_map` は「ワープ案内図 / 星図石碑」です。通常ステージで未発見ワープポイントが残っている場合だけ出現候補にします。発見後に使用済みとなり、プレイヤーから最も近い未発見ワープポイントへの方角を左上ミニマップ外周の矢印で約45秒表示します。未発見ワープポイントの正確な位置やミニマップ上の点は表示しません。対象ワープポイントを発見した場合、または表示時間が切れた場合にイベント完了にします。
- ワープ案内図は宝箱を出しません。報酬は探索補助そのものです。初期実装では一度使用した案内図は再使用不可です。
- 掘削・属性ギミック系イベントの代表マーカー、発光岩、受電石、ワープ案内図は自発光し、暗闇に完全には隠れません。発見時は共通カメラフォーカスと種類別初回会話を使い、フォーカス中は既存会話中と同じくゲーム進行を停止します。
- 通常ステージSaveDataには、イベント発見済み、完了済み、報酬生成済み、報酬タイル、起動済み、発光岩HP/破壊済み、受電石通電済み、ワープ案内図の対象インデックスと残り表示秒数を `dungeon_event_instance` のイベント固有パラメータへ含めます。

## ダンジョンイベント 魔女救助・依頼系

- 魔女救助・依頼系イベントは `buried_witch`、`lost_baggage_witch`、`item_request_witch`、`surrounded_witch`、`cold_witch_campfire`、`heavy_rock_witch` です。すべて `DungeonEventSystem` の `DungeonEventInstance` として生成・保持し、NPCや関係ギミックは自発光して暗闇に完全には隠れません。発見時は共通カメラフォーカスと種類別初回会話を使い、フォーカス中は既存会話中と同じくゲーム進行を停止します。
- 救助・依頼イベントは短時間・局所完結にします。長距離護衛、チコリ誘導、探知前提、魔女NPCが敵やリングからダメージを受ける仕様は初期実装では採用しません。魔女NPCは非ブロッキングなイベント表示で、進路を塞ぎません。
- 発見済みの魔女NPCは近距離で `F/Enter` により話しかけられます。初回会話では要求や困りごとを提示し、解決条件を満たしていない状態で再度話しかけると同じ条件を短く再確認できます。解決条件を満たした後に話しかけると、共通会話オーバーレイの終了時コールバックで報酬を出してイベント完了にします。完了後は再度話しかけてもお礼の一言だけを返します。
- `buried_witch` は「埋まった魔女」です。魔女の上下左右に、そのダンジョンの通常岩壁と同じ `TileType::Rock` を置きます。専用HPを持つイベントオブジェクトではなく、プレイヤーが通常の壁と同じ掘削処理で取り除きます。周囲の岩壁をすべて空洞化すると解決済みになり、魔女に話しかけると控えめなお礼報酬を出して完了します。
- `lost_baggage_witch` は「荷物を落とした魔女」です。魔女の近くに自己発光する落とし物を1つ置きます。プレイヤーが落とし物に近づくと回収状態かつ解決済みになり、その後魔女へ話しかけると完了します。落とし物は遠距離から正確に丸見えになりすぎないよう、光源半径は小さめにします。
- `item_request_witch` は「特定カテゴリや効果のアイテムを要求してくる魔女」です。個別アイテム名ではなく、回復カテゴリまたは `heal` 効果、斬撃/`blade`/武器カテゴリ、探索/掘削系カテゴリや効果などの大枠条件からランごとに1条件を選びます。初回会話で要求条件を提示し、条件を満たす所持品がある状態で再度話しかけると1個消費して報酬会話へ進みます。条件を満たす所持品がない場合は条件を再確認し、イベントは未完了のまま残します。保護中の個体アイテムと杖装備中アイテムは提出しません。
- `surrounded_witch` は「敵に囲まれた魔女」です。魔女の周囲にイベント所属の敵を2〜3体配置し、敵種はスポーンごとに通常湧きと同じステージ/深度の敵出現重みで抽選します。それらを倒すと解決済みになります。敵数は増やしすぎず、護衛イベントにはしません。魔女は攻撃対象にならず、イベント敵の死亡状態は通常の敵イベント通知から反映します。
- `cold_witch_campfire` は「寒がっている魔女」です。魔女の近くに消えた焚き火を置きます。`damageType == fire`、火魔法後の火属性オーラ、または `EffectSpec` の `cast_fire` / `light` 系効果を持つリング接触で点火できます。`fire` タグ単体だけでは直接発火させません。点火後の焚き火は自発光が強くなり、解決済みになります。
- `heavy_rock_witch` は「重いものをどかしてほしい魔女」です。魔女の近くに通常壁とは別の硬い1マス障害物を置きます。掘削力、攻撃力、打撃、重い武器でHPを削れ、破壊すると解決済みになります。初期実装では非ブロッキングな高HPイベントオブジェクトとして扱います。
- 報酬は既存の宝箱、素材、お金、ワールドドロップ処理を流用し、魔女NPCの報酬会話が閉じたタイミングで付与します。報酬量は控えめにし、イベント専用インベントリ形式は作りません。
- 通常ステージSaveDataには、要求確認済み、解決済み、完了済み、報酬請求済み、報酬生成済み、落とし物回収済み、提出済みアイテムID、要求条件、焚き火点火済み、硬い岩HP/破壊状態、イベント敵の起動済み状態を `dungeon_event_instance` のイベント固有パラメータへ含めます。埋まり魔女のガレキは通常地形なので、破壊状態は地形編集として保存します。同一ランのメモリ保持とリトライスナップショットでは、破壊済みオブジェクト、掘削済み地形、撃破済み敵が復活しないようにします。

## ダンジョンイベント統合調整

- 採用済みイベントは、戦闘系の `sleeping_enemy_treasure`、`monster_swarm_room`、`nest_room`、`boss_monster_room`、掘削・属性ギミック系の `glowing_rock_room`、`electric_circuit_room`、ランダム特殊部屋の `coin_room`、探索補助の `warp_guide_map`、魔女救助・依頼系の `buried_witch`、`lost_baggage_witch`、`item_request_witch`、`surrounded_witch`、`cold_witch_campfire`、`heavy_rock_witch` です。`safe_cavern` は削除済みで、新規生成しません。
- ステージ1〜3は固定配置表でイベントを置きます。固定イベントは区分内で決定的に配置し、同種重複制限、魔女数制限、戦闘数制限では落としません。`coin_room` と `warp_guide_map` だけは固定表に含めず、ランダム生成として扱います。
- 固定配置は次の通りです。ダンジョン1-1は「光る岩」「埋まった魔女」、ダンジョン1-2は「荷物を落とした魔女」「大量発生の部屋」、ダンジョン1-3は「アイテム要求魔女」「巣穴つぶし」です。ダンジョン2-1は「重いもの魔女」「寒がっている魔女」、ダンジョン2-2は「電気回路」「アイテム要求魔女」、ダンジョン2-3は「敵に囲まれた魔女」「親分モンスター部屋」です。ダンジョン3-1は「眠る敵の宝物庫」「大量発生の部屋」、ダンジョン3-2は「巣穴つぶし」、ダンジョン3-3は「親分モンスター部屋」です。ダンジョン3には魔女イベントを置きません。
- 固定配置表を持たないステージでは、従来の補助イベント生成を使います。補助イベント生成時は同一種類を重複させません。魔女系は最大2件、戦闘系は最大2件までに制限し、魔女救助系だけに偏らないようにします。特殊部屋由来イベントは既存部屋の管理を優先するため同種重複を許容します。高危険の補助イベント `monster_swarm_room`、`nest_room`、`boss_monster_room`、`surrounded_witch` はmainPath進行度28%未満には置きません。
- `warp_guide_map` はワープポイントが有効で未発見ワープポイントが残っている通常ステージだけに低確率で出します。不可思議の迷宮、ワープポイント無効ステージ、未発見ワープポイントがない状態では生成しません。
- 発見フォーカスは同一インスタンスにつき1回だけです。種類別の初回発見会話も既存StoryEventフラグにより1回だけ再生します。フォーカス演出後は短いクールダウンを置き、発見演出が連続で割り込まないようにします。プレイヤー被弾直後は危険な割り込みを避けるため、短時間だけ発見開始を遅延します。
- 自発光はイベント代表オブジェクト、魔女NPC、巣穴、発光岩、受電石、焚き火、ワープ案内図、特殊部屋中心へ既存 `LightSource` として統合します。基本半径は3〜5タイル程度に抑え、画面外から過剰に目立たせません。プレイヤー魔女と魔女NPC自身の自発光は共通補正を使い、足元ではなく体の中央付近を中心にして、基準自発光半径の2倍で扱います。魔女NPC自身の自発光は完了済みでも魔女が表示されている間は維持します。ミニマップのワープ発見や視界判定は既存の光源統合処理に従い、イベント専用の別系統表示は増やしません。
- 報酬はテンポを壊さない量に抑えます。魔女イベントは控えめな素材またはお金、光る岩と電気回路は宝箱1個、ワープ案内図は宝箱なしの探索補助です。巣穴破壊報酬は強めなので初期調整では巣穴1個につきCommon宝箱1個に抑えます。親分モンスターは経験値3倍とRare宝箱1個に留め、通常敵の上位版として扱います。
- 通常ステージの保持、ワープポイントスナップショット、死亡リトライでは `DungeonEventInstance` 一覧、敵システム、宝箱、ワールドドロップを一緒に復元します。完了済みイベント、破壊済みイベントオブジェクト、使用済みワープ案内図、未取得報酬が不自然に復活しないようにします。不可思議の迷宮は既存方針通り通常ステージ保存対象外ですが、同一ラン中の状態はランタイムで保持します。
- F1デバッグ表示では総イベント数、発見済み数、完了数、戦闘系数、魔女系数、発見クールダウン、最寄りイベントのkindと状態を確認できます。生成時はステージID、総数、種類別生成数をログに出します。生成候補がない場合や配置に失敗した場合はクラッシュさせず警告ログに留めます。
- デバッグコンソールのイベント配置プルダウンは `DungeonEventDefinition` の `debugPlaceable` な項目から生成します。ID、表示名、デバッグUIを別々に手書きせず、イベント一覧の単一入口を維持します。
- ミミック系宝箱は通常の宝箱として配置され、開封操作またはリング接触で開けようとした瞬間に低確率で `surprise_box` / `mimic` / `pandora_box` へ変身して襲ってきます。変身した宝箱は通常の中身を出さず、撃破時の報酬はミミック系モンスター側の `drop_item` 挙動に任せます。チコリ誘導・探知前提の荷物探しは採用せず、落とし物は同じ部屋または近くの小範囲で完結させます。長距離護衛はテンポを崩すため採用しません。

## 収納箱と倉庫

- 収納箱を開くと、最初に `しまう`、`取り出す`、`一括操作` を選びます。
- 採掘リュックは採掘中に持てる通常アイテムの枠です。通常アイテムは24枠を基準とし、個体アイテムは1個で1枠、スタックアイテムは1種類で1枠として扱います。
- 拠点素材4種などの素材は通常アイテム枠を消費しません。素材は倉庫へ預けず、`MaterialInventory` として表示のみ行います。
- 倉庫は個体アイテムとスタックアイテムの両方を扱います。スタックアイテムを預ける場合は1個ずつ移動し、同じ種類が倉庫にあれば既存スタックへ加算します。
- 倉庫容量は拠点強化炉で拡張します。Lv1は48枠、Lv2は72枠、Lv3は100枠、Lv4は140枠、Lv5は200枠です。
- 通常の `しまう` 画面では、リング装備中アイテムはリュック一覧に存在しないため倉庫へ直接預けられません。ただし `一括操作` の `全部しまう` はリング上のアイテムも倉庫へ移動します。
- 杖装備中の `ItemInstance` は倉庫へ預けられません。先に杖装備を外す必要があります。
- `一括操作` の `全部しまう` は、リュック内の通常アイテムとリング上のアイテムを可能な限り倉庫へ移動します。倉庫がいっぱいの場合は移動できる分だけ移動し、残った数をメッセージで表示します。装備中の杖は残します。
- `一括操作` の `プリセット1を準備`、`プリセット2を準備`、`プリセット3を準備` は、該当リングプリセットの実行に足りないアイテムだけを倉庫からリュックへ取り出します。取り出し後にプリセット自体は適用しません。
- プリセット準備は、収納箱にないアイテムやリュック満杯で取り出せないアイテムがあっても失敗扱いにはせず、取り出せる分だけ取り出して理由をメッセージで表示します。
- 未登録のプリセット準備ボタンは半透明表示にし、実行できません。
- 収納箱画面中の `1`〜`3` はプリセット1〜3の準備ショートカットとして扱い、収納箱内のタブやページの直接選択には使いません。
- `しまう` のリュック一覧と `取り出す` の倉庫一覧では、`T` またはゲームパッド `X` / West で対象一覧をカタログ順に並び替えます。

## 商人ワゴン

- 拠点の `商人ワゴン` を調べると商人UIを開きます。
- 初期機能は `宝を売る`、`不要アイテムを売る`、`基本商品を買う` です。UI上は売却対象一覧と購入商品一覧を左右に表示します。
- 保護ONの `ItemInstance`、杖装備中の `ItemInstance`、素材、リング装備中アイテム、ストーリーアイテムは売却不可です。壊れた個体アイテムは保護OFFなら売却できます。
- 売却価格はObjects DBの `価格` を基礎価格として使います。強化値、軽量化/大型化、破損状態、商人側の買取倍率、本日の高価買取倍率を反映します。
- 商人機能強化はLv1通常売買、Lv2品揃え5枠、Lv3品揃え6枠/買取基本価格+10%、Lv4本日の高価買取解禁、Lv5品揃え8枠/レア商品増加、Lv6品揃え9枠/買取基本価格+20%、Lv7品揃え10枠/高レア商品増加です。
- 本日の高価買取はカテゴリではなく、売却可能な `宝` から最大4種類を直接選びます。対象アイテムの売却画面では売値行の上に赤い斜め文字で `高価買取中!` を表示します。
- 珍品販売、珍品レア度上昇、買い戻しは採用しません。商人強化では通常品揃えの枠数、レア度上限、レア商品の出やすさを段階的に増やします。
- 帰還時に商人更新条件を満たしている場合のみ、次に商人ワゴンを開いた時に品揃えを更新し、更新フラグを消費します。

## 作業台

- 拠点の `作業台` を調べると、`修理`、`攻撃力強化`、`掘削力強化`、`耐久力強化`、`強化リセット`、`軽量化`、`大型化` を選べます。
- 修理対象は、`currentDurability` が `maxDurability` 未満、または `isBroken=true` の `ItemInstance` です。保護ONで壊れた個体アイテムも修理できます。
- 修理後は `currentDurability=maxDurability`、`isBroken=false` に戻します。
- 通常修理費は `基礎価格 x 消耗割合 x 0.4` です。壊れた状態からの修理費は `基礎価格 x 0.6` です。小数は切り上げて整数化します。
- 攻撃力強化は `attackBonus`、掘削力強化は `digBonus`、耐久力強化は `durabilityBonus` と `maxDurability` を増やします。強化時は `enhanceLevel` を1増やし、最大+5までです。
- 強化コストはお金と強化鉱石を使います。
- スタック可能アイテムを強化する場合、`count` を1減らして `ItemInstance` を生成し、その個体へ強化を適用します。個体化後はスタックに戻しません。
- 強化リセットは強化値と攻撃/掘削/耐久補正を0に戻します。軽量化は重量補正を下げ、大型化はサイズ補正と重量補正を上げます。
- 軽量化は作業台機能Lv1、大型化はLv3で解禁します。重量化、小型化、追加効果付与は採用しません。

## 拠点強化炉

- 拠点の `拠点強化炉` を調べると拠点強化UIを開きます。
- 拠点強化炉では、お金と素材を使って、倉庫容量強化、商人機能強化、作業台機能解禁、リング工房解禁、プレイ性能強化枠を進めます。
- 拠点強化UIの強化項目一覧は、共通の縦並びタブUIとして描画します。左側に強化名、右側に `Lv.x/y` または `上限` を表示します。
- 主な拠点拡張素材は `古木の建材` です。プレイ性能強化枠では将来用素材として `魔力のしずく` を使います。必要に応じて他素材も要求できる構造にします。
- 倉庫容量強化はLv1 48枠、Lv2 72枠、Lv3 100枠、Lv4 140枠、Lv5 200枠です。
- 商人機能強化はLv1通常売買、Lv2品揃え5枠、Lv3品揃え6枠/買取基本価格+10%、Lv4本日の高価買取、Lv5品揃え8枠/レア商品増加、Lv6品揃え9枠/買取基本価格+20%、Lv7品揃え10枠/高レア商品増加です。
- 作業台機能解禁はLv1軽量化、Lv2作業台費用-10%、Lv3大型化、Lv4作業台費用-20%、Lv5作業台費用-30%です。強化リセットは作業台の基本機能として扱います。
- リング工房は初期状態では未解禁です。拠点強化炉で解禁すると拠点にリング工房が表示され、利用可能になります。2本目/3本目リング解放はここでは行いません。
- プレイ性能強化枠では、既存の最大HP、リング半径、リング速度に加えて、`収集術式` を実装します。収集術式は未強化でも弱く発動し、`魔力のしずく` とお金で強化すると採掘中に近くのワールドドロップをより広い範囲からプレイヤーへ吸い寄せます。初期吸引半径は56px、Lv1以降は+32pxずつ伸び、Lv5で216pxです。吸引は発動範囲の外周でも弱くなりすぎないようにし、近づいたドロップが素早く手元へ寄る速度感にします。
- 拠点強化状態はSaveDataへ保存します。未実装機能はクラッシュさせず、未解禁/未実装表示にします。

## リング工房

- リング工房は通常のリング装備メニューとは別施設です。リング装備アイテムの付け替え、リング装着順の変更、2本目/3本目リング解放は行いません。
- 初期状態では未解禁です。現時点では拠点強化炉の `リング工房解禁` で利用可能になり、将来はストーリー進行フラグでも解禁できる構造にします。
- レベルアップで得たリング強化ポイントの配分を再調整できます。総強化量は増やさず、リング1〜3それぞれの半径ポイント、速度ポイント、重量上限ポイントの配分だけを変更します。
- 再調整UIは `リング1〜3 × 半径/速度/重量` のセルを表示します。移動元セルを選び、次に移動先セルを選ぶと1ポイント移動します。
- 再調整の確定にはお金と `月のカケラ` を消費します。未確定の配分案は確定前なら戻れます。
- レベルアップとは別枠のリング専用恒久強化として、`初期リング半径強化`、`初期リング速度強化`、`ずらし距離強化` を実装します。これらは `月のカケラ` とお金を消費し、SaveDataへ保存します。
- 将来用枠として、`リング投げ距離強化`、`リング投げクールダウン短縮`、`リング重量ペナルティ軽減`、`リング装着枠増加` を未解禁表示で保持します。
- UIには現在値、強化後の値、必要なお金、必要な月のカケラ、確定ボタン、未解禁項目を表示します。
- リング工房強化は採掘開始時とロード時に、初期リング半径、初期リング速度、右クリック長押しのリングずらし距離へ反映します。

## 本棚と図鑑

- 本棚はルネの家の中に置く拠点専用施設です。通常のリング装備画面とは混ぜず、調べると `アイテム図鑑`、`宝図鑑`、`敵図鑑` の3種類を選べます。
- アイテム図鑑は宝カテゴリ以外のObjects DB由来アイテムを対象にします。武器、盾、掘削道具、探索道具、魔導書、薬、弱体アイテム、捕獲敵アイテムなどを含みます。
- 宝図鑑はObjects DB上のカテゴリが `宝` のアイテムだけを対象にします。
- 敵図鑑はEnemyCatalogで読み込んだ敵を対象にします。
- 図鑑状態は `未発見`、`発見済み`、`入手済み`、`リング装備済み`、`効果発動済み`、`完全確認済み` の段階で管理します。
- アイテムと宝は入手時に `入手済み`、リング装備時に `リング装備済み` まで更新します。ただしリングに乗せただけでは効果詳細は掲載しません。
- 効果詳細は、実際に効果が発動した時点で掲載します。敵ダメージ、地形掘削、照明、状態異常、防御/消弾、探知、能力変化などをEffectDispatcher、敵命中、地形掘削、Projectile防御などの実処理地点から記録します。
- 敵図鑑は敵への接触/命中など採掘中の実イベント、撃破、捕獲で `発見済み` へ更新します。この段階では外見と名前だけを表示し、説明文とステータスは `？？？` にします。
- 虫眼鏡の `inspect_enemy` で観察した敵だけを `完全確認済み` へ更新します。`完全確認済み` の通常敵は採掘画面上でHPゲージを常時表示し、敵図鑑にも説明文、HP、攻撃力、移動速度、報酬、捕獲難度、捕獲時効果を表示します。ボスHPゲージは従来どおりボス表示として扱います。
- 最初のダンジョン `stage_01_stardust` では、虫眼鏡未入手時に深度100mまでの範囲へ `item_magnifying_glass` の固定報酬ノードを配置します。同じく虫とりアミ未入手時は `item_capture_net` の固定報酬ノードを深度100mまでの範囲へ配置します。プレイヤーが拾わずに通り過ぎた場合は強制入手しません。入手時に虫眼鏡は `tutorial:magnifying_glass`、虫とりアミは `tutorial:capture_net` を発火します。
- 未実装効果コードやDBから削除されたIDはクラッシュさせず無視または欠損名で扱います。特殊タグだけを理由に効果発見にはしません。
- 新しい効果が図鑑へ追加された場合、採掘画面上に効果発見ポップアップを表示します。表示位置は効果発動地点の近くに置き、画面外へ出る場合や既存HUDと重なる場合は画面内の近傍候補へ補正します。ポップアップは出現時と消滅時にフェードし、本文はフェードイン完了後から一文字ずつ表示し、全文表示後2.5秒保持します。
- 複数の効果発見が同時に起きた場合、同じアイテムIDの未発見効果は1つのポップアップへまとめ、異なるアイテムIDはキューに積んで順番に表示します。同じアイテムIDと効果キーの組み合わせは一度だけ表示し、図鑑更新ログにも残せる構造にします。
- 図鑑状態と発見済み効果はSaveDataへ保存します。

## 一時停止メニュー

- 通常画面で `Esc` を押すと `PauseMenu` に入ります。
- `PauseMenu` 中はゲーム進行を停止します。
- テストプレイ起動時のみ、`F5` でゲームを再起動します。通常起動時の `F5` は無効です。
- メニュー項目は `ステータス`、`アイテム`、`リング`、`オプション`、`ゲーム終了` です。
- `W` / `S` または上下キーで項目を移動します。
- `F` または `Enter` で選択中の項目を決定します。
- 左クリックで項目またはボタンを決定します。
- `Esc` または右クリックで前の画面へ戻ります。メイン一覧で戻ると通常画面へ戻ります。
- `ステータス` は現時点で取得できるHP、レベル、経験値、リング番号、リング装着数、半径、速度を表示します。
- `アイテム` はアイテム画面を開きます。
- `リング` はリング画面を開きます。
- `オプション` は `操作`、`音量`、`画面` のタブを持つ設定画面を開きます。`Z` / `X` またはゲームパッド `LB` / `RB` で設定タブを切り替えます。`画面` タブでは表示モード、ウィンドウサイズ、VSync、軽量化、明るさ、画面揺れ、入力アイコンを変更できます。
- `オプション` 画面には現在選択中の項目を説明するヘルプウィンドウを表示します。`操作` タブでは選択中の操作と割当列、`音量` タブでは選択中スライダー、`画面` タブでは選択中の画面項目を説明します。`VSync` の項目名はそのまま表示し、ヘルプ内でモニターの更新タイミングに描画を合わせる設定であること、画面の裂けを抑える一方で入力遅延やFPSに影響する場合があることを説明します。
- `ゲーム終了` は確認ダイアログを表示します。確認で `終了する` を選んだ場合だけゲーム終了を要求し、選択直後には即終了しません。

## ショートカットHUD

- ショートカットは8列×3行です。
- 通常画面では画面下部に現在の1行だけを表示します。
- `Tab` で `Row 1` → `Row 2` → `Row 3` → `Row 1` の順に切り替えます。
- `Q` / `E` とマウスホイールで、表示中の行のカーソルを左右へ移動します。
- `1`〜`8` で、表示中の行の該当枠を直接選択します。
- 選択中の枠は強調枠とカーソルで表示します。
- HUDには `Row n/3`、アクティブリング番号、8枠のアイテム名、選択カーソル、操作ヒント `Q/E 選択, Tab 行切替, F 使用, R リングへ` を表示します。
- 未接続または空のショートカット枠で `F` / `R` を押した場合は、ゲーム進行を壊さず安全に何もしません。

## アイテム画面

- `I` または一時停止メニューの `アイテム` から開きます。
- アイテム画面中はゲーム進行を停止します。
- 8列×3行、合計24枠を一括表示します。
- 右側には選択中アイテムの詳細を常時表示します。
- `WASD` / 矢印キーでカーソルを上下左右へ移動します。
- `Q` / `E` でカーソルを左右へ移動します。
- `F` / `Enter` で選択中アイテムを使用します。
- `R` で選択中アイテムをアクティブリングに乗せます。
- `T` でリュック内アイテムをカタログ順に並び替えます。ゲームパッドでは `X` / West で同じ操作を行います。
- `G` で選択中枠のアイテムをつかむ / 置く操作を行います。
- つかみ中に移動先で `G` または `F` を押すと置きます。移動先が埋まっている場合は元の空いた枠と入れ替えます。
- つかみ中に `Esc` を押すとキャンセルし、元の枠へ戻します。
- つかみ中は画面下部に「つかみ中」表示を出します。
- `Esc` で戻ります。一時停止メニューから開いた場合は一時停止メニューへ戻り、通常画面から開いた場合は通常画面へ戻ります。
- 詳細表示には、名前、カテゴリ、説明文、通常効果、軌道効果、攻撃力、ダメージ種別、掘削力、耐久力、重さ、特殊タグ、効果テキストを表示します。
- Objects DB由来アイテムは拾得後に空き枠へ表示され、`R` でアクティブリングへ装着できます。装着時はDB上の攻撃力、ダメージ種別、掘削力、耐久力、重さをリングアイテムへ写します。
- Objects DB由来アイテムの使用は `通常効果` の `EffectSpec` を `EffectDispatcher` へ渡します。ただしカテゴリ `杖` の `通常効果` は杖装備効果用なので、通常使用処理へは渡しません。現在実処理へ接続済みの通常効果は最小セットのみです。
- ダンジョン中にアイテム使用が成功した場合は、右側ダンジョンログへ「ルネは［アイコン］アイテム名を使った！」を表示します。使用効果でHPが回復した場合は、プレイヤー位置に緑の回復パルスも表示します。

### 杖装備スロット

- 杖装備は `InventorySystem::equippedStaffInstanceId` で `ItemInstance` のIDだけを参照します。杖本体の耐久、保護、強化値、加工補正は既存の `ItemInstance` 側を正とし、Objects DB定義をSaveDataへコピーしません。
- 装備できるのはカテゴリ `杖` のリュック内アイテムだけです。スタックとして存在する杖を装備する場合は1個を `ItemInstance` 化し、その `instanceId` を装備参照にします。
- 壊れた杖は装備できません。装備中の杖を選ぶと装備を外します。
- 装備中の杖はリング搭載、売却、破棄、倉庫移動ができません。移動や売却したい場合は先に装備を外します。
- リング搭載中の杖はリュック内の `ItemInstance` ではないため装備できません。装備したい場合は先にリングから外してリュックへ戻します。
- インベントリ、リング配置、倉庫、商人などの共通アイテムスロットでは、装備中の杖の右上に緑色の `E` を重ねて表示します。保護表示は従来どおり左上です。

### 杖装備効果

- カテゴリ `杖` の `通常効果` 欄だけを杖装備効果として扱い、装備中の1本から `EquipmentModifiers` へ集約します。カテゴリ `杖` 以外の通常効果は従来通り通常使用効果です。
- `target=equip_all` はリング1〜3へ、`equip_ring1` / `equip_ring2` / `equip_ring3` は対象リングだけへ反映します。杖をリングへ搭載した場合は `通常効果` を使わず、従来通り `軌道効果` だけを使います。
- アイテム詳細では、カテゴリ `杖` の通常効果欄を `使用時の効果` として表示しません。代わりに `装備時効果` として表示し、軌道効果欄は他アイテムと同じ `リングに乗せたときの効果` として分けて表示します。
- 杖の `効果テキスト` 欄は、既存と同じく全角 `｜` または改行で分割した説明行として扱います。装備時効果とリングに乗せたときの効果の両方がある場合は、装備時効果行、リング効果行の順に対応します。リング効果行は図鑑の発見状態を通した表示だけを使い、未発見の効果を `効果テキスト` から直接表示しません。
- リング別補正は `ringSpeedMul`, `ringRadiusMul`, `ringWeightLimitAdd`, `ringShiftDistanceMul`, `ringThrowDistanceMul`, `ringThrowSpeedMul`, `ringThrowCooldownMul`, `ringReturnSpeedMul`, `ringOutputMul`, `ringAnchorMul`, `ringDamageSpeedMul`, `lightRadiusMul`, `detectRangeMul`, `guardAreaMul`, `reflectPowerMul`, `reflectChanceAdd`, `metalWeightPenaltyMul`, `digPowerMul` を持ちます。
- 全体補正は `durabilityCostMul`, `sellPriceMul`, `moneyVisibleLevel`, `dangerHintLevel` を持ちます。現段階では集約と装備変更ログ表示まで行い、耐久消費、売却額、所持金可視化、危険ヒントの処理にはまだ接続しません。
- `*_mul` は乗算、`*_add` は加算、`*_level` は最大値を採用します。値0の倍率は既存の軌道補正と同じく未指定相当の1.0として扱います。
- 杖装備補正は、game_balance.cfg等の基準値、レベルアップ、恒久強化、リング工房の既存強化の後に適用し、その後にリング搭載アイテムの軌道効果、重量ペナルティ、一時状態補正を適用します。
- `ring_speed_mul`, `ring_radius_mul`, `ring_weight_limit_add`, `ring_shift_distance_mul`, `ring_throw_distance_mul`, `ring_throw_speed_mul`, `ring_throw_cooldown_mul`, `ring_return_speed_mul`, `ring_output_mul`, `ring_damage_speed_mul`, `dig_power_mul`, `metal_weight_penalty_mul` はリング性能へ反映します。
- `ring_weight_limit_add` は重量上限へ加算し、重量ペナルティ計算時の追加許容量としても扱います。`metal_weight_penalty_mul` は重量超過による速度ペナルティ係数へ乗算します。
- `ring_output_mul` はリングアイテムの接触ダメージと接触掘削ダメージの最終出力補正に使います。`ring_damage_speed_mul` は既存の `damage_speed` とは別枠で速度依存ダメージボーナスへ乗算します。
- `light_radius_mul`, `detect_range_mul`, `guard_area_mul`, `reflect_power_mul`, `reflect_chance_add`, `ring_anchor_mul` は既存処理がある範囲にだけ反映します。特殊タグは分類、検索、抽選制御、演出SEルーティングに使い、タグ単体では杖装備効果を発動しません。
- 起動時または杖装備変更時のログに、装備中の杖名、対象 `instanceId`、主要なリング別 `EquipmentModifiers` を出します。
- リング画面には装備中の杖名とリング1〜3別の杖補正サマリを表示します。`equip_ring1` / `equip_ring2` / `equip_ring3` の対象外リングは `対象外` と表示し、未実装の装備効果コードはクラッシュさせず説明行と警告ログに留めます。

## リング画面

- 一時停止メニューの `リング` から開きます。
- リング画面中はゲーム進行を停止します。
- 最大3リング前提ですが、未解禁リングのボタンは表示しません。解禁済みリングが1本だけの場合、フッターにはリング選択操作を表示しません。
- リングボタンは `リング 1` のように表示します。
- 解禁済みリングが複数ある場合は `Z` / `X` でアクティブリングを切り替えます。
- `1`〜`3` はリング直接選択ではなく、リングプリセット1〜3の呼び出しに使います。`Shift+1`〜`Shift+3` は現在の全リング配置をプリセットへ登録します。
- リング上の装着アイテムは円形リング上に表示します。固定の横並びスロット一覧は使いません。
- 1リングあたり最大24個まで装着できます。ただし、そのリングの重量上限を超える場合は装着できません。
- リング重量はリングごとに管理します。初期の最大重量はリング1〜3すべて10.0kgです。レベルアップの重量上限強化を選ぶたびに、選択中リングだけ重量上限が+1.0kgされます。
- 装着アイテムは5度単位の角度を持ちます。等間隔固定ではなく、左右キーまたは `A` / `D` で選択中アイテムの角度を5度ずつ調整できます。
- アイテム同士は重ねて配置できません。重なる角度への移動は拒否します。
- 装着アイテムはドラッグ＆ドロップでも移動できます。ドラッグ中はマウス位置からリング中心への角度を5度単位に丸め、リング円周上で仮表示します。
- ドロップ位置が配置可能ならその角度に確定します。配置不可の場合は、ドロップ角度から±30度以内で最も近い配置可能な5度刻み位置を探し、見つかればその位置へ短いイージングアニメーションで補正配置します。±30度以内に配置可能位置がない場合は、移動前の角度へ同じくイージングアニメーションで戻します。
- 新しくリングへ入れたアイテムは、既存アイテムが占有していない円周上の最も広い空き部分の中央へ自動配置します。1個だけ装着済みの場合、次のアイテムは反対側の180度ずれた位置に配置されます。
- `Q` / `E` で装着アイテムを選択します。
- `F` / `Enter` で選択中アイテムの詳細を確認します。
- `R` で選択中アイテムを外します。最後の1個は安全のため外せません。
- `T` またはゲームパッド `X` / West で、装着アイテムの順序を維持したままリング上へ等間隔に整列します。
- `Shift+R` またはゲームパッド `LT` + 十字キー下で、装着アイテムを可能な分だけ全部取ってリュックへ戻します。リュックに空きが足りない場合は重いものから空き枠分だけ戻します。
- `G` で装着アイテムをつかむ / 置く操作を行います。
- つかみ中に `G` または `F` を押すと、最も広い空き部分へ再配置します。
- つかみ中に `Esc` を押すとキャンセルし、配置可能な空き部分へ戻します。
- `Esc` で一時停止メニューへ戻ります。
- 表示内容は、円形リング配置、装着数、アクティブリングの重量/重量上限、半径、速度、重量補正、選択中アイテム詳細です。
- リング画面の速度は、アクティブリングの装着重量補正と `orbit_speed` などの軌道効果補正を含んだ実効速度です。重量、重量補正、攻撃力、ダメージ種別、掘削力、耐久力は実戦で使う値と同じ値を表示します。
- 杖を装備している場合は、装備中の杖名とリングごとの杖補正サマリを表示します。対象リングには速度、半径、重量上限などの主要補正を表示し、対象外リングは `対象外` と明示します。
- 選択中アイテムは、矩形スロット枠ではなくアイテムの見た目に沿った枠で強調します。
- セーブデータには各リングアイテムの角度を保存します。旧データや不正な角度は読み込み時に配置可能な角度へ再配置します。
- Objects DB由来アイテムの軌道効果は `軌道効果` の `EffectSpec` を `EffectDispatcher` へ渡します。現在実処理へ接続済みの軌道効果は最小セットのみです。

## ゲーム仕様

### プレイヤー

- 2D見下ろし画面上を移動します。
- 地形タイルの壁は通り抜けられません。
- 壁に当たった場合は、軸ごとに移動を解決して壁沿いに滑ります。
- マウス方向を向きます。
- プレイヤーグラフィックは `assets/majo.png` を使います。画像は3x3のスプライトシートで、indexは左上から右方向へ `0` から `8` まで割り当てます。
- 待機時は `0, 1, 2, 1, ...` をループし、各コマは12f表示します。歩行時は `3, 4, 5, 6, 7, 8, ...` をループし、各コマは8f表示します。
- スプライトは左向きが素材そのままです。右向きの場合だけ左右反転して描画します。
- 周囲にスコップと松明を装備したスペルリングを持ちます。
- 敵を倒すと経験値を得ます。
- 経験値が一定値に達するとレベルアップします。
- HP減少は原則として `Player::applyDamage(amount, DamageSource source)` を入口にし、最後にHPを減らした `DamageSource` を記録します。
- HPが0以下になると `GameOver` へ移行し、ゲーム進行を停止します。毒ダメージでもHP0になれば死亡します。

### 死亡とリトライ

- ゲームオーバー画面では、`GAME OVER`、死因、到達時間、レベル、撃破数、掘削数、入手アイテム数、`リトライ`、`帰還` を表示します。
- 死因は最後にHPを減らした `DamageSource` から表示します。現時点の文言は `毒で死亡`、`スライムの攻撃で死亡`、`スライムの接触で死亡`、`罠で死亡`、`不明なダメージで死亡` です。
- `リトライ` を選ぶと、通常ステージでは最後に到達したワープポイントのメモリスナップショットがあればそこから再開します。スナップショットがない場合は、現在の通常ステージ状態を保持したままステージ開始地点から再開します。
- `リトライ` 復元後、HPは全回復し、毒、鈍足などの一時状態は解除します。
- ワープポイントスナップショットは、プレイヤー位置、HP、レベル、経験値、所持品、リング装着、掘削済み地形、敵状態、未取得ワールドドロップ、木箱破壊状態、宝箱開封状態、月のカケラ取得状態、ワープポイント発見状態、ボス出現状態を復元対象にします。
- 死亡時リトライでは、敵状態と未取得ワールドドロップを全消去しません。破棄するのはエフェクト、一時弾、MagicSystemの一時魔法、短時間演出、一時状態異常です。
- `帰還` を選ぶと拠点画面へ戻ります。現在の所持品、経験値、解放済みワープポイントを保持し、HPは全回復し、毒、鈍足などの一時状態は解除します。通常ステージの掘削済み地形、敵状態、未取得ワールドドロップ、木箱破壊状態、宝箱開封状態、月のカケラ取得状態、ワープポイント発見状態はメモリ上に保持します。
- ダンジョン中に開始地点の `ダンジョン入口` または発見済みワープポイントから帰還する場合も、通常の `帰還` と同じ拠点帰還境界を使います。確認UIは `帰還する` と `戻る` を表示し、帰還確定時は通常ステージ状態を保持したまま拠点側のダンジョン入口へ戻します。
- 通常ステージでは、死亡しても所持品はすべて持ち帰れます。インベントリ内アイテムとリング装着中アイテムを保持対象にします。
- 4つ目のローグライクダンジョンでは、死亡時にラン中取得品を持ち帰れない設計にします。死亡時はラン開始時の所持状態へ戻し、成功帰還時はラン中取得品をすべて持ち帰ります。
- セーブは1ファイルのみを前提にし、拠点でのみ保存できます。ラン中はメモリ上の所持品スナップショットを使い、永続化は拠点へ戻ってから実行します。
- `帰還` は拠点画面へ遷移します。死亡後に帰還した場合も、通常ステージでは現在の所持品とリング装着を保持します。
- `PauseMenu`、`Inventory`、`Ring`、`LevelUp` 中でもHP0を検出した場合は、各画面状態を閉じて `GameOver` を優先します。
- ローグライク用リザルトは、不可思議の迷宮の専用記録として扱います。ランキング、難易度選択、チェックポイント詳細、複雑な解放要素は実装しません。恒久強化は拠点機能強化と最小プレイ性能強化だけ実装します。

### ステージ進行とワープポイント

- 通常ステージは `StageCatalog` のストーリー面を選択し、開始からクリアまで遊べるステージ進行として扱います。
- 通常ステージには、`DungeonLayout::warpPointAnchors` から露出配置のワープポイントを複数配置します。スタート直後とゴール直前に寄りすぎないmainPath上の候補を使い、壁ではなく空洞上に見える状態で配置します。
- ワープポイントは `stageId`、`index`、`tilePosition`、`discovered`、`undiscoveredLightRadiusTiles=3`、`discoveredLightRadiusTiles=6` を持ちます。将来のダンジョン入口UIやセーブに接続しやすいよう、発見済みワープポイントは `discoveredWarpPoints()` から参照できる構造にします。
- ワープポイント周辺はチャンク生成時に小さめの安全空洞として扱い、壁に埋まらないようにします。
- ワープポイントは `WorldIconRenderer` 経由で `assets/others/img_15.png` を縮小描画し、1px黒アウトラインを付けます。周囲リングは未発見が黄、発見済みが水色で表示します。画像が読めない場合は従来の円形簡易エフェクトへフォールバックします。
- 未発見時は周囲3タイル程度、発見後は周囲6タイル程度の自発光を持ちます。光源は既存の `LightSource` 配列へタイル単位からpxへ変換して追加し、既存の円形視界と統合します。
- プレイヤーがワープポイントへ触れるとHPを全回復し、実際にHPが回復した場合はプレイヤー位置に緑の回復パルスを表示します。未発見の場合は `discovered=true` にして右側ダンジョンログへ「ワープポイント発見」を表示します。発見済みワープポイントに再接触しても再発見表示は出しませんが、HP全回復は行います。
- 発見済みワープポイントへ一定距離まで近づくと、ダンジョン内から拠点へ帰還できます。帰還前には確認UIを出し、確定しない限り探索状態は変更しません。
- `DungeonLayout::startTile` 付近にはダンジョン内の `ダンジョン入口` を描画します。入口へ一定距離まで近づいて調べた場合も、発見済みワープポイントと同じ帰還確認UIを使って拠点へ戻れます。
- ワープポイント解放数はSaveDataにも保存します。
- 最新ワープポイント位置もSaveDataへ保存します。古いセーブなどで位置がない場合は、解放済みワープポイント数と仮配置間隔から推定した位置を使います。
- ワープポイントから再開する時、保持中ダンジョンの宝箱や木箱などで開始位置が塞がっている場合は、近傍の通行可能な空きタイルへプレイヤー開始位置だけを押し出します。ワープポイント自体の保存位置や発見状態は変更しません。
- ワープポイント到達時、その時点の探索状態を死亡時リトライ用のメモリスナップショットとして保存します。死亡した瞬間には新しいスナップショットを作りません。
- ワープポイント到達時スナップショットの保存対象は、プレイヤー位置、HP、レベル、経験値、所持品、リング装着、掘削済み地形、敵状態、未取得ワールドドロップ、木箱破壊状態、宝箱開封状態、月のカケラ取得状態、ワープポイント発見状態、ボス出現状態です。
- スナップショット復元時も敵状態と未取得ワールドドロップは保持し、エフェクト、一時弾、MagicSystemの一時魔法、短時間演出、一時状態異常だけを破棄します。
- そのステージに配置された全ワープポイントを発見し、かつそのステージを1回以上クリアしている場合、拠点のダンジョン入口から任意再生成を選べます。現行固定8個の場合は8個すべて、将来ワープポイント数が可変化した場合は配置済み総数を判定に使います。
- 2周目以降は、現在保持しているダンジョン内のボスをまだ倒していなくても、全ワープポイント発見済みであれば任意再生成を選べます。
- 再生成前には未回収ドロップが消えることと、ボス再戦用に地形も作り直すことを警告表示します。確認後、地形、敵、未取得ワールドドロップ、木箱、宝箱、月のカケラ、ワープポイント配置を作り直します。
- 再生成しても、所持品、MaterialInventory、お金、強化状態、図鑑、ストーリー進行は保持します。
- ローグライクダンジョンではワープポイントを配置しません。不可思議の迷宮では生成時にワープポイント数を0とし、通常ステージ状態保存やワープポイント再入場の対象外にします。
- 旧仕様ではラン中の掘削数25到達でボスを出現させていましたが、現在はこの条件を使いません。
- 通常ステージでは、8個目に見つけたワープポイントを深部到達地点として扱います。
- 8個目のワープポイントから、スタート地点から遠ざかる方向へ20マス進んだ地点をボス出現地点にします。20マスは既存タイルサイズ基準です。
- プレイヤーがボス出現地点付近に到達すると、`BossEncounterState` による共通ボスフローを開始します。初回は `boss_before:<stageId>` 会話を先に再生し、会話完了後に `StageDefinition.bossEnemyId` の敵を `EnemySystem` のボスとして出現させます。ただし対象ステージの再戦ボスを捕獲中の場合は、捕獲品が手持ち、倉庫、リング装備のどこかに残っている間、そのステージのボスは再出現しません。
- 捕獲した再戦ボスを売却、破損消失、その他の消費処理で所持から失った場合は、次に条件を満たしたダンジョンで再びボスと再戦できます。
- ボス敵は通常敵と同じ出現、経験値、報酬、撃破フローを使います。移動や特殊ギミックは通常AIに加え、必要に応じて `BossActionRuntime` の共通ボスアクションシーケンスで上書きできます。星くずモグラは小ジャンプ潜行、ランダム地中待機、土煙予告、その場大ジャンプ、突進、壁衝突硬直を行います。ジャンクラブはガラクタリングと転倒を行います。アストラグナは通常敵HPではなく専用外殻・封印パーツ・守護星接触救出を `BossActionRuntime` 内で処理し、救出成功時だけ共通撃破フローへ合流します。
- ボスを撃破すると共通撃破演出へ入り、ゲーム進行を停止します。演出後に `boss_after:<stageId>` 会話を再生し、通常ボスは会話完了後に `StageClear` へ移行します。再戦時のボス捕獲成功も撃破扱いとし、同じフローに入ります。
- 初回ボスは捕獲できません。捕獲可能になるのは、そのステージを1回以上クリアした後の再戦ボスだけです。
- `boss_before` / `boss_after` のステージ会話は初回だけ再生します。再戦時は通常の戦闘、報酬、クリア遷移だけを行います。
- 再戦ボスも通常どおり経験値、お金、素材ドロップの対象です。ラスボス再戦でも報酬は発生します。
- 初回撃破専用の追加報酬は設けません。クリア報酬はアイテムやスコアではなく、シナリオ進行として扱います。
- ステージクリア画面では次ステージ解禁、最低限のクリア記録、拠点帰還操作を表示します。シナリオパートは `boss_after` 会話と拠点帰還後の `stage_clear:<stageId>` 会話へ分離します。
- ステージクリア画面の確認後、フェード遷移で拠点画面へ戻ります。
- ラスボス初回撃破時は共通撃破演出と `boss_after` 会話の後、`StageClear` を挟まずエンディング紙芝居へ移行し、紙芝居完了後に拠点へ戻ります。
- ボス撃破時に次ステージ解禁フラグとして `unlockedStages` を進め、SaveDataに保存できるようにします。
- 複数ステージ定義、通常ステージ進行、ローグライク面の基本フロー、ステージ別ボスID解決、星くずモグラの共通ボスアクションシーケンス、ジャンクラブの専用ボスFSMは接続済みです。アストラグナ以降の専用パターン、不可思議の迷宮のバランス調整は継続作業です。

### SaveData

- セーブファイルは1つだけです。標準の保存先は `%LOCALAPPDATA%/MajoShovel/save.dat` で、ルネの家の中にある `日記` から手動保存します。将来用として帰還時自動保存フラグ `auto_save_on_return` も保存できます。
- SaveDataは `MAJO_SHOVEL_SAVE_V1` から始まるUTF-8の行指向テキストです。JSONライブラリは使わず、`key value...` 形式で人間が読める状態を優先します。
- 後方互換として、存在しない行は既定値で補います。ヘッダー不正、読み込み不能、古いセーブの不足項目はクラッシュさせず、新規データまたは既定値で継続し、警告ログを出します。
- セーブデータにObjects DB全定義はコピーしません。通常アイテム、倉庫、リング、商人品揃えは `objectId` 参照だけを保存します。
- 所持金、プレイ時間、プレイヤー状態: `money`、`play_time_seconds`、`player_level`、`player_xp`、`player_xp_to_next` を保存します。`play_time_seconds` はタイトル/オープニングと開発用編集画面を除いたゲーム内滞在時間を秒数で保存し、日記セーブ画面では時間・分へ丸めて表示します。
- リュック内アイテム: Objects DB由来スタックの `object objectId count`、個体アイテムの `object_instance instanceId objectId currentDurability maxDurability enhanceLevel attackBonus digBonus durabilityBonus weightModifier sizeModifier protectionEnabled isBroken` を保存します。
- 杖装備スロット: `equipped_staff instanceId` を保存します。未装備時は `-` です。ロード時に参照先の `ItemInstance` がリュック内に存在しない、カテゴリ `杖` ではない、壊れている、または同じ `instanceId` がリング搭載中の場合は装備解除して警告ログを出します。
- 倉庫内アイテム: `warehouse_object objectId count`、`warehouse_object_instance ...` を保存します。個体アイテムはロード後も同じ `instanceId` で扱います。
- MaterialInventory: `material old_wood_building_material/enhancement_ore/moon_fragment/mana_drop count` を保存します。
- リング装着: `ring type objectId damage damageType digPower durability maxDurability weight hitRadius hitInterval localAngle instanceId enhanceLevel attackBonus digBonus durabilityBonus weightModifier sizeModifier protectionEnabled isBroken ringIndex` を保存します。`ringIndex` は0始まりです。
- リングプリセット: 登録済み枠は `ring_preset presetIndex registered` を保存し、各登録アイテムは `ring_preset_item presetIndex ringIndex type objectId instanceId localAngle currentDurability maxDurability enhanceLevel attackBonus digBonus durabilityBonus weightModifier sizeModifier protectionEnabled isBroken` を保存します。`presetIndex` と `ringIndex` は1始まりです。
- 保護、破損、強化値: リュック個体、倉庫個体、リング装着中個体の `protectionEnabled`、`isBroken`、`enhanceLevel`、各補正値を保存します。
- 拠点強化状態: `upgrade_max_hp`、`upgrade_ring_radius`、`upgrade_ring_speed`、`upgrade_collection_range`、リング別レベルアップ強化の `level_ring_points ring radius speed weight` を保存します。旧形式の `level_ring_radius_points`、`level_ring_speed_points`、`level_ring_weight_limit_points` は読み込み互換として扱い、全リングに同じポイントを復元します。
- 倉庫容量、商人、加工台、リング工房: `warehouse_capacity_level`、`merchant_upgrade_level`、`merchant_refresh_pending`、`merchant_stock_version`、`merchant_stock objectId price`、`high_value_buy_object objectId`、`processing_unlock_level`、`ring_workshop_unlocked`、`workshop_initial_radius_level`、`workshop_initial_speed_level`、`workshop_shift_distance_level` を保存します。
- 図鑑状態: `codex_entry kind id stage` でアイテム/宝/敵の解禁段階を保存し、`codex_effect objectId effectKey` で効果発見済み状態を保存します。
- 採掘進行: `current_stage`、`current_stage_id`、`unlocked_stages`、`unlocked_warp_points`、`latest_warp x y` を保存します。日記セーブ画面の進行表示では `current_stage_id` そのものではなく、`unlocked_stages` から最新解放済み通常ストーリーダンジョンを求めます。通常ストーリー3ステージをクリア済みの場合は `ストーリークリア` と表示します。
- 通常ステージのダンジョン状態保持はランタイムのメモリ状態として扱います。帰還後も掘削済み地形、敵状態、未取得ワールドドロップ、木箱破壊状態、宝箱開封状態、月のカケラ取得状態、ワープポイント発見状態を保持しますが、現行SaveDataへ地形全体や敵個体状態は書き出しません。
- ストーリー進行フラグ: `story_flag flagName` を複数行で保存します。現時点ではステージクリア時に `stage_clear_N` を追加します。
- セーブ読込時、Objects DB由来アイテムのIDが現在のDBに存在しない場合、そのIDを保持した `欠損アイテム(objectId)` として復元します。該当DB定義が戻れば、次回取得や表示更新で通常定義を参照できます。

### スペルリング

- 最大3本のスペルリングを扱います。通常画面ではアクティブリングを切り替え、リング画面では選択中リングの配置を編集します。
- リング上にはスコップと松明が装着されています。
- リング上アイテムは当たり判定位置を維持したまま、描画のみ上下に浮き沈みします。
- 浮き沈み中のリング上アイテムには地面影を描画し、影の濃さは主人公の影と同じです。
- 初期配置では、スコップと松明は同じリング上で180度ずれた位置にあります。
- 初期リング半径は1マス分、つまり16pxです。
- アイテム位置は物理演算ではなく、角度、半径、速度から数式で計算します。
- リング装着アイテムは `objectId` を持つ場合、Objects DBの `攻撃力`、`ダメージ種別`、`掘削力`、`耐久力`、`重さ` を参照します。DBが読めない、またはIDが見つからない場合は、既存のスコップ/松明のフォールバック値で動きます。
- 敵への接触ダメージは、装着アイテムの攻撃力を基準に、ダメージ種別の軽い倍率、プレイヤー攻撃補正、`orbit_power`、速度ダメージボーナス、杖装備の `ring_output_mul`、防御補正を通して計算します。
- 地形掘削は、装着アイテムの掘削力と効果コードを使ってタイルへダメージを与えます。スコップ以外のObjects DBアイテムでも掘削力または地形向け効果コードがあれば地形を掘れます。杖装備の `dig_power_mul` と `ring_output_mul` は接触掘削ダメージへ乗算します。
- 地形掘削の相性は道具名ではなく地形属性で分岐します。`dig` は通常掘削、`dig_hard` は `hard` / `ore` 向け掘削として扱います。
- スコップは `soft` 向けの通常掘削道具です。`hard` / `ore` も削れますが、硬い地形特化道具より効率は落ちます。
- ツルハシは `hard` / `ore` 向けの掘削道具です。硬い地形には `dig_hard` を使い、`soft` ではスコップ以下の効率に抑え、単純な上位互換にしません。
- 特殊タグは分類、検索、ドロップ候補の絞り込み、演出SEルーティングに使います。`hard_dig_tool` などの特殊タグ単体では掘削効果を発動せず、挙動は `dig`、`dig_hard`、`dig_multi` などの効果コード側で発火します。
- 耐久力は敵命中または地形掘削で1ずつ減り、0になると破損イベントを1回だけ発生させます。破損イベントではリング上のアイテム位置を中心に、移動方向へ流れない破片、煙、リング状のパーティクルを表示します。右側ダンジョンログには、入手ログと同じアイコン付き表記で「アイテム名 が壊れた」を表示します。保護OFFのリングアイテムは従来通り消滅し、保護ONのリングアイテムはリング上に残ります。`耐久力=-1` は壊れない扱いです。
- 破損中のアイテムは、インベントリ、拠点UI、リング詳細、リング上表示などの通常表示で、名前の先頭に `壊れた` を付けます。画像アイコンは共通描画オプションで暗く描画し、選択枠や破損瞬間の演出は通常通り見えるようにします。ただし、破損した瞬間の右側ポップ/ダンジョンログだけはイベント通知として扱い、「アイテム名 が壊れた」の元の名前と通常アイコンを使います。
- 重さはリングごとに装着重量として集計します。各リングの初期装備重量と杖装備の `ring_weight_limit_add` を基準に、そのリングで追加重量が増えるほど対象リングの基本速度へ軽い減速補正をかけます。`metal_weight_penalty_mul` はこの減速係数へ乗算し、`orbit_speed` などの軌道効果倍率は重量補正後の速度へ別倍率としてかかります。

### リングプリセット

- リングプリセットは3枠持ちます。各プリセットはリング1〜3すべての配置をまとめて登録し、用途別の編成をキー一発で呼び出せるようにします。
- 登録時は、各リング上の `objectId`、`instanceId`、リング番号、`localAngle`、耐久値、強化値、補正値、保護状態、破損状態を保存します。
- 呼び出し時は、まず登録時と同じ `instanceId` の個体を探します。同一個体がなければ、同じ `objectId` の別個体から、強化値、攻撃/掘削/耐久補正、現在耐久、最大耐久、軽量化/大型化、破損状態、保護状態がより近いものを優先して選びます。
- 登録アイテムが現在のリング、リュックのどちらにも存在しない場合、その枠は空として扱い、他の配置だけを適用します。
- プリセット呼び出しでリングから外れるアイテムはリュックへ戻します。戻し先が足りず入れ替えできない場合は、プリセット適用を中止してメッセージを表示します。

計算式:

```text
itemAngle = baseAngle + itemIndex * spacing
x = ringCenter.x + cos(itemAngle) * radius
y = ringCenter.y + sin(itemAngle) * radius
```

### リング中心ずらし

- 右クリックまたはゲームパッド `LT` を押している間、リング中心がプレイヤーの向き方向へ移動します。ゲームパッドでは右スティック入力中、その方向を優先します。
- 離すと滑らかに通常位置へ戻ります。

### リング投げ

- `C` キーまたはゲームパッド `RT` でアクティブリングを投げます。
- 投げる対象はアクティブリングだけです。投げ中も他リングは通常どおりプレイヤー周囲に残ります。
- 投げ開始時に、進行方向側のリング上の点を切れ目として記録します。リングはその切れ目位置から線状へ滑らかにほどけ、切れ目側を先頭にして前方へ発射されます。
- 一定距離または一定時間が経過すると、到達点をピークにして滑らかにUターンする戻り状態になります。
- 戻り状態では、投げ開始時の切れ目とリング上で反対側にあたる点へ線が収束し、通常のリング形状へ戻ります。
- 投げたリングが戻り終わるまで、アクティブリング切替は行いません。
- クールダウンがあります。

### 暗闇と光源

- ゲーム画面は基本的に暗闇です。
- 光源から半径 `light_radius` px の円内だけ、地形、敵、リングアイテムを描画します。
- 初期設定では `light_radius=200` です。
- 光は半透明グラデーションではなく、円の内側は見える、外側は見えないというハードな視界です。
- 現在の光源はプレイヤー魔女の体中央付近と松明位置です。プレイヤー魔女の光源半径は基準値の2倍を使います。
- 松明はリング上を移動するため、プレイヤー周囲とは別の場所を照らせます。

### 地形

- タイルサイズは48pxです。
- チャンクサイズは32x32タイルです。
- 地形はチャンク内の配列で管理します。
- 基本地形は、空洞、土、岩、鉱石、硬い岩です。
- 地形属性は `soft`、`hard`、`ore` の3種類です。土は `soft`、岩と硬い岩は `hard`、鉱石は `ore` です。空洞は掘削対象外です。
- 土、岩、鉱石、硬い岩にはHPがあり、リング上の掘削アイテムの当たり判定でダメージを受けます。
- 硬い岩は岩よりHPが高いが、通常の掘削処理で破壊可能です。描画は現時点では濃い灰色の仮色です。
- HPが0になると空洞になります。
- `dig` は `soft` に通常効率で効き、`hard` / `ore` には低効率で効きます。効果コードなしで `digPower` だけを使う旧フォールバック掘削は廃止し、地形掘削は `dig` / `dig_hard` / `dig_multi` の効果コード経由で発動します。
- `dig_hard` は `hard` / `ore` に高効率で効き、`soft` では効率を落とします。ツルハシ系はこの効果コードで硬い地形への役割を持たせます。
- `dig_multi` は対象タイルと上下左右の計5タイルを掘る複数掘削です。地形属性への相性は通常掘削と同じです。
- チャンク生成時は `DungeonLayout` のmainPath/branchPathからの距離を参照し、パス周辺を掘削誘導用の土として配置します。スタート、ワープポイント、特殊部屋、goalTile周辺だけを初期空洞の中心にし、ワープポイント同士を連続した空洞通路では接続しません。土の外側を岩/鉱石、遠い場所を岩/硬い岩にします。ただし各距離帯の地形は単一種類で固めず、土、岩、鉱石、硬い岩、空洞を混ぜます。掘削導線は土を主成分にしますが、途中に小さな空洞や岩/鉱石も混ざります。壁の中にも自然な小空洞が一定量発生します。
- 境界にはseed由来のノイズと洞窟幅の揺らぎを入れ、チャンク境界で洞窟が切れないようワールドタイル座標ベースで判定します。
- goalTile周辺にはやや広めの空洞を作り、将来のボス地点や次層入口を接続できる余地を残します。

### 敵

- 敵は固定容量のオブジェクトプールで管理します。
- スコップで地形を掘り、タイルが空洞化したとき、その掘削地点からスポーンします。
- 開始直後から時間経過だけで湧く定期スポーンは行いません。
- プレイヤー周辺のタイルグリッドにBFSで距離場を作り、空洞をたどってプレイヤーへ近づきます。
- 敵は地形の実タイルを参照して移動し、壁を貫通しません。
- プレイヤーに接触するとダメージを与えます。
- スコップまたは松明に当たるとダメージを受けます。
- HPが0になると非アクティブ化され、経験値を落とします。
- ボス敵は全ワープポイント発見後に設定されるボス出現地点付近で1回だけ出現します。ボス撃破時は通常の死亡イベントとは別にボス撃破イベントを出し、`Game` の共通ボスフローが撃破演出、ボス後会話、通常ボスのステージクリアまたはラスボス初回のエンディング紙芝居へ遷移します。
- 再戦ボスの捕獲成功はボス撃破イベントと同じ扱いにします。初回ボスは捕獲不可です。対象ステージの再戦ボス捕獲品を所持している間、そのステージのボスは再出現しません。

### レベルアップ

プレイヤーレベルの上限は100です。Lv100到達後は追加経験値を蓄積せず、経験値表示は `MAX` とします。

必要経験値は `xp_base + 現在Lv * xp_per_level` で計算します。標準値は `xp_base=12`、`xp_per_level=8` なので、Lv1→2は20、Lv50→51は412、Lv99→100は804です。

レベルアップ時は最大HPがレベル別テーブルに従って自動上昇します。拠点強化の最大HPアップはこの値に別枠で加算します。レベルアップによって最大HPが増えた場合、現在HPも増加分だけ回復します。全回復はしません。

最大HPテーブル:

- Lv1-10: `10, 13, 16, 18, 22, 25, 27, 30, 33, 36`
- Lv11-20: `38, 40, 43, 44, 46, 48, 49, 52, 54, 56`
- Lv21-30: `58, 59, 61, 62, 64, 65, 66, 68, 70, 72`
- Lv31-40: `73, 75, 76, 77, 79, 80, 81, 82, 84, 85`
- Lv41-50: `86, 87, 89, 90, 91, 92, 93, 94, 95, 96`
- Lv51-60: `97, 98, 99, 101, 102, 103, 104, 105, 106, 107`
- Lv61-70: `108, 109, 111, 112, 113, 114, 115, 116, 117, 118`
- Lv71-80: `119, 120, 121, 122, 124, 125, 126, 127, 128, 129`
- Lv81-90: `130, 131, 132, 133, 134, 135, 136, 137, 138, 139`
- Lv91-100: `140, 141, 142, 144, 145, 146, 147, 148, 149, 150`

レベルアップ発生時は、強化カード選択ウィンドウを即表示せず、先に短いレベルアップ演出を再生します。演出中はゲーム進行と選択入力を停止し、プレイヤー周囲にキラキラ粒子を出し、ダメージポップアップとは別系統の `Level UP!!` ポップアップを表示します。演出が完了してから、対象リングタブと3枚の強化カードを表示します。

レベルアップ演出では `se.level_up.jingle` を共通ジングル処理で再生します。ジングル中は現在のBGMを停止し、ジングルの実再生時間が終わったら直前のBGM、またはジングル中に予約されたBGMを再開します。ジングルが未登録の場合でも、フォールバック秒数で同じ停止/再開フローを維持します。
タイトル `レベルアップ` の下には、小さい文字で `リングの強化を選ぼう` と表示します。

強化対象はリング1〜3のどれか1つです。初期選択は現在のアクティブリングです。`Z` / `X` またはリングタブのクリックで対象リングを切り替えます。

1. 選択中リングの半径 +0.1倍
2. 選択中リングの速度 +0.1倍
3. 選択中リングの重量上限 +1.0kg

リング半径とリング速度のレベルアップ強化は、リングごとのポイント加算倍率として扱います。倍率は `1.0 + 0.1 * ポイント数` で、同じリングに2回選ぶと1.2倍、3回選ぶと1.3倍です。

各選択肢はキーボードの `1` / `2` / `3` と、マウスクリックの両方で選択できます。
カード上には `1` / `2` / `3` の番号を表示しません。ウィンドウ下部の操作説明にも直接決定キーは表示しません。
各カードはやや細めの幅で3枚を中央寄せにします。カード同士の間隔は狭めに取り、それに合わせてレベルアップウィンドウの横幅も少し小さくします。
各カードの項目名と説明文は中央揃えにします。選択中テキストは出さず、枠と背景色だけで選択状態を示します。
各カードには、実際の現在値から強化後の値への変化を `現在値 → 強化後の値` 形式で表示し、強化後の数値は黄色で表示します。
各カードには現在の段階を `強化回数：1` 形式で表示します。
選択後は共通の結果表示ウィンドウを開き、以下のような文言と実数値の変化を表示します。変化行の変更後の数値は黄色で表示します。

- リング半径: `リング1のサイズが大きくなった！` / `リング1 半径: 54 → 59`
- リング速度: `リング2の回転速度が速くなった！` / `リング2 回転速度: 2.18 → 2.39`
- リング重量上限: `リング3の重量上限が拡張された！` / `リング3 重量上限: 10.0kg → 11.0kg`

### UIクリック基盤

- `UiContext` が1フレーム分のマウス位置、左クリック、消費状態を管理します。
- `UiRect` はスクリーン座標上の矩形当たり判定です。
- UIがクリックを処理した場合、`UiContext` がポインター入力を消費します。
- 虫とりアミは左クリック入力ではなくリング接触判定で処理します。ポインター入力の消費状態は、捕獲判定そのものには使いません。

### UI表示規約

UIの見た目と配置は `src/engine/Ui.hpp` / `src/engine/Ui.cpp` の共通部品を正とします。細かい色、余白、ヘッダー高さ、フッター高さ、ボタン間隔は `ui::` 定数を変更すれば、共通部品を使う画面へ一括反映されます。

- 基本ウィンドウは紺色、半透明、白枠です。
- ウィンドウは `Header / Body / Footer` に分けます。
- Header は濃い黒紫で塗りつぶし、タイトルを左揃えで表示します。
- Header タイトルは白、太字風、通常本文より少し大きめです。
- 操作説明が必要なウィンドウは、Footer に小さめの文字で表示します。
- Footer には操作説明だけを置きます。
- Footer とダンジョン下部の操作説明は、`InputHelpGlyph` でキー、マウス、ゲームパッドボタン、スティック、十字キーをインラインアイコンとして描画します。`WASD/方向キー`、`AWSD/方向キー`、`WASD/矢印` は個別キー列ではなく、移動用の十字キー/左スティック系アイコンへまとめて表示します。
- 操作説明のマウスアイコンは、ダンジョン中の `右長押し 中心ずらし` だけに使います。クリックによる決定、戻る、文字送り、施設調査、デバッグ画面の選択などは操作説明には表示しません。`F/Enter`、`Esc`、ゲームパッドの決定/戻るボタンは表示します。
- ダンジョンと拠点の画面最下部に出す通常操作説明は、メッセージ表示中、会話開始待ち、確認/結果/数量ダイアログ、アイテム/リング/一時停止/拠点施設などのウィンドウ表示中には表示しません。ウィンドウ自身のFooterはそのウィンドウ内の補助表示として扱い、画面最下部の通常操作説明とは分けます。
- 入力アイコンは現在の最終入力デバイスを優先し、キーボード/マウス操作中はキーキャップとマウス、ゲームパッド操作中はA/B/X/Y、LB/RB、LT/RT、スティック、十字キーを優先します。割当変更済みの操作は `InputAction` の `bindingMap` を参照して表示します。
- アイコン描画は画像アセットに依存しないプロシージャル描画を基本とし、`Renderer::fillSoftCircle`、`drawSoftLine`、`drawSoftRing` などのsoft系プリミティブでアンチエイリアス付きにします。任意PNGアトラスへ置き換える場合も、同じ `InputHelpGlyph` の入口を使います。
- `素材が足りません` などの状態メッセージは Footer へ置かず、Body 内の該当ボタン下部へ中央揃えで表示します。
- 決定・実行系ボタンは Body の右側に置きます。
- 戻る・キャンセル系ボタンは Body の左側に置きます。
- 共通の結果表示ウィンドウのOKボタンは、下中央ボタン位置から30px下げて配置します。
- ボタンは `assets/UI_buttons.png` を使います。上から通常、実行系、キャンセル系の3行で、各行は透明な1pxガイド線で横3分割されています。
- ボタンの横幅は可変です。左右ブロックは固定描画し、中央ブロックだけをタイル描画します。ガイド線解析とタイル描画はウィンドウ素材と同じ共通ロジックを使います。
- ボタンの縦幅は `ui::ButtonHeight` の53px固定です。
- モーダルも同じ `Header / Body / Footer` 構造を使います。
- 共通の結果表示ウィンドウはタイトルを表示しません。本文は中央揃えにし、タイトルを出さない分だけ本文とOKボタンを上に寄せ、ウィンドウ高さも小さめにします。
- ウィンドウは特に無効化しない限り、20フレーム程度の開閉アニメーションを適用します。
- 開く時は透過率を 1 から 0、拡大率を 0.9 から 1.0 へ変化させます。
- 閉じる時は透過率を 0 から 1、拡大率を 1.0 から 1.1 へ変化させます。
- タブや左右ペイン切替は、選択中を白、非選択をグレー寄りで表示します。
- 詳細パネルは左ラベル、右値の構造にし、右値は折り返し表示を使います。
- 外枠から本文までは `ui::PanelPadding` を基準にします。
- セクション間、ボタン間、フッター余白は `ui::` 定数を基準にします。

共通描画関数:

- `UiWindowScope`: ウィンドウ全体へ既定の開閉アニメーションを適用し、スコープ内の本文描画にも同じ拡大率と透明度を反映します。個別に無効化する場合は `animated=false` を指定します。
- `drawUiWindow`: パネル、Header、Footerをまとめて描画します。
- `drawUiHeader`: Header背景と左揃えタイトルを描画します。
- `drawUiFooter`: Footerに操作説明を描画します。
- `InputHelpGlyph`: FooterやHUDの操作説明文字列を解析し、`F/Enter`、`F/Enter/Esc`、`Esc`、`Q/E`、`Z/X`、`Shift+R`、`Tab`、`WASD/方向キー`、`右長押し` などを入力アイコン列として描画します。明示指定が必要な場合は `{act:Confirm}`、`{key:Esc}`、`{move}` のようなタグも使えます。
- `drawUiButton`: `assets/UI_buttons.png` の通常、実行系、キャンセル系のいずれかを使い、横幅可変・縦幅固定ボタンを描画します。
- `drawUiBodyMessageBelow`: ボタン下部に中央揃えの状態メッセージを描画します。
- `uiBottomLeftButtonRect`: Body左下の戻る・キャンセル系ボタン位置を返します。
- `uiBottomRightButtonRect`: Body右下の決定・実行系ボタン位置を返します。

任意PNG描画API（Renderer）:

- `ImageHandle acquireImage(path, filter)`: PNGをキャッシュへ登録し、ハンドルを返します。`filter` は `Nearest` / `Linear` を指定できます。
- `drawImage(handle, center, size, options)`: ハンドル指定で描画します。`options` ではアンカー、色、回転、反転、1px単位アウトライン（有効/色/太さ）を指定できます。アウトライン太さはスクリーンピクセル基準です。
- `drawImage(path, center, size, options, filter)`: パス直接指定の簡易経路です。内部で `acquireImage` を使います。
- `getImageSize(...)`: 読み込み済み画像のピクセルサイズを返します。
- `invalidateImage(path)` / `invalidateAllImages()`: キャッシュの破棄APIです。アセット差し替え時に使用します。
- `setImageCacheBudgetBytes(bytes)`: 画像キャッシュ予算を設定します。予算超過時は未使用テクスチャからLRUで解放します。
- `imageCacheStats()`: キャッシュ使用量、ヒット/ミス、ロード失敗数を取得します。
- ゲーム側では `ItemImageRenderer` / `ObjectImageRenderer` / `EnemyImageRenderer` / `WorldIconRenderer` を介して呼び出し、`drawImage(path, ...)` を直接散在させない方針にします。共通の縮小・アウトライン処理は `ScaledImageRenderer` と各画像種別の共通描画関数に集約します。

音声API（AudioEngine）:

- `AudioEngine` は `src/engine/Audio.hpp/.cpp` に置く、SDL3ベースの音声再生基盤です。ゲーム固有イベント名や画面状態は持たず、ID指定でBGM/SEを再生します。
- `App` は `SDL_INIT_AUDIO` を含めてSDLを初期化し、起動時に `assets/audio/audio_manifest.tsv` を読み込みます。音声デバイスを開けない場合でもゲーム起動は継続し、音声だけを無効化します。
- 現時点の対応音源はSDL3の `SDL_LoadWAV` で読めるWAVです。OGG、MP3、ストリーミングBGM、ループポイント付きBGMは後続拡張とします。
- マニフェスト形式はTSVで、列は `id`、`type`、`path`、`volume`、`loop`、`cooldown_ms`、`display_name` です。`type` は `bgm` または `se`、`path` は `assets/audio/audio_manifest.tsv` からの相対パス、または `assets/` 始まりのパスを使います。`display_name` は編集ツール用の日本語表示名で、再生APIは引き続き `id` を使います。
- `playBgm(id, fadeSeconds, restart)` は既存BGMを必要に応じてフェードアウトし、新しいBGMをフェードイン再生します。`stopBgm(fadeSeconds)` はBGMだけを停止します。
- `cueDurationSeconds(id, type)` は読み込み済みcueの実再生秒数を返します。未登録や未変換の場合は `std::nullopt` とし、演出側はフォールバック秒数を使います。
- `Game` 側の共通ジングル処理は、現在のBGM cueを退避して `stopBgm` でBGMを止め、SE cueをジングルとして再生し、`cueDurationSeconds` で取得したキュー長の経過後に退避BGMまたはジングル中に予約されたBGMを再開します。個別演出はBGM停止/再開を直接実装せず、この共通処理を使います。
- `playSe(id, volumeScale)` はSEを多重再生します。`maxSeVoices` で同時再生数を制限し、`cooldown_ms` で連打抑制できます。
- 音量は `master`、`bgm`、`se` の3バスで管理します。値は `settings.json` に保存し、起動時に `AudioEngine` へ反映します。一時停止メニューの `オプション` > `音量` から即時変更できます。
- `assets/audio/audio_manifest.tsv` または `assets/audio` 配下のWAV変更時は、アセット監視からマニフェストを再読み込みします。再読み込み時点で鳴っている音は保持され、次回再生から新しい定義を使います。
- 仮音源は `tools/generate_placeholder_audio.ps1` で生成します。出力先は `assets/audio/bgm/` と `assets/audio/se/` です。既存WAVを残して不足分だけ生成する通常作業では `-OnlyMissing` を指定し、全WAVを作り直したい場合はスイッチなしで実行します。SEだけを高品質寄りの合成で上書きする場合は `-OnlySe -HighQualitySe` を指定します。正式音源へ差し替える場合もIDは維持し、原則として `audio_manifest.tsv` の `path` だけを変更します。
- デバッグパネルの編集ツールには `BGM編集` と `効果音編集` を置きます。どちらもゲーム画面内の `AudioCueEdit` で開き、左に用途としての日本語表示名、右に対象フォルダ内のWAV候補、詳細欄に内部ID、現在パス、選択候補を表示します。`display_name` が空の場合だけ内部IDを代替表示します。
- `AudioCueEdit` のcue IDリストとWAV候補リストは共通スクロールリストUIを使います。内容が表示領域を超える場合はスクロールバーを表示し、マウスホイール、スクロールバーのドラッグ、トラッククリックで表示位置を移動できます。リスト内容は枠内でクリップし、枠外にはみ出す行や文字は描画しません。ホイールスクロール中に選択中の行へ自動で戻さず、方向キーなどで選択を移動した時だけ選択行を表示範囲へ追従させます。
- `BGM編集` は `assets/audio/bgm/`、`効果音編集` は `assets/audio/se/` を再帰スキャンします。新しい本番音源や比較用音源は、仮WAVと同じフォルダへ追加して `再スキャン` すれば候補に出ます。`custom/` などの別フォルダは現時点では使いません。
- 音声編集画面では `試聴` で選択WAVを一時cueとして鳴らし、`適用` で選択中cue IDの実行時定義と画面上の `path` を差し替えます。`保存` または `Ctrl+S` で `audio_manifest.tsv` にUTF-8 BOM付きTSVとして書き戻し、`AudioEngine::reloadManifest()` を呼びます。変更対象は原則 `path` だけで、音を使う側の `playBgm("bgm.*")` / `playSe("se.*")` 呼び出しは変更しません。
- 初期IDは `bgm.title`、`bgm.base`、`bgm.dungeon`、`bgm.boss`、`se.ui.confirm`、`se.ui.cancel`、`se.ui.menu_open`、`se.ui.tab_switch`、`se.ui.book_open`、`se.ui.item_move`、`se.ui.item_use`、`se.ui.ring_place`、`se.ui.upgrade_select`、`se.level_up.jingle`、`se.transition`、`se.dig.hit`、`se.dig.break`、`se.dig.ore_break`、`se.attack.hit`、`se.pickup`、`se.boss.spawn`、`se.boss.defeat`、`se.player.damage`、`se.player.pinch`、`se.ring.throw`、`se.enemy.defeat`、`se.enemy.spawn`、`se.enemy.alert`、`se.enemy.attack`、`se.enemy.shoot`、`se.enemy.heal`、`se.projectile.impact`、`se.ring.guard`、`se.ring.reflect`、`se.magic.cast`、`se.magic.impact`、`se.capture.throw`、`se.capture.success`、`se.capture.fail`、`se.discovery`、`se.discovery.warp`、`se.chest.open`、`se.crate.break`、`se.item.break`、`se.explosion.tick`、`se.explosion.boom`、`se.explosion` です。リング戻り音は現時点では作りません。
- 現在接続済みの音イベントは、Opening/Titleの `bgm.title`、Baseの `bgm.base`、Playingの `bgm.dungeon`、ボス戦の `bgm.boss`、共通画面遷移の `se.transition`、UIの決定/キャンセル/メニューオープン/タブ切替、本棚、アイテム移動/使用、リング配置、レベルアップ選択/ジングル、採掘ヒット/破壊/鉱石破壊、リング投げ、プレイヤー被弾、ピンチ警告、敵の出現/発見/攻撃/射撃/回復/通常撃破、敵命中、弾の壁衝突/リング防御/反射、魔法の発動/着弾、捕獲の成功/失敗、宝箱開封、木箱破壊、リングアイテム破損、隠し発見、ワープポイント発見、爆発予兆クリック、専用爆発、拾得、ボス出現/撃破です。
- 採掘音は `DiggingSystem::hitTiles()` が空でなければ `se.dig.hit`、`dugTiles()` が空でなければ `se.dig.break` または鉱石用の `se.dig.ore_break` を鳴らします。連続採掘の鳴りすぎは `audio_manifest.tsv` の `cooldown_ms` で抑制し、初期値は `se.dig.hit` が70ms、`se.dig.break` が90ms、`se.dig.ore_break` が110msです。
- アクション系SEは下位システムへ `AudioEngine` を渡さず、`EnemyEvent`、`ProjectileSoundEvent`、`MagicSoundEvent` などの意味イベントを `GameCore.cpp` で集約して再生します。直接 `Game` が状態を確定する宝箱、木箱、ワープ発見、アイテム破損などは `GameDungeon.cpp` からID再生します。
- UI音は `UiContext` に `UiSoundEvent` を蓄積し、`Game::update` の終了時に `Game` 側で `AudioEngine` へ流します。`Ui.cpp` は音源IDや `AudioEngine` を直接知らず、共通UIヘルパーは意味イベントだけを発行します。
- `UiSoundEvent` の対象は、`Confirm`、`Cancel`、`MenuOpen`、`TabSwitch`、`BookOpen`、`ItemMove`、`ItemUse`、`RingPlace`、`UpgradeSelect` です。共通ヘルパーではキャンセルボタン、結果/数量ダイアログ、コマンドメニュー、ドロップダウン、タブを接続済みです。個別画面の直書きUIは、タイトル、ポーズ、ゲームオーバー、ステージクリア、アストラル結果、拠点施設/採掘開始UI、ダンジョン帰還確認、リング画面、インベントリ画面、レベルアップ選択から接続します。

環境設定（Settings）:

- 進行セーブとは別に `%LOCALAPPDATA%/MajoShovel/settings.json` へ保存します。ファイルがない場合はデフォルト設定を作成し、壊れている場合はログ警告を出してデフォルトで起動します。
- `src/engine/Settings.hpp/.cpp` の `GameSettings` / `SettingsStore` が、設定値の構造、正規化、JSON読み書きを担当します。設定ファイルは `version`、`audio`、`video`、`performance`、`presentation`、`input` を持ちます。
- `audio` は `masterVolume`、`bgmVolume`、`seVolume` を0.0〜1.0で持ち、`App` 起動時と設定変更時に `AudioEngine` の3バスへ即時反映します。
- `video` は `windowMode`（`windowed` / `borderless`）、`windowWidth`、`windowHeight`、`vsync` を持ちます。起動時にウィンドウ生成へ使い、`F4` の borderless 切替、ウィンドウリサイズ、VSync変更を保存します。
- `performance` は `lightweight` を持ちます。軽量化ON時はリング装着アイテム由来の光源を維持したまま、それ以外のダンジョン中の追加ライトを優先度付きで絞り、暗幕グラデーション描画を粗くし、影描画、常時装飾エフェクト、粒子数を抑えます。敵数、Projectile数、ドロップ取得判定などゲーム性に直結する処理は軽量化では変更しません。
- `presentation` は `brightness`、`screenShake`、`inputIcons` を持ちます。`brightness` は0.70〜1.30の数値で保存する画面全体の最終明るさ補正で、暗闇限定ではなくタイトル、拠点、ダンジョン、UI、紙芝居を含む最終フレーム全体へ反映します。古い `dark` / `standard` / `bright` の保存値は読み込み時に互換変換します。`screenShake` は画面揺れ倍率で、OFFは0、弱は0.5、標準は1.0として扱います。`inputIcons` は操作ヘルプの入力アイコンを自動、キーボード/マウス固定、ゲームパッド固定から選びます。
- `input` は `InputAction` 単位の `bindings` を持ちます。キーボード、マウスボタン、ゲームパッドボタン、ゲームパッド軸を保存でき、起動時に `Input` へ反映します。必須アクションが空の場合はデフォルト割当で補完します。
- 一時停止メニューの `オプション` は `操作`、`音量`、`画面` のタブ構成です。変更は `Input`、`AudioEngine`、SDLウィンドウ/Rendererへ即時反映し、`settings.json` へ遅延保存します。開発中はデバッグコンソールから `settings show`、`settings audio master|bgm|se VALUE`、`settings video mode windowed|borderless`、`settings video vsync on|off`、`settings video size WIDTH HEIGHT`、`settings performance lightweight on|off`、`settings presentation brightness 0.70-1.30`、`settings presentation shake off|low|standard`、`settings presentation icons auto|keyboard|gamepad`、`settings input show [ACTION]`、`settings input reset`、`settings input clear ACTION`、`settings input add ACTION DEVICE VALUE`、`settings reload`、`settings save` も使えます。
- `操作` タブは分類タブ、操作行、`キーボード/マウス` 列、`ゲームパッド` 列を持つ共通選択テーブルUIで、セルを決定すると物理入力待ちになり、最初に入ったキー/マウスボタン/ゲームパッドボタン/ゲームパッド軸でその列を上書きします。`Esc` は入力待ち中止、`Backspace` / `Delete` は選択セルの割当削除です。
- 操作設定UIでは、同じ物理入力が別操作に割当済みの場合に確認ダイアログを出し、承認時だけ既存割当を外して移します。選択項目初期化、分類初期化、全初期化を持ち、変更は `Input` へ即時反映し、`settings.json` へ遅延保存します。必須操作は全デバイスの割当を空にできません。
- `音量` タブは `Master`、`BGM`、`SE` のスライダーを持ち、左右入力またはスライダークリック/ドラッグで5%単位相当の調整と直接指定ができます。
- `画面` タブは表示モード（ウィンドウ / フルスクリーン）、ウィンドウサイズ、VSync、軽量化、明るさ、画面揺れ、入力アイコンを変更できます。フルスクリーンは内部的にはボーダーレス全画面として扱います。ウィンドウサイズ候補は 1280x720、1600x900、1920x1080、2560x1440 です。軽量化の初期値はOFF、明るさは70%〜130%のゲージで初期値は100%、画面揺れの初期値は標準、入力アイコンの初期値は自動です。
- 各オプションタブの本文下部にはヘルプウィンドウを置き、選択中の項目説明と直近の変更ステータスを同じ領域で確認できるようにします。

## アーキテクチャ

### `src/engine`

ゲーム固有の軽量エンジン層です。汎用エンジン化は狙わず、このゲームに必要な機能だけを持たせています。

- `App`: SDL初期化、ウィンドウ生成、メインループ、終了処理
- `Time`: delta time、FPS計測
- `Input`: キーボード/マウス/ゲームパッド入力のアクション抽象化。`InputBinding` により物理入力と `InputAction` を分離します。操作設定UIの物理入力待ちは `InputRemapCapture` が担当し、通常入力へ流す前のSDLイベントから捕捉します。
- `Audio`: BGM/SEのID管理、WAV読み込み、SDL3音声ストリーム、ミックス、BGMフェード、SE多重再生、cue長取得、音量バス
- `Settings`: 音量、画面、操作設定バインドを進行セーブと分けて保存する環境設定
- `Ui`: スクリーン座標UIのクリック判定、ポインター入力消費、簡易ボタン描画、共通タブ描画、スクロール可能な選択テーブル
- `Renderer`: SDL描画APIのラッパー。英数字は内蔵ドットフォント、日本語を含むUTF-8文字列はGDI+でTTFからテクスチャ化して描画します。プレイヤー用スプライトシートを読み込み、必要なコマだけを切り出して描画します。加えて任意PNGを扱う公開API（`acquireImage` / `drawImage` / `invalidateImage`）を持ち、内部でテクスチャキャッシュと予算超過時のLRU解放を行います。
- `Camera`: ワールド座標とスクリーン座標の変換
- `ObjectPool`: 敵や将来のエフェクト用の固定容量プール
- `FileWatcher`: `data` / `assets` 配下の変更監視

### `tools`

- `dev_auto_reload.ps1`: コード変更を監視し、自動ビルドとゲーム再起動を行う開発用スクリプト

### `src/game`

ゲーム固有ロジックです。

- `Game`: 各システムの更新/描画順、画面モード、一時停止メニュー、リング画面を管理
- `Player`: プレイヤー状態、移動、向き
- `TileMap` / `Chunk`: チャンク式タイルマップ
- `SpellRingSystem` / `SpellRingItem`: スペルリング状態、リングアイテム位置
- `ScaledImageRenderer`: 任意PNGをアスペクト比維持で最大サイズ内に縮小し、Nearestフィルタ、1px固定アウトライン、選択中アウトラインを共通描画する低レイヤです。
- `ObjectImageRenderer`: `Objects` タブの `画像番号` を `assets/objects/obj_番号.png` に解決し、Objects個別スケールを適用して `ScaledImageRenderer` へ渡します。
- `EnemyImageRenderer`: `Enemies` タブの `画像番号` を `assets/enemies/en_番号.png` に解決し、敵本体描画とアイコン用フレーム切り出しに使います。アイコン描画は任意の向きベクトルとアニメーション時刻を受け取り、リング上の捕獲敵だけ4方向と歩行フレームを切り替えます。
- `ItemImageRenderer`: アイテム表示の共通入口です。`ItemVisualRef` に従い、通常Objects画像と捕獲敵画像を同じUI/リング描画経路から描き分けます。
- `ObjectVisualPose`: Objects DBの `地面角度` / `リング角度` から、地面・UI・リング描画で使う回転角度を解決します。
- `WorldIconRenderer`: Objects DBに属さないワールド用アイコンを意味付きIDから `assets/others/img_番号.png` に解決し、Others個別スケールを適用して、お金、素材、宝箱、木箱、ワープポイント、上部HUDのアイコン描画に使います。
- `DiggingSystem`: スコップによるタイル掘削
- `EnemySystem`: 敵のスポーン、更新、戦闘、プール管理
- `LevelSystem` / `UpgradeSystem`: 経験値、レベルアップ、3枚カード式の強化選択
- `InventorySystem`: 掘削で得たアイテムの保持、使用、リング追加、ショートカットHUD、アイテム画面
- `DebugOverlay`: デバッグ表示

### `src/data`

- `GameBalance.hpp`: 現在のバランス値を集約しています。
- `RuntimeBalance.hpp` / `RuntimeBalance.cpp`: `data/game_balance.cfg` を読み込むランタイムバランス定義です。
- `GoogleSheetSource.hpp` / `GoogleSheetSource.cpp`: Googleスプレッドシートの公開CSV取得、CSVパース、設定読み込みを行います。
- `ObjectCatalog.hpp` / `ObjectCatalog.cpp`: `Objects` タブの列を読み、正規化済みの `ObjectDefinition` へ変換します。効果文字列のパース、タグ分割、数値変換、検証、`ItemRegistry` と `objectsById` の構築も行います。加えて `効果コード一覧` / `特殊タグ一覧` を読み、効果コード定義と特殊タグ定義をMap化して保持します。効果コード未定義やtarget不一致などの検証警告は `ObjectCatalog::validationWarnings` に集約します。
- `StageCatalog.hpp` / `StageCatalog.cpp`: `Stages` タブを読み、`StageDefinition` と `StageCatalog::stagesById` を構築します。ステージ基本情報と生成パラメータの保持、検証警告、デフォルト4ステージへのフォールバックを担当します。

データ定義はゲームロジックから分離し、ローカル設定ファイルとスプレッドシート由来データの両方を扱えるようにしています。

## パフォーマンス方針

- 土タイル1個ごとの重いオブジェクトは作りません。
- 地形はチャンク内の配列で管理します。
- プレイヤー周辺チャンクのみを生成/描画します。
- 掘削判定はスコップのhitRadiusに重なるタイル範囲だけを見ます。
- 敵は毎回 `new/delete` せず、固定容量プールで管理します。
- スペルリング武器はRigidbody的な処理ではなく、数式ベースで制御します。
- 当たり判定は軽量な円/矩形/グリッド判定を使います。
- ファイル監視はOS依存APIではなく、軽量なmtimeポーリングで実装しています。
- 描画は現状SDLの即時描画ですが、タイル数が増えた場合はチャンク描画キャッシュまたはバッチ化を優先します。

## 現在の制限

- スプレッドシートは `--test-play` 付きのテストプレイ起動時のみ読み込みます。通常起動と実行中のオンライン再取得は行いません。
- Googleスプレッドシート取得はWindows版のWinHTTP実装です。
- スプレッドシートは認証なしの公開CSVとして取得するため、非公開シートや権限が必要なシートは読めません。
- `Objects`、`効果コード一覧`、`特殊タグ一覧` は読み込み、正規化、検証、保持、起動ログ確認までです。特殊タグはワールドドロップ候補の絞り込み、拠点売却対象の判定、演出SEルーティングに使います。特殊タグから直接、状態異常、ガード、属性効果、捕獲効果は発動しません。
- Objects DB由来アイテムは拾得、アイテム画面表示、通常効果使用、リング装備、リング上の基本パラメータ反映、リングから外した時のインベントリ返却、拠点での宝系売却、虫とりアミ系アイテムのリング接触捕獲まで実装済みです。通常使用効果は未対応コードを安全に無視します。
- 効果コードの実処理は最小セットのみ接続済みです。chance系、buff/debuff系、属性魔法系、反射系、ガード系の基本処理は接続済みです。未実装効果コードは安全に無視し、必要に応じてデバッグログ対象にします。
- 現在遊べるステージ進行は、ストーリー3面とローグライク面 `不可思議の迷宮` のStageCatalog定義、ステージ選択、次ステージ解禁保存まで接続済みです。ステージごとの専用ボス品質、配置バランス、通しプレイ調整は継続作業です。虫とりアミと捕獲敵のリングアイテム化は最小実装済みです。
- ボスは既存EnemySystemの簡易拡張です。専用AI、専用攻撃、専用アセット、敵の質向上はまだありません。ボス発見会話、出現、撃破演出、撃破後会話、通常ボスのクリア結果画面、ラスボス初回撃破後のエンディング紙芝居遷移は共通フローとして接続済みです。クリア報酬はアイテムではなく、シナリオ進行と次ステージ解禁だけです。
- GameOver周りは基本仕様のみです。不可思議の迷宮の専用リザルトとハイスコアは実装済みです。ランキング、難易度選択、チェックポイントの詳細化は未実装です。恒久強化は拠点強化炉の拠点機能強化と最小プレイ性能強化のみ実装済みです。
- 所持品持ち帰りはメモリ上のラン再開始と、拠点での1ファイルセーブ永続化に対応しています。ラン中の任意地点セーブ、複数スロット、クラウド同期は未実装です。
- 敵は壁を貫通しませんが、追跡はプレイヤー周辺のBFS距離場に基づく簡易実装です。
- 照明は本格的なライトマップではなく、光源円内/外の描画切り替えです。
- 音は `AudioEngine` 基盤と仮WAV、BGM/画面遷移/UI/採掘/攻撃/拾得/敵挙動/ボス演出の接続、デバッグ編集ツールからのBGM/SE差し替えまで実装済みです。正式音源そのものは未実装です。音量設定保存、windowed/borderless、ウィンドウサイズ、VSync、軽量化、明るさ、画面揺れ、入力アイコン、操作設定バインド保存と、それらを変更するオプションUIは実装済みです。ゲームパッド入力は基本操作、右スティック照準、`LT` リング中心ずらし、`RT` リング投げまで実装済みです。セーブは拠点限定の1ファイルSaveDataとして実装済みです。
- 買い戻し、珍品販売、重量化、小型化、追加効果付与は採用しません。2本目/3本目リングはストーリー進行に応じて解放されます。リング工房では解放しません。
- 自動テストターゲットはまだありません。
- 英数字UIとデバッグ表示は内蔵の簡易ASCIIドットフォント、日本語UIは `assets/fonts/craftmincho.otf` を使用します。
- C++コード変更のプロセス内ホットリロードは未対応です。コード変更後は再ビルドが必要です。
- `dev_auto_reload.bat` 使用時は、実行中ゲームを動かしたまま再ビルドし、成功時だけプロセス再起動を自動化できます。
- 実行中プロセス内でのC++関数差し替え、DLLホットスワップ、状態を保ったままのコード更新は未対応です。

## 拠点・倉庫・素材・強化実装前の整合性メモ

2026-05-01時点の監査では、Objects DB、効果コード一覧、特殊タグ一覧は `ObjectCatalog` で読み込み、`EffectSpec` の構文不正、未定義効果コード、未定義特殊タグ、target不一致、排他タグ衝突、効果コード名と同名の特殊タグを起動時ログへ集約します。追加で旧コード/旧タグと互換targetも `deprecated_entry` として警告します。

- 現在のObjects DBで使われているEffectSpec targetは `area`、`enemy`、`orbit`、`player`、`projectile`、`terrain` です。`ground` は現行DBには出ていませんが、旧データ保護のため `terrain` 相当として検証と実行側で互換扱いします。杖装備効果用に `equip_all`、`equip_ring1`、`equip_ring2`、`equip_ring3` もtargetとして認識しますが、カテゴリ `杖` の `通常効果` 欄以外では検証警告にします。
- 旧移行対象は警告対象です。`hard_dig` は `hard_dig_tool`、`detect_hidden` は `hidden_detector`、`detect_treasure` は `treasure_detector`、`heal_enemy` と `buff_enemy` は `target=enemy` のEffectSpec、`stun_chance` は `status_stun_chance` へ寄せます。`orbit_anchor` と `orbit_shift` は特殊タグではなくEffectSpecの効果コード側で扱います。
- 現行DB上の旧効果コード使用は `item_firefly_bottle` の `area:detect_hidden` と `item_compass` の `area:detect_treasure` です。旧特殊タグの使用は検出されていません。DB本体の大量修正は別作業にします。
- 特殊タグは分類、検索、ドロップ候補、売却対象、材質/属性の参照、演出SEルーティングに使います。`poison`、`slow` などのタグだけで状態異常を付与せず、`light_source` タグだけで照明を発動せず、`shield` タグだけで防御処理を発動しません。状態付与は `status_*` 効果コード、照明は `light` 効果コード、軌道補正は `orbit_*` 効果コードを `EffectDispatcher` 経由で処理します。
- `InventorySystem` の通常使用、`SpellRingSystem` の装備反映、掘削接触時の軌道効果、敵/Projectile命中時の効果は `EffectSpec` を `EffectDispatcher` へ渡す経路です。カテゴリ `杖` の `通常効果` は杖装備効果用として保持し、通常使用の `EffectDispatcher` 経路へは渡しません。未実装効果コードはクラッシュさせず、必要に応じて未実装ログへ出します。
- `GameOver`、`StageClear`、拠点帰還相当の処理では所持品・リング装着・経験値・解放済みワープポイントを保持します。通常ステージでは掘削済み地形、敵状態、未取得ワールドドロップ、木箱破壊状態、宝箱開封状態、月のカケラ取得状態、ワープポイント発見状態も保持し、エフェクト、一時弾、MagicSystemの一時魔法、短時間演出、一時状態異常だけを破棄します。倉庫・素材・強化を追加する場合は、この帰還境界で永続化するものとランタイム保持するものを分けます。
- MSVCビルドでは `/utf-8` を維持します。仕様書、ソース、設定ファイルはUTF-8保存を前提にします。

## ダンジョン生成拡張前の監査メモ

2026-05-05時点の監査では、洞窟ネットワーク式生成を入れる前の接続点は以下です。当時はStageParams DBを作らず、既存の1ステージ縦スライス、ワープポイント、ボス、GameOver、StageClearの流れを維持する前提でした。現在はStageCatalogによる複数ステージ定義へ移行済みですが、以下の接続点メモは地形生成・帰還境界の確認用として残します。

- チャンク生成は採掘開始時の `WorldLoading` 内にある初期 `TileMap::updateAround(player.position)` と、`Game::update` の `TileMap::updateAround(player.position)` でプレイヤー周辺範囲を確保します。加えて `TileMap::render`、`tileAtWorld` 経由の衝突、掘削、敵経路探索でも未生成チャンクが遅延生成されます。
- 通常のゲーム起動では、拠点表示前にダンジョン全体の新規生成を行いません。拠点用の初期状態、所持品、スペルリング、セーブ読込だけを行い、ダンジョン新規生成はダンジョン入口で開始を選んだ後の `WorldLoading` へ遅延します。
- 地形生成は現在 `TileMap::initializeChunk` 内で完結し、ワールドタイル座標から `Empty`、`Dirt`、`Rock`、`Ore` とHPを決めます。実装上のタイルサイズは `balance::TileSize=48`、チャンクは32x32タイルです。
- `Game` はラン開始時にチャンク生成とは独立した `DungeonLayout` を生成して所有します。`TileMap::initializeChunk` はチャンク境界内だけで完結する配列生成を維持しつつ、DungeonLayoutの距離情報を参照して初期地形タイプとHPを決めます。
- 掘削結果は `DiggingSystem` が `hitTiles`、`openedTiles`、`dugTiles`、`rewardDropRequests` を返します。埋まり報酬や隠れ敵は、タイルが空洞化した `openedTiles` / `dugTiles` を消費する処理として `Game::update` の掘削直後へ挿入できます。
- 掘削イベントはカウンタ方式です。各カウンタは掘削で空洞化したタイル数を数え、minimum未満では発生禁止、minimum以上guarantee未満では低確率発生、guarantee到達で確定発生します。発生に成功した時点で該当カウンタを0へ戻します。
- 掘削時ランダム敵スポーンは `EnemySystem::spawnFromDugTiles(digging.openedTiles())` です。`enemy_min_dug_tiles=16`、`enemy_guarantee_dug_tiles=30` を使い、従来より明確に頻度を落とします。生成済み隠れ敵を入れる場合は、同じ空洞化タイルに対して「隠れ敵消費済み」フラグまたはイベント種別を持たせ、通常ランダムスポーンへ渡す対象から除外すれば二重発生を避けられます。
- 掘削時のお金発生は `dig_money_min_dug_tiles=6`、`dig_money_guarantee_dug_tiles=12` を使います。発生時は小額の `WorldDropKind::Money` を `WorldDropSystem::spawnMoneyDrop` で出し、拾得時は既存の所持金加算処理を使います。
- 掘削時の通常アイテム発生は `dig_item_min_dug_tiles=10`、`dig_item_guarantee_dug_tiles=30` を使います。発生時はObjects DBの既存ドロップ候補テーブルから選び、`WorldDropSystem::spawnDigItemDrop` でワールドドロップ化します。
- 掘削イベント全体の体感目標は「嬉しい結果2:敵1」程度です。敵ばかり出る状態を避け、採掘ゲームとして「掘ると何か出る」頻度を優先します。
- 光源は `Game::render` で `std::vector<LightSource> itemLights` に集約し、リングアイテム、ダンジョン入口、ワープポイント、ボス出現地点を渡しています。ワープポイント光源は既にこの経路に乗っており、洞窟ネットワーク式の入口、出口、祭壇なども同じ `extraLights` へ追加できます。
- ワールドドロップは `WorldDropSystem::spawnDigItemDrop`、`spawnRewardDrop`、`spawnObjectDrop`、`spawnMoneyDrop`、`spawnMaterialDrop` で生成します。`WorldDropSystem::update` がプレイヤー接触時にObjectsアイテムは `InventorySystem`、お金は所持金、素材は `MaterialInventory` へ反映します。素材は通常アイテム24枠を消費しません。描画時はObjectsアイテムを `ObjectImageRenderer`、お金と素材を `WorldIconRenderer` へ流し、画像がない場合だけ従来の円形フォールバックを使います。Objects DBの特殊タグはドロップ候補の絞り込みに使うだけで、タグ単体から直接効果を発動しません。収集術式により、接触拾得判定の前に近くの生成済みワールドドロップをプレイヤー方向へ吸い寄せます。吸い寄せても即時取得にはせず、満杯判定や素材加算は既存の接触拾得処理へ任せます。
- GameOver、StageClear、拠点帰還は `Game` がエフェクト、Projectile、MagicSystemの一時魔法、短時間演出、一時状態異常を破棄する境界です。通常ステージの洞窟レイアウト、掘削済み地形、敵状態、未取得ワールドドロップ、木箱、宝箱、月のカケラ、ワープポイント発見状態は保持対象で、ワープポイントスナップショットにも含めます。

監査結果は起動時に一度だけ `Dungeon generation extension pre-audit` ログとして出します。DebugOverlayのチャンク表示は `アクティブ数/生成済み総数` とし、遅延生成の増加を画面上で確認できます。

### DungeonLayout

`DungeonLayout` は `src/game/DungeonLayout.hpp` / `src/game/DungeonLayout.cpp` に置きます。`TileMap` のチャンク配列とは別のラン内レイアウト情報であり、StageParams DB、敵ID、アイテムID、報酬IDは扱いません。

- `DungeonGenerationContext`: `stageId`, `seed`, `stageHardnessMultiplier`, `roguelike` を持つ生成入力です。
- `DungeonLayout`: `stageId`, `seed`, `stageHardnessMultiplier`, `startTile`, `goalTile`, `mainPathPoints`, `branchPathPoints`, `warpPointAnchors`, `specialRoomAnchors` を持ちます。
- `startTile`: 既存開始地点付近のタイルです。現時点では `(0, 0)` を使います。Game側ではこの付近にダンジョン内の `ダンジョン入口` を描画し、拠点帰還用の調べる対象として扱います。
- `goalTile`: `startTile` から十分離したタイルです。方向は固定せず、seedから上下左右斜めへばらつかせます。
- `mainPathPoints`: startからgoalまでをつなぐタイル空間の点列です。直角通路ではなく、ノイズ付きポリラインで曲線、蛇行、ゆるいカーブを含めます。
- `branchPathPoints`: mainPath上の複数地点から伸びる短い枝道の点列です。
- `specialRoomAnchors`: 枝道終端またはmainPath周辺に置く特殊部屋です。各要素は `type`、`center`、`radius` を持ちます。通常生成で使う型は `OreRoom`、`CoinRoom`、`TreasureRoom`、`EnemyRoom` です。`SafeCavern` は既存互換・チュートリアル用の型として残しますが、通常生成ローテーションには入れません。
- `warpPointAnchors`: mainPathの進捗に沿って置くワープ候補です。Game側の露出ワープポイント配置に使います。

距離計算は `calculateDungeonLayoutMetrics` で行います。

- `distanceFromStart`: 現在タイルと `startTile` の直線距離です。
- `pathProgress`: 現在タイルから最も近いmainPath上の射影位置を、startからgoalまでの0.0から1.0で表した値です。
- `distanceFromMainPath`: 現在タイルからmainPath点列セグメントまでの最短距離です。

自然洞窟生成では、`mainPathPoints` と `branchPathPoints` の周辺を空洞ではなく柔らかい掘削導線にします。初期空洞はスタート、ワープポイント、特殊部屋、goalTile周辺に限定し、プレイヤーが土を掘り進めると次の空洞へ到達する構造にします。`distanceFromMainPath` はデバッグと進捗判定用にmainPathだけから算出し、枝道上にいる場合も主道からどれだけ外れているかを確認できる値として扱います。

地形HPは以下で決めます。

```text
effectiveHp = terrainBaseHp * stageHardnessMultiplier * localHardnessMultiplier
```

暫定のステージ硬さ倍率は、`stage_01_stardust=1.0`、`stage_02_junk_magic=2.0`、`stage_03_star_core=3.5`、`stage_04_astral_mine=4.0` です。`localHardnessMultiplier` はmainPath/branchPathから離れるほど高くなり、ノイズで局所差を持ちます。

DebugOverlayには `stageId`, `seed`, `startTile`, `goalTile`, `distanceFromStart`, `pathProgress`, `distanceFromMainPath` に加え、現在タイルの地形種別、現在HP/effectiveHp、localHardnessMultiplier、nearest warp index、nearest warp discovered、discovered warp count、current special room type、nearest special room type、reward node count、money node count、buried visible count、buried hidden count、exposed enemy node count、buried enemy node count、spawned enemy node countを表示します。これにより、レイアウト生成の向き、距離、主道からの外れ具合、地形硬さ、露出ワープポイントの発見状態、特殊部屋、報酬ノード配置状態、敵ノード配置状態を確認できます。

`Base` 中のF1デバッグ表示では、拠点中だけ最低限 `Base map mode`、`player base position`、`nearest facility name`、`hovered facility name`、`interaction available`、`pause menu return mode` を表示します。

### RewardNode / MoneyNode / ChestNode / CrateNode

洞窟ネットワーク式生成の報酬配置下地として、`Game` はラン内状態の `RewardNode`、`MoneyNode`、`ChestNode`、`CrateNode` を持ちます。Objects DBが空、該当 `objectId` を取れない、宝箱重み候補がない場合でも警告ログだけで継続し、クラッシュしない扱いにします。StageParams DBはこの段階では作りません。

- `PlacementVisibility::Exposed`: 空洞上にむき出しで置かれる配置です。光源範囲内で表示し、プレイヤー接触で拾得または金貨回収します。
- `PlacementVisibility::BuriedVisible`: 壁タイルに紐づく埋まり配置です。光源範囲内では壁上にキラキラ表示し、掘削でそのタイルが空洞化した時に報酬を出すか金貨を回収します。
- `PlacementVisibility::BuriedHidden`: 壁タイルに紐づく隠し配置です。通常壁と同じ見た目で、掘削でそのタイルが空洞化した時に報酬を出すか金貨を回収します。将来の `detect_hidden` / `detect_treasure` 効果はこのvisibilityとtile参照へ接続します。

配置ノードの `PlacementVisibility` は地形生成より優先します。`Exposed` のノードが壁や鉱石上に置かれた場合は、その周囲を小さなポケット空洞として強制的に空洞へ変えます。`BuriedVisible` / `BuriedHidden` のノードが空洞内に置かれた場合は、そのタイルを強制的に土、岩、鉱石、硬い岩のいずれかの壁へ変えます。ただし地形反映後、復元後、掘削済み地形、露出配置との競合、旧ラン状態などで埋まりノードのタイルがすでに `Empty` になっている場合は、そのノードを `Exposed` へ昇格します。薄く描画される埋まり宝箱や壁上キラキラは空洞床には残さず、空洞上では通常の開封または拾得対象として扱います。

配置生成時は、開始地点、ゴール、ワープポイント、既存の月のカケラ、報酬、金貨、宝箱、木箱をタイル予約として扱います。新しい配置候補が予約済みタイルや予約半径内に入る場合は、近傍タイルを探索して空き位置へずらします。近傍にも空きがない場合は、そのノードを生成しません。これにより、露出配置と埋まり配置が同じタイルを取り合ったり、木箱や宝箱がワープポイント上に置かれたりする状態を避けます。敵ノードの敵種抽選や敵テーブル接続はこの予約処理の対象外です。

通常報酬、金貨、宝箱の `BuriedVisible` は、mainPath中央ではなくmainPathから数タイル外れた側壁候補へ配置します。壁に紐づかない空洞上のヒント表示は作らず、空洞上に出た場合は `Exposed` として扱います。

`EnemyPlacementType::Exposed` の敵ノードは、ダンジョン全域を新規生成時に一括実体化しません。遠方で先に実体化すると、プレイヤーが空洞を見る前に敵AIが動いて配置地点から離れるためです。通常更新中に、プレイヤー光源または標準光源の半径に1タイル分の余白を足した範囲へ入った時だけ、ノードのタイル中心へ固定実体化します。実体化に成功したノードは `spawned=true` とし、通常敵と同じ描画、AI、当たり判定、捕獲処理に接続します。固定実体化は通常の敵ソフト上限では止めず、ObjectPool容量、地形上の配置可否、既存敵との重なりだけを確認します。

`RewardNode` は `tile`、`visibility`、`rewardKind`、optional `objectId`、`revealed`、`spawned`、`collected` を持ちます。むき出し報酬と埋まり/隠し報酬は `WorldDropSystem::spawnObjectDrop` でワールドドロップに接続します。埋まり/隠し報酬は `DiggingSystem::openedTiles()` を `Game::update` の掘削直後で消費します。`objectId` が空またはDBに存在しない場合は仮報酬としてノードだけ消費し、警告ログまたは通知で継続します。特殊タグから直接効果は発動しません。

`MoneyNode` は `tile`、`amount`、`visibility`、`collected` を持ちます。むき出し金貨と埋まり/隠し金貨は `WorldDropSystem::spawnMoneyDrop` でワールド上にばら撒き、プレイヤーが接触した時に `money_` へ加算します。金貨ノードは通常ステージ状態としてメモリ保持しますが、現行SaveDataへは書き出しません。

`ChestNode` は `tile`、`visibility`、`chestKind`、`depthRank`、`revealed`、`opened` に加え、ラン中に後から出現する宝箱用のジャンプ出現状態を持ちます。宝箱はランダム配置で、固定配置、ワープポイント近く固定、ボス後固定は行いません。目安は2画面に1〜2個です。配置状態は `PlacementVisibility::Exposed`、`PlacementVisibility::BuriedVisible`、`PlacementVisibility::BuriedHidden` の3種で、床置き、壁に埋まって光っている、壁に埋まって隠れている状態を表します。埋まり宝箱のタイルは硬めの壁になりやすいよう、配置後の地形上書きで岩/硬い岩を多めに選びます。

イベント完了、巣穴破壊、導入チュートリアルなど、ラン中に後から出現する宝箱は共通の宝箱出現処理を使います。生成された宝箱は最終タイルへ向かって、ワールドドロップと同じ `sin(t * pi)` 型の高度ジャンプで表示し、着地するまでは開封入力とリング接触開封を受け付けません。通常レイアウト生成時に最初から置かれる宝箱はジャンプ出現させません。

通常報酬、金貨、宝箱には、mainPathから少し外れた壁内へ `PlacementVisibility::Exposed` として置く壁内ポケット配置を追加します。この配置は小さな空洞を掘り抜いた先に露出アイテムや宝箱が見える状態を作り、プレイヤーが壁を掘って向かう目標として使います。

宝箱種別は通常宝箱 `C`、レア宝箱 `R`、超レア宝箱 `S` の3種です。描画は `WorldIconRenderer` 経由で通常/レア/超レアと開封前/開封後の `assets/others/img_4.png`〜`img_9.png` を使います。画像が読めない場合は従来の図形描画へフォールバックします。宝箱は接近して `F` / `Enter` で調べるか、攻撃/リング接触で開けられます。攻撃で開けても中身は減りません。開いた宝箱は空箱になり、再度中身を出しません。

宝箱を開けると、中身は即時取得ではなく `WorldDropSystem` へばら撒かれ、プレイヤーが近づいて拾います。ミミック系宝箱だった場合は開封時に箱系モンスターへ変身し、通常の宝箱中身は出しません。Objectsアイテムは必ず抽選します。抽選回数は通常宝箱が1〜2個、レア宝箱が1〜3個、超レア宝箱が2〜3個です。現在ステージID、深度ランク、宝箱種別からObjects重み列を解決し、その列の重みが1以上のObjectsアイテムだけを候補にして重み抽選します。候補なし時は警告ログを出し、クラッシュさせません。

宝箱と木箱から出るワールドドロップは、着地点を決める時に壁、ワープポイント、宝箱、木箱、既存のアイテム/お金/素材ドロップとの重なりを避けます。最初の半径で置けない場合は外側のリングへ探索範囲を広げ、見つかった空き位置をジャンプ演出の着地点にします。

追加報酬として、お金枠30%と素材枠30%を独立抽選します。両方同時に出てもよい扱いです。素材枠の対象は `old_wood_building_material`、`enhancement_ore`、`mana_drop` です。`moon_fragment` は通常宝箱素材枠には含めず、通常宝箱の主報酬にも含めません。宝箱素材の基本量は1〜3個、お金の基本量は通常宝箱20〜60、レア宝箱60〜160、超レア宝箱150〜400です。最終量は `基本量 × ステージ倍率 × 深度倍率 × 宝箱倍率` を切り上げ、最低1を保証します。

既存の地形タイル `Ore` を破壊した時は、既存Objectsドロップ抽選を維持したまま、`enhancement_ore` を確定でワールド上へばら撒きます。新しい鉱石ブロックオブジェクトは作りません。基本量は `ore_material_min`〜`ore_material_max`、既定1〜3個です。最終量はステージ倍率と深度倍率を適用して切り上げ、最低1個を保証します。鉱石タイル由来の素材ドロップはコード側で管理し、宝箱用Objects重み列は参照しません。

`CrateNode` は `tile`、`depthRank`、`destroyed` を持つ破壊可能ワールドオブジェクトです。木箱はランダム配置で、ローグライクステージにも配置します。目安は1画面に1〜2個です。木箱は攻撃/リング接触で破壊でき、破壊後は再度中身を出しません。

木箱は `old_wood_building_material` を確定ドロップします。基本量は1〜3個で、宝箱と同じステージ倍率、深度倍率、通常宝箱 `C` の宝箱倍率を適用し、切り上げ、最低1を保証します。お金は `crate_money_chance`、既定30%で発生し、基本量は5〜20です。お金量にも同じ倍率を適用します。

木箱の追加報酬は `crate_bonus_chance`、既定30%で発生します。木箱専用の重み列は作らず、現在ステージ・現在深度の通常宝箱 `C` テーブルを流用します。例として `stage_01_stardust` の深度2では `ST_C2` を使います。追加報酬も即時取得ではなく `WorldDropSystem` へばら撒きます。

`MoonFragmentNode` はワープポイント周囲に配置する `moon_fragment` 専用ノードです。ワープポイント生成時、各ワープポイントの周囲数タイル内へ1〜3個配置します。`moon_fragment` はワープポイント周囲が主入手経路で、通常宝箱の主報酬には含めません。配置状態は床に見えて落ちている `PlacementVisibility::Exposed` と、壁に埋まって光っている `PlacementVisibility::BuriedVisible` の2種だけです。壁に埋まって隠れている `PlacementVisibility::BuriedHidden` にはしません。ワープポイント安全空洞内に `BuriedVisible` が入った場合も、そのタイルだけ壁へ上書きして、光る埋まり月片として扱います。

月のカケラはワープポイント発見前後を問わず存在します。壁状態でもワープポイント自発光や通常光源の範囲内で薄く光って見えます。床置きは接近で、壁埋まりは掘って露出した時に `WorldDropSystem::spawnMaterialDrop(MaterialType::MoonFragment, 1, ...)` でワールド上へ出し、拾得時に `MaterialInventory` の `moon_fragment` へ加算します。取得済みノードは再出現しません。

`WorldDropItem` は後続のダンジョン保持仕様で保存できるよう、最低限 `kind`、`id`、`quantity`、`position`、`spawnedAtSeconds` を保持します。`kind` はObjectsアイテム、お金、素材を区別します。お金の `id` は `money`、素材の `id` は `old_wood_building_material`、`enhancement_ore`、`moon_fragment`、`mana_drop` のいずれかです。

未取得ワールドドロップは通常ステージの状態保持対象です。上限は `game_balance.cfg` の `world_drop_limit_per_stage` を使い、初期値は300です。上限超過時は古い低価値ドロップから自動削除しますが、`moon_fragment` は自動削除対象外です。

素材ドロップは `MaterialInventory` に直接加算します。素材4種は通常アイテムではなく、通常アイテム24枠を消費しません。セーブデータでは既存仕様通り `material old_wood_building_material/enhancement_ore/moon_fragment/mana_drop count` 形式で保存します。今回の実装では素材を通常インベントリへ入れません。

### EnemyNode

洞窟ネットワーク式生成の敵配置下地として、`Game` はラン内状態の `EnemyNode` を持ちます。具体的な敵ID、敵テーブル、敵出現グループの本格抽選はまだ行わず、現時点では既存 `EnemySystem` の通常敵選択へ接続します。敵IDや敵テーブルは将来 `Enemies` 側の定義とステージ別出現テーブルで扱い、生成側に直書きしません。

- `EnemyPlacementType::Exposed`: 空洞上にいる敵です。mainPath周辺の空洞候補へ置き、プレイヤー接近時に既存 `EnemySystem` でスポーンします。描画可否、BFS追跡、壁衝突は既存EnemySystemの処理を使うため、光源範囲内でだけ見え、壁を貫通しません。
- `EnemyPlacementType::BuriedHidden`: 壁に埋まっている隠れ敵です。見た目は通常壁と同じで、掘削により紐づくタイルが空洞化した時に既存 `EnemySystem` でスポーンします。

`EnemyNode` は `tile`、`placementType`、`dangerTier`、`enemySpawnGroup`、`spawned` を持ちます。`dangerTier` と `enemySpawnGroup` は将来の敵テーブル接続用フィールドで、今回の最小実装では出現敵の直接指定には使いません。隠れ敵ノードが消費した `openedTiles` は `EnemySystem::spawnFromDugTiles` へ渡す通常ランダムスポーン候補から除外し、同一タイルで隠れ敵と掘削時ランダムスポーンが重複しすぎないようにします。

### SpecialRoom

特殊部屋は `DungeonLayout::specialRoomAnchors` にチャンク生成とは独立したラン内レイアウトとして保持します。通常生成では `OreRoom`、`CoinRoom`、`TreasureRoom`、`EnemyRoom` を使い、ステージが進むほど追加部屋を増やします。StageParams DBはまだ作らず、敵ID、アイテムID、報酬IDも生成側には直書きしません。

- `OreRoom`: 小鉱脈部屋です。中央を空洞化し、周囲に鉱石が多く出るようにします。
- `CoinRoom`: 金貨部屋です。中心寄りを空洞化し、見えている `MoneyNode` を5〜8個追加します。金貨は部屋中心寄りの空洞内へ配置し、埋まり/隠し金貨は置きません。
- `TreasureRoom`: 宝物部屋です。中央を空洞化し、周囲に硬い岩を置きやすくします。`RewardNode` をむき出し/埋まり/隠し混在で追加します。
- `EnemyRoom`: 敵部屋です。空洞化し、むき出し敵と隠れ敵を多めに追加します。少量の報酬ノードも置けます。

`TileMap::initializeChunk` は従来どおりチャンク単位でタイル配列を初期化しますが、各タイルの地形決定時に `DungeonLayout` のmainPath/branchPath距離、warpPointAnchors、specialRoomAnchorsを参照します。これによりチャンク境界で洞窟や部屋が切れにくくなり、Game側の掘削、敵、光源、リング処理は既存の入口を維持します。

## アイテム・素材・拠点強化向けデータモデル

将来の1ファイルセーブに備え、アイテム保持は `StackItem`、`ItemInstance`、`MaterialInventory` に分けます。Objects DBの全定義をセーブへコピーせず、セーブ側は `objectId` を参照IDとして持ちます。DBから該当IDが消えていても、読み込み時に無視または未反映として扱い、クラッシュさせません。

### StackItem

スタック可能アイテム用の保存単位です。

```cpp
struct StackItem {
    std::string objectId;
    int count;
};
```

対象は回復薬、一時強化、一時弱体、その他の消費アイテムです。既存の24枠UIとの互換のため、実装上は `InventoryObjectStack` が `StackItem` を継承し、表示用に `ItemData` のキャッシュを持ちます。このキャッシュは表示・既存処理互換用で、保存時の正規データは `objectId` と `count` です。

### ItemInstance

個体差を持つ装備・リング装着アイテム用の保存単位です。

```cpp
struct ItemInstance {
    std::string instanceId;
    std::string objectId;
    int currentDurability;
    int maxDurability;
    int enhanceLevel;
    int attackBonus;
    int digBonus;
    int durabilityBonus;
    double weightModifier;
    double sizeModifier;
    bool protectionEnabled;
    bool isBroken;
    std::vector<EffectSpec> addedEffects;
    std::vector<std::string> addedTags;
};
```

`instanceId` は個体を一意に識別するIDです。`objectId` はObjects DB定義への参照であり、基礎パラメータ、通常効果、軌道効果、特殊タグはDB側を正とします。強化や保護で増える値だけを個体側へ持たせます。`addedEffects` は必ず `EffectSpec` として扱い、`addedTags` から直接効果を発動しません。

現段階では既存挙動維持を優先し、インベントリ上のObjects DBアイテムは従来通りスタック表示します。リングへ装着する時に `ItemInstance` を生成し、`SpellRingItem` へ `instanceId`、耐久、強化値、保護状態、追加効果、追加タグを写します。リング装着処理はDB定義を参照して基礎値を再適用し、個体側の強化値を足します。

### MaterialInventory

強化素材は通常アイテム枠を消費しない別管理です。素材は固定4種類です。

| 保存ID | 表示名 | 用途 |
|---|---|---|
| `old_wood_building_material` | 古木の建材 | 拠点拡張用 |
| `enhancement_ore` | 強化鉱石 | アイテム強化用 |
| `moon_fragment` | 月のカケラ | リング強化用 |
| `mana_drop` | 魔力のしずく | プレイ性能強化用 |

素材入手先は以下を基本にします。

| 素材 | 主入手 | 補助入手 |
|---|---|---|
| 古木の建材 | 木箱 | 宝箱 |
| 強化鉱石 | 鉱石タイル破壊 | 宝箱 |
| 魔力のしずく | 敵撃破 | 宝箱 |
| 月のカケラ | ワープポイント周囲 | 敵/ボスのレアドロップ |

`MaterialInventory` は素材種別ごとの個数だけを持ちます。素材は24枠の通常インベントリ、リング装備枠、消費アイテムスタックとは別に保存します。

### 既存互換と保存方針

- 既存の24枠仕様は維持します。Objects DB由来スタックと個体アイテムを表示します。
- 既存のリング装備処理は維持します。`SpellRingItem` に個体フィールドを追加し、未設定なら従来値で動きます。
- 現行セーブ形式は後方互換を保ちます。旧`ring`行は読み込めます。新しい`ring`行には `instanceId`、強化値、補正、保護、破損状態を末尾に追記します。
- `material`行で4種素材を保存します。素材行がない古いセーブは0個として扱います。
- DB定義が消えた `objectId` は、スタック復元では追加せず、リング装備では基礎パラメータ未反映のまま安全に継続します。
- 特殊タグから直接効果を発動しません。実際の効果処理は通常効果、軌道効果、追加効果の `EffectSpec` を `EffectDispatcher` に渡す経路に限定します。

## スタック、個体化、保護仕様

Objects DB由来アイテムは、取得時にスタック管理か個体管理へ振り分けます。強化素材4種は `MaterialInventory` に入り、リュック/倉庫枠を使いません。素材判定は保存IDまたは表示名で行い、`old_wood_building_material`、`enhancement_ore`、`moon_fragment`、`mana_drop` を保存名として使います。

スタック対象は、回復、一時強化、一時弱体、`consumable` / `food` / `potion` 系タグを持つ消費アイテムです。通常使用では `count` を1減らし、`ItemInstance` は生成しません。

スタックしない対象は、武器、盾、掘削道具、探索道具、魔導書、宝、捕獲敵アイテム、強化済みアイテム、リングに乗せたことがある消費アイテムです。取得時点で `InventoryObjectInstance` として `ItemInstance` を持ち、24枠内ではスタック一覧の後ろに個体一覧として表示します。

スタックアイテムをリングへ装備する場合は、`count` を1減らし、その1個分だけ `ItemInstance` を生成して `SpellRingItem` へ写します。生成された個体は `instanceId`、耐久、保護、強化値、補正、追加効果、追加タグを持ちます。リングへ乗せた個体はスタックへ戻しません。

強化処理でスタックアイテムを対象にする場合も同じく、`count` を1減らして `ItemInstance` を生成し、その個体へ強化値を適用します。現段階では個体フィールド、リング反映、専用の作業台UI、修理、攻撃力/掘削力/耐久力強化、強化リセット、軽量化、大型化を実装済みです。

保護ON/OFFは `ItemInstance` またはリング上の個体ごとに `protectionEnabled` として保持します。保護はダンジョン中のインベントリ画面、リング管理画面のどちらでも `P` で切り替えます。保護ONの個体は売却不可です。

耐久が0になった場合、`isBroken=true` として扱います。破損した瞬間は `SpellRingSystem` が破損イベントを保持し、`Game` がそのイベントを消費して破片パーティクル、画面通知、アイコン付きダンジョンログへ変換します。保護OFFのリングアイテムは従来通り消滅します。保護ONのリングアイテムは消滅せず残りますが、破損中は攻撃、採掘、Projectile防御、軌道効果を発揮しません。リュック内の個体アイテムは拠点の作業台で修理できます。

## 仮決定

- 最初のプレイ可能版では、手作業マップではなく簡易プロシージャル地形を使います。
- ワールド原点周辺は、開始直後に移動できるよう空洞にします。
- SDL_image、SDL_ttfはまだ使いません。SDL_ttfのプリビルド配布が環境のウイルス対策で隔離されたため、日本語表示はWindows版ではGDI+と同梱TTFで実装しています。
- 見た目は矩形、円、線、内蔵ドット文字、同梱TTFによる日本語文字で代用します。
- 敵の追跡はBFS距離場で実装し、必要になったら群れ制御や詰まり回避を追加します。
- 変更監視はまずポータブルなポーリング実装にし、必要になった段階でWindowsのファイル通知APIなどへ置き換えます。
- コード変更の自動反映は、外部ウォッチャーによるバックグラウンド再ビルドと成功時再起動方式にします。
- 世界観・ステージ構成は、本書の `世界観・あらすじ・ステージ構成 暫定` 章を暫定基準とします。
- ストーリー面は `星くずのダンジョン`、`魔導具廃棄層`、`落星の眠る地底` の3面構成を第一候補とします。
- フリー面は `不可思議の迷宮` を第一候補とし、毎回レベル1・初期状態から始まる高難度ローグライク試練として扱います。

## 次に実装すべき優先順位

1. 売却価格、強化費用、恒久成長量のバランス調整
2. ストーリー3面と不可思議の迷宮の通しバランス調整、ワープ後ボス到達距離、シナリオ進行、拠点遷移の整理
3. Objects DB由来アイテムの通常使用効果拡張、ショップ処理の拡張、杖/リング装備まわりの体験調整
4. 敵の質向上、詰まり回避、群れの押し合い、スポーン演出
5. 正式音源、画面設定の戻し確認、実機ゲームパッド確認
6. オプションUIの表示調整
7. スプレッドシート由来データの詳細バリデーション強化
8. リロード対象を画像、音、ステージ定義へ拡張
9. チャンク描画キャッシュまたはタイル描画バッチ化
10. ストーリー面3種とフリー面1種のステージ定義方式とシート運用を固める
11. 杖の種類によるリング投げ挙動の差別化
12. 虫とりアミの宝箱/ギミック捕獲拡張
13. 数学、チャンク参照、オブジェクトプール、レベルアップ処理の簡易テスト
14. `不可思議の迷宮` 用の星の歪み、特殊部屋、専用リザルトを調整し、試練ボス仕様を設計する

## 主要ファイル

- エントリポイント: `src/main.cpp`
- アプリ基盤: `src/engine/App.cpp`
- 入力: `src/engine/Input.cpp`
- UI基盤: `src/engine/Ui.cpp`, `src/engine/Ui.hpp`
- 描画: `src/engine/Renderer.cpp`
- 日本語フォント: `assets/fonts/craftmincho.otf`
- カメラ: `src/engine/Camera.cpp`
- ゲーム統括: `src/game/Game.cpp`
- 地形/チャンク: `src/game/TileMap.cpp`, `src/game/Chunk.hpp`
- スペルリング: `src/game/SpellRingSystem.cpp`, `src/game/SpellRingItem.cpp`
- 掘削: `src/game/DiggingSystem.cpp`
- 敵/戦闘: `src/game/EnemySystem.cpp`
- 経験値/強化: `src/game/LevelSystem.cpp`, `src/game/UpgradeSystem.cpp`
- インベントリ: `src/game/InventorySystem.cpp`
- バランス値: `src/data/GameBalance.hpp`
- ランタイムバランス: `src/data/RuntimeBalance.cpp`, `data/game_balance.cfg`
- スプレッドシート取得: `src/data/GoogleSheetSource.cpp`, `data/google_sheet_source.cfg`
- アイテム定義: `src/data/ObjectCatalog.cpp`
- ファイル監視: `src/engine/FileWatcher.cpp`
- テスト起動: `run_game.bat`
- 開発用自動再ビルド/再起動: `dev_auto_reload.bat`, `tools/dev_auto_reload.ps1`
## EffectDispatcher

効果発動は `ObjectCatalog` がObjects DBからパースした通常効果/軌道効果の `EffectSpec` を必ず経由します。特殊タグは分類、検索、ドロップ候補の絞り込み、演出SEルーティングに使い、特殊タグから直接効果を発動しません。

現在の実処理は最小セットです。`EffectDispatcher::registerFoundationHandlers()` は次のhandlerを登録します。これに加え、接触文脈が必要な一部効果は各システム側で同じ `EffectSpec` を参照して処理します。一部の基礎状態異常とbuff/debuffは、捕獲後挙動IDから生成される `EffectSpec` の安全な受け皿として、`効果コード一覧` に定義が欠けても登録します。

- `heal`: `target=player` / `owner` / `self` または `enemy` / `target` のHPを `value` 分だけ即時回復する。`duration=0` の即時効果として扱い、最大HPを超えない。
- `dig`: `target=terrain` の対象タイルへ通常掘削ダメージを与える。`soft` には `value` 分、`hard` / `ore` には低効率の補正後ダメージを与える。
- `dig_hard`: `target=terrain` の対象タイルへ硬い地形向け掘削ダメージを与える。`hard` / `ore` には `value` を基準に最低1.25倍、`soft` には低効率の補正後ダメージを与える。
- `dig_multi`: `target=terrain` の対象タイルと上下左右の計5タイルへ複数掘削ダメージを与える。各タイルの地形属性ごとに通常掘削相性を適用する。
- `light`: `target=area` のリングアイテムへ照明半径を付与する。`value` は照明レベルとして扱い、内部では半径へ変換する。
- `detect_hidden`: `target=area` のリングアイテムへ隠し要素探知半径を付与する。範囲内の隠し報酬、お金、宝箱を `BuriedVisible` へ変える。
- `detect_treasure`: `target=area` のリングアイテムへ宝探知半径を付与する。範囲内の宝系報酬と宝箱を `BuriedVisible` へ変える。
- `orbit_speed`: `target=orbit` の軌道効果として、リング回転速度倍率へ乗算集約する。
- `orbit_gravity`: `target=orbit` の軌道効果として、リング軌道半径倍率へ乗算集約する。1未満の値で軌道を内側へ寄せる。
- `orbit_power`: `target=orbit` の軌道効果として、リングアイテム接触ダメージと接触掘削ダメージの倍率へ乗算集約する。
- `orbit_antigravity`: `target=orbit` の軌道効果として、リング軌道半径倍率へ乗算集約する。1より大きい値で軌道を外側へ広げる。
- `orbit_anchor`: `target=orbit` の軌道効果として、リングずらし中のリング中心追従を安定させる。値が大きいほど、リング中心がプレイヤー移動や向き変更に振り回されにくくなる。
- `orbit_shift`: `target=orbit` の軌道効果として、リングずらし距離倍率へ乗算集約する。
- `damage_speed`: `target=orbit` の軌道効果として、速度依存ダメージ補正へ乗算集約する。
- `status_poison`, `status_slow`, `status_giant`, `status_paralyze`, `status_sleep`, `status_bleed`, `status_stun`, `status_confuse`: `target=enemy` / `target` または `player` / `owner` / `self` の `EntityStatus` へ状態を付与する。
- `status_poison_chance`, `status_slow_chance`, `status_paralyze_chance`, `status_bleed_chance`, `status_sleep_chance`, `status_stun_chance`, `status_confuse_chance`: `value` を0から100の確率パーセントとして扱い、成功時だけ対応する状態異常を付与する。
- `buff_attack`, `debuff_attack`, `buff_speed`, `debuff_speed`, `buff_defense`, `debuff_defense`: `value` を倍率、`duration` を秒数として `EntityStatus` のmodifierへ反映する。
- `knockback`: `target=enemy` / `target` の敵にノックバック速度を付与し、短時間AI移動より優先して押し戻す。
- `coin_drop_chance`: `target=enemy` / `target` の敵へリングアイテムが当たった時、`value` を0から100の確率パーセントとして扱い、成功時に1〜3Gの小額お金ドロップをその場へ生成する。敵撃破時の確定お金ドロップ量は変更しない。
- `nonlethal_hit`: `target=enemy` / `target` の軌道効果として、リングアイテム接触ダメージの最終適用時に敵HPを1で止める。`EffectDispatcher` handler ではなく、死亡判定直前の最終ダメージ調整が必要なため `EnemySystem` のリング命中処理で扱う。
- `capture_net`: `target=enemy` / `target` の軌道効果として、リングアイテムが敵へ接触した時に捕獲判定を行う。`EffectDispatcher` handler ではなく、敵との接触、耐久消費、捕獲成功時の敵除去が必要なため `EnemySystem` のリング命中処理で扱う。
- `inspect_enemy`: `target=enemy` / `target` の軌道効果として、リング上の虫眼鏡が敵へ接触した時に敵図鑑の詳細を記録する。表示内容は敵の説明とステータスまでで、捕獲後アイテムの効果開示には使わない。敵図鑑更新とHPゲージ表示条件の更新が必要なため `EnemySystem` のリング命中処理で扱う。
- `cast_fire`, `cast_ice`, `cast_thunder`, `cast_wind`, `cast_earth`: `MagicSystem` へ属性魔法発動を依頼する。`value` は魔法レベルまたは威力として扱い、0以下は1に補正する。

未登録の効果コード、空DB、不正な効果ID、今回対象外の効果はクラッシュさせず無視します。`EffectContext::logUnimplementedEffects` が有効な呼び出しでは、未実装effectをデバッグログに出します。

Projectileの防御と反射は、敵弾とリングアイテムの衝突地点で判定するため `EffectDispatcher` handler ではなく `ProjectileSystem` 側で処理します。

探知効果を持つリングアイテムは、半径円ではなく小さいふきだしバッジを表示します。`detect_hidden` は青、`detect_treasure` は黄の円と三角形を組み合わせた塗りつぶしで表示し、中央に白いイタリックのビックリマークを描きます。

## MagicSystem

`cast_*` 系効果コードは `EffectDispatcher` から `MagicSystem` へ渡します。通常使用ではプレイヤー前方、リング装備中は発動したリングアイテム位置を起点に、アイテムごとの魔法クールダウンに従って自動発動します。リング命中や掘削接触でも同じ起点処理を使います。リングアイテムから発動した場合、そのアイテムは短時間だけ属性をまとい、接触ダメージのダメージ種別もその属性として扱います。

実装済みの暫定挙動:

- `cast_fire`: 高さを持つ火の玉を外向きに放つ。火の玉は壁を貫通し、放物線状に落下する。敵に当たるか地面に落ちると約1秒の炎を残し、範囲内の敵へ火属性ダメージを与える。
- `cast_ice`: 尖った氷弾を3発放つ。高さは一定で、壁に当たると消える。鈍足は付与せず、命中時は氷属性ダメージのみを与える。
- `cast_thunder`: 近距離の敵1体へ自動命中する雷を発生させる。火・氷より発動クールダウンを長くし、命中時に確率で麻痺を付与する。
- `cast_wind`: 扇形ブーメラン風の風波を外向きに放つ。壁を貫通し、命中時に風属性ダメージとノックバックを与える。
- `cast_earth`: 前方の地面から尖った岩を突き出し、命中時に土属性ダメージとスタンを与える。同時に高さを持つ土塊を複数飛ばし、土塊の接触でも土属性ダメージを与える。

火球、氷弾、雷撃、風波、岩、土塊、着弾後の炎、属性まといは一時的な照明を発します。照明は `MagicSystem` の生存中魔法から毎フレーム `LightSource` として集約し、魔法本体や着弾効果の寿命が切れると同時に消えます。火球着弾後の炎はダメージ判定とは別に短い残光を持ちます。火球着弾後の炎と氷弾の砕け光源は発生直後に少し広がり、その後0まで縮むイージングで消えます。風波の光源は小さめにし、寿命に合わせて1回だけイージングで縮小消滅します。

## MagicFxSystem

魔法の見た目は `MagicSystem` の当たり判定・ダメージ処理から分離し、専用の `MagicFxSystem` で段階的に高品質化します。`MagicFxSystem` は完成魔法そのものではなく、炎、氷、雷、風、岩などの演出を構成する汎用Particle/Emitter基盤です。今後は `fire_aura`、`fireball_loop`、`fire_ground_burn` などの魔法クリップを、この基盤の組み合わせとして作ります。

実装済みの基盤:

- `MagicFxParticleShape`: `SoftCircle`, `Circle`, `Ring`, `SparkLine`, `Shard`, `Crystal`, `WindCrescent`, `WindSparkle`, `EarthSpike`, `EarthCrack`, `DirtClod` を持つ。メラメラ、煙、冷気、火花、氷片、岩片、三日月状の風、風の小さな反射光、岩柱、地割れ、土塊などの低レイヤー表現に使う。
- `MagicFxSpawnShape`: `Point`, `Circle`, `Ring`, `Line`, `Cone` を持つ。点発生、円形燃焼、リング状拡散、線状の風、扇形放出を共通化する。
- `MagicFxEmitterConfig`: 位置、方向、色、寿命、サイズ、速度、拡散角、発生範囲、重力、ドラッグ、バースト数、継続発生レートをまとめて指定する。
- `MagicFxSystem::emitBurst()`: 着弾、破裂、砕け、土煙など一瞬の粒子群を出す。
- `MagicFxSystem::addEmitter()`: 燃焼、帯電、冷気、風の流れなど継続粒子を出す。ハンドルで位置更新と停止ができる。
- Particleは擬似高さ、垂直速度、重力を持ち、土塊や火花が跳ねる表現に使える。
- Particleは深度ソート描画へ追加され、地形、敵、リングアイテムと同じワールド描画順に混ぜられる。

火属性クリップ:

- `fire_aura`: 火属性をまとったリングアイテムに追従する継続Emitter。外炎、明るい芯、細い火花、薄い煙を親子Emitterで重ね、細かい粒子の揺らぎで燃焼感を表現する。
- `fireball_loop`: 火球本体に追従する継続Emitter。高密度の明るい芯、外側の赤橙炎、後方へ流れる火花、短い煙の尾を重ね、飛翔中のメラメラと速度感を表現する。
- `fire_ground_burn`: 火球が敵や地面に着弾した地点で再生する複合クリップ。初期の火花リング、拡散フラッシュ、小粒子の炎、芯の炎、跳ねる火花、薄い煙を組み合わせ、約1秒間燃え続ける。

氷属性クリップ:

- `ice_aura`: 氷属性をまとったリングアイテムに追従する継続Emitter。外周の小さな氷片、薄い冷気ミスト、短寿命の白いキラキラ線を重ね、硬質で冷たい輝きを表現する。
- `ice_shard_loop`: 氷弾本体に追従する継続Emitter。進行方向へ揃えたゴツゴツしたひし形結晶、細かな欠け氷、後方へ流れる冷気線、粉雪、短いグリントを重ね、大きめで硬質な氷弾として見せる。
- `ice_shatter`: 壁や敵に命中した地点で再生する大きめのバースト。大きな氷片、小さな氷片、白青いリング状フラッシュ、キラキラの反射線、冷気ミストを組み合わせ、破片と冷気が少し長めに残る。照明は一度少し広がってから、粒子の余韻に合わせてゆっくり縮小消滅する。

雷属性クリップ:

- `thunder_aura`: 雷属性をまとったリングアイテムに追従する継続Emitter。短寿命の火花線をリング状に出し、帯電感を表現する。
- `thunder_strike`: 敵に命中したかどうかに関わらず、発動地点または命中点へ上空から雷を落とす。雷本体は分岐しない一本の折れ点列を持つ専用短寿命クリップで、上から下へ進みながらランダムに折れ曲がる。太い外側光、黄色い中間光、白い芯線を重ね、太さを大きく変える。折れ角は丸い線ではなく尖った多角形で描き、通常の魔法FXと同じ深度描画順で表示する。落雷地点には、不規則な地面放電、上へ跳ねる細い放電、欠けた魔法円のような薄い電磁アーク、火花、上昇しながらゆっくり消える発光粒子、イージングで縮小する照明を出す。発光粒子は透明度、寿命、XY方向の微移動を粒子ごとにランダム化する。地面放電と縦放電は強く折れ曲がった点列を短時間表示し、点列自体は震わせずに徐々に消える。丸いリング状パルスは使わず、柔らかい発光粒子で着弾点の明るさを補う。照明は発光粒子の余韻に合わせて長めに残り、最後はイージングで消える。命中時は強め、敵がいない時も発動地点へ少し弱めの落雷を出す。
- `thunder_bolt`: 旧互換用。内部では強い `thunder_strike` を再生する。
- `thunder_spark_burst`: 命中点で火花を放射状に散らす。麻痺付与の手触りを補強する。

風属性クリップ:

- `wind_aura`: 風属性をまとったリングアイテムに追従する継続Emitter。小さな三日月状の風、細かいミスト、短寿命の小さな反射光を重ね、まとわりつく風を表現する。
- `wind_wave_loop`: 風波本体に追従する継続Emitter。外円弧と内円弧から作る滑らかな三日月状メッシュを主形状にし、本体は進行方向へ安定させる。直線状の流線は使わず、細かいミストと三日月の縁に乗る小さな反射光で風の圧力、速度感、輝きを補強する。風波は時間で消滅し、終盤は減速しながらEmitterを止め、残った粒子がフェードアウトする。
- `wind_impact`: 命中点から進行方向へ三日月状の風片、弱い圧力波、細かいミスト、小さな反射光を広げ、ノックバックの衝撃風を表現する。

土属性クリップ:

- `earth_aura`: 土属性をまとったリングアイテムに追従する継続Emitter。小さなゴツゴツした石を周囲に浮かせ、細かな砂粒を落として土属性のまといを表現する。
- `earth_spike_rise`: 岩の突き出し時に専用の削れた岩柱ポリゴン、地面側に固定表示する不規則な割れ目、重い土煙、細かい砂煙、飛び散る岩片、控えめな魔法の光粒子とキラキラを重ねる。光粒子は短時間の継続Emitterで多めに出し、発生開始タイミングを散らし、ゆっくり滞在してから消える。岩柱本体の照明は控えめにし、従来の半分程度の半径に抑える。割れ目は岩柱より奥に描き、移動・揺れ・サイズ変化をさせない。割れ線は生成時の `startSize` だけを基準にした固定折れ点列で描き、フェードインせず完成形で出す。ヒビ本数は少なめにし、角度セクターごとに起点を散らしてヒビ同士の重なりを抑える。主線と枝はランダムな折れ点、太さの強弱、大きめの折れ角を持たせてギザギザさせ、枝からさらに細い枝が分岐するパターンも混ぜる。左右交互の規則的なジグザグにはせず、折れ点間隔と横ずれ方向をランダム化し、同方向へ続く折れも混ぜる。ヒビ全体は小さめ・細めにし、ヒビ同士はバラバラに消さず、岩柱本体と同じ寿命・同じフェードアウトタイミングで一括フェードアウトする。岩柱は左右対応点を使わず、曲がった中心線、左右別々の輪郭、欠けた先端、不均一な稜線で非対称に描く。下部は大きな1枚面にせず、根元輪郭を欠けさせ、複数の小面に割って平面感を避ける。独立した楕円影は使わない。メイン岩柱は1つの固定粒子として描き、ブルブル震えさせない。岩柱は少し長く残し、最後だけ短くフェードアウトする。
- `dirt_clod_loop`: 土塊本体に追従する継続Emitter。ゴツゴツした土塊本体、回転する小石、放物線飛翔中の砂煙を重ねる。土塊は発射角、高さ、速度、半径、重力、落ちるタイミングをランダム化する。飛距離は周囲に落ちる程度へ抑え、半径のランダム上限を広めに取る。
- `earth_debris_burst`: 土塊命中時または着地時に、ブロック破壊を小さくしたような岩片、細かな欠片、砂煙を散らす。破壊エフェクトの余韻に合わせて、土属性の照明をイージングで縮小して消す。

`MagicSystem` の円、線、ポリゴンによる仮描画は削除し、見た目は `MagicFxSystem` のParticle/Emitterへ寄せます。`MagicSystem` は発動、当たり判定、ダメージ、状態異常、寿命管理、照明用 `LightSource` の列挙を担当します。

## EntityStatus

状態異常は `status_*` 系の効果コードからのみ付与します。`poison`、`slow` などの特殊タグだけでは状態異常を発動しません。

状態異常の視覚表現は `EntityStatusVisuals` に集約し、プレイヤーと敵で同じ状態IDから同じ基本表示を使います。状態異常中は、色変更、オーラ、頭上表示、サイズ変更、上下反転、揺れのいずれかを必ず出します。複数状態が重なった場合、サイズ・反転・揺れ・頭上表示は合成し、色変更は優先度の高い状態を1つ採用します。`status_confuse` は色変更せず、頭上に黄色い星2つを回転表示します。`status_frozen` は氷色の色変更に加えて、細い氷片がきらめく粒子を出します。

状態異常を新規付与したときは、対象の頭上に状態名のワールド座標フローティングテキストを表示します。表示名と色は `EntityStatusVisuals` の状態表示メタ情報で一元管理します。プレイヤーへ付与された状態名は赤文字、敵へ付与された状態名は状態ごとのテーマ色で表示します。継続地形や連続ヒットによる再付与で表示が連打されないよう、すでに同じ状態が残っている間の更新では原則として状態名を再表示しません。

実装済み効果コード:

- `status_poison`: `value` を毒の秒間ダメージとして扱う。`value <= 0` の場合は1DPS。`duration > 0` はその秒数、`duration = 0` はデフォルト8秒。
- `status_slow`: `value` を移動速度倍率として扱う。`value <= 0` の場合は0.65倍。`duration > 0` はその秒数、`duration = 0` はデフォルト8秒。
- `status_bleed`: `value` を出血の秒間ダメージとして扱う。`value <= 0` の場合は1DPS。停止中は0.5倍、移動中は1.5倍で小数ダメージを蓄積する。`duration = 0` はデフォルト8秒。
- `status_paralyze`: 移動と敵の攻撃行動を停止する。麻痺中は描画位置を左右1pxに揺らす。`duration = 0` はデフォルト1.5秒。
- `status_sleep`: 移動と敵の攻撃行動を停止する。時間経過またはダメージを受けると解除される。`duration = 0` はデフォルト4秒。
- `status_stun`: 移動と敵の攻撃行動を短時間停止する。スタン中は上下反転表示し、解除時に短い飛び起き表示を行う。`duration = 0` はデフォルト0.6秒。
- `status_confuse`: 敵AI専用の混乱。移動は停止せず、0.25〜0.65秒ごとにランダムな方向へふらつく。混乱中は射撃、接触攻撃、ジャンプ攻撃、カウントダウン爆発を行わず、壁抜け/掘り進み移動も行わない。移動速度は0.75倍。表示は色変更ではなく、頭上の黄色い星2つの回転で示す。`duration = 0` はデフォルト3秒。
- `status_giant`: `value` を巨大化強度として扱い、`1 + value * 0.5` をサイズ倍率にする。倍率は1.0から2.0に丸める。`duration = 0` はデフォルト8秒。

未実装ステート仕様:

以下はObjects DB / 効果コード一覧に先行定義してよいが、現時点では未実装として扱う。実装するまでは未登録効果コードと同じくクラッシュさせず無視し、必要に応じて未実装ログへ出す。いずれも特殊タグから直接発動せず、`status_*` または条件効果コードを `EffectDispatcher` へ渡した時だけ付与する。

- `status_blind`: 盲目。攻撃命中率低下、狙いブレ、視界低下を表す状態。テレビ、ゲーム機などの「画面・光・ノイズで敵の狙いを狂わせる」宝で使う。`value` は命中率倍率または視界倍率、`duration` は秒数。
- `status_wet`: 水濡れ。単体では原則ダメージを持たず、感電、凍結、熱風、炎消しなどの相互作用の起点にする。洗濯機、バケツ、`water_spray` から付与する。`value` は濡れ強度、`duration` は秒数。
- `status_hot`: 熱々。継続ダメージを与え、熱々状態のキャラに触れた別キャラにも接触ダメージを与える。電子レンジなどで使う。水濡れや水系効果での軽減・解除は実装時に調整する。
- `status_frozen`: 凍結。移動と攻撃行動を停止する。基本時間は2〜3秒程度とし、炎ダメージまたは炎系効果を受けた場合は即解除する。表示は氷色の色変更と、周囲のきらめく氷片粒子で示す。`cold_air_aura` や氷系宝の蓄積・命中から付与する。
- `status_shocked`: 感電。継続ダメージ、移動停止、ビリビリ演出を持つ。感電状態のキャラに触れた別キャラにも感電を伝播する。`shock_wet` は水濡れ状態の対象にだけ `status_shocked` を付与する条件効果として扱う。
- `status_glued`: 接着。接着剤による移動低下を表す。既存の `status_slow` とは別ステートとして扱い、接着剤のビンなどで使う。`value` は移動速度倍率、`duration` は秒数。

ステートにしないもの:

- 酔い専用ステートは作らず、酒瓶などは `status_confuse` / `status_confuse_chance` で表現する。
- 跳ね上げは `bounce_grounded` などの制御効果として扱い、EntityStatusにはしない。飛行敵には無効とする。
- 匂いはステートにせず、腐った肉などの `spawn_bias_maggot` のようなリング装備中の出現補正で扱う。

同じstateが既にある場合は `KeepLonger` で長い残り時間を採用します。敵とプレイヤーは毎フレーム `EntityStatus::update()` を行い、毒と出血は小数ダメージを蓄積してHPへ反映します。敵の毒状態は緑、鈍足状態は青寄りの色で描画し、出血は赤系の状態オーラ、麻痺・スタンは動きと姿勢の演出で見分けます。

## 軌道補正基盤

軌道補正は `EffectSpec` の `target=orbit` からだけ集約します。リング上アイテムの軌道効果再集約時に `OrbitModifiers` を作り直し、装着中Objects DBアイテムの `orbitEffects` を `EffectDispatcher` 経由で反映します。

- `orbit_speed`: `OrbitModifiers::speedMultiplier` に乗算集約し、`SpellRingSystem::effectiveAngularSpeed()` に反映する。
- `orbit_power`: `OrbitModifiers::powerMultiplier` に乗算集約し、リングアイテム接触ダメージと接触掘削ダメージへ反映する。
- `orbit_gravity` / `orbit_antigravity`: `OrbitModifiers::gravityMultiplier` / `antigravityMultiplier` に乗算集約し、リング軌道半径へ反映する。
- `orbit_anchor`: `OrbitModifiers::anchorStrength` に加算集約し、リングずらし入力中のリング中心追従を粘らせる。追従遅延には上限距離を設け、操作不能になるほど離れないようにする。
- `orbit_shift`: `OrbitModifiers::shiftMultiplier` に乗算集約し、`Player::spellRingShift` の目標距離へ反映する。
- `damage_speed`: `OrbitModifiers::speedDamageMultiplier` に乗算集約し、リング上の全アイテムが敵へ接触した時の速度ダメージボーナスへ追加倍率として使う。
- `slash_power`: `target=item` 専用の斬撃接触ダメージ倍率として扱う。属性ダメージ種別を持つ blade系武器でも「属性＋斬撃」を表現できるよう、攻撃力側の接触ダメージへ乗算し、速度ダメージボーナスには掛けない。複数ある場合は乗算し、実際に敵へ接触ダメージを与えた時に効果判明する。
- `item_orbit_offset`: `target=item` 専用の個別軌道距離補正として扱う。値はpxで、そのアイテムだけリング中心から見た外向き方向へ加算移動する。補正後の位置は描画、敵接触、掘削、Projectile防御に使い、補正後の速度から `damage_speed` 用の速度ダメージも計算する。リング画面の配置可否判定には混ぜず、複数ある場合は加算する。装備中に常時有効な効果として効果判明する。
- `critical_chance`: リングアイテムが敵へ接触した時の会心率として扱う。`target=item` はそのアイテムだけ、`target=orbit` は装着中リング効果として全リングアイテムへ加算する。値は%で、最終会心率は0〜100%に丸める。
- `critical_power`: 会心時のダメージ倍率として扱う。複数ある場合は最大値を採用し、未指定時の会心倍率は2.0倍。会心倍率は速度ダメージボーナス込みの接触ダメージへ掛け、その後に敵の耐性、防御、正面ガードを適用する。
- 会心表示: 敵接触ダメージの `EnemyEvent` に会心フラグを渡し、`DamagePopupStyle::Critical` で通常より大きいイタリック表示にする。表示文字列は `123!` のように `!` を付け、出現直後は赤くフラッシュしてから白へ戻る。
- `nonlethal_hit`: `target=enemy` / `target` のアイテム接触効果として扱う。通常の接触ダメージ計算、会心、睡眠特効、敵耐性、防御、正面ガードをすべて反映した後、実際にHPへ適用するダメージだけを `enemy.hp - 1` 以下に丸める。ダメージ表示とHPバーは丸めた後の実ダメージを使う。
- `regen`: `target=player` の継続回復効果として扱う。値はHP/秒で、装着中の未破損アイテムから毎フレーム加算集約し、合計上限は0.5 HP/秒。HP満タン時は蓄積せず、蓄積が1以上になった時に `Player::heal()` で整数回復する。実際にHPが回復した時に効果判明する。

リング速度は、リングごとの基本速度へ杖装備の `ring_speed_mul` を掛け、その後に装着重量補正をかけ、さらに `orbit_speed` などの軌道効果倍率を別倍率としてかけます。リング画面の速度、重量、重量補正、攻撃力、掘削力、耐久力は実戦で使う値と同じ値を表示します。

## みねうちの剣

`みねうちの剣` は `Objects` の `item_mineuchi_sword` として定義します。カテゴリは `武器`、`通常効果=none`、`軌道効果=enemy:nonlethal_hit:1:0`、`攻撃力=8`、`ダメージ種別=blunt`、`掘削力=1`、`耐久力=70`、`重さ=1.3`、`画像番号=192`、`特殊タグ=weapon,metal,blade,medium` を基本値とします。

剣の先が峰になっている斬新な剣として、鉄の剣より低火力の打撃武器にします。敵に小ダメージを与えますが、とどめは刺さず、敵HPを1まで減らして捕獲やイベント条件へつなげる用途を持ちます。

## 虫とりアミ（Capture Net）

虫とりアミはObjects DB由来の探索アイテムです。左クリックやゲームパッドボタンで直接使う常備アクションではなく、アイテム画面からリングへ乗せて使います。`通常効果` は `none` とし、通常使用では発動しません。

実装済みの虫とりアミ系アイテムは、`虫とりアミ`、`スーパー虫とりアミ`、`ハイパー虫とりアミ` の3種類です。いずれも `軌道効果=enemy:capture_net:<捕獲率倍率>:<再判定秒数>` を持ち、リング上のアミが敵に接触した時だけ捕獲判定を行います。

`capture_net` の `value` は捕獲率倍率です。`1.0` は標準、`1.25` は25%上昇、`1.60` は60%上昇として扱います。`duration` は同じアミが次に捕獲判定できるまでの秒数です。`duration=0` の場合は既定値0.75秒です。

判定を行った時点で、成功/失敗/ボス制限に関わらず、そのアミの耐久を1消費します。耐久が0になったアミは通常のリングアイテム破損処理へ入り、保護OFFならリングから消滅し、保護ONなら壊れた状態で残ります。

捕獲対象は、リング上のアミの接触円と敵の実効半径が重なった敵です。プレイヤーから対象までの距離は判定しません。リング接触自体を射程として扱います。

捕獲成功時は、対象敵を即座に敵AI/当たり判定から外し、敵スプライトのスナップショットを使った吸収演出へ移します。演出では捕獲地点で発光し、敵が白いシルエットになり、短い溜めの後にプレイヤー位置へ高速移動して収納されます。敵画像の描画は既存の `EnemyImageRenderer` を使い、白シルエットはマスクオーバーレイと発光リングで表現します。

虫とりアミのチュートリアルは `tutorial:capture_net` の StoryEvent として定義します。虫とりアミ系アイテムをワールドドロップから入手した時に一度だけキューします。内容は、虫とりアミをリングに乗せること、リング上のアミが敵に当たると捕獲を試みること、捕まえた敵をリングに乗せられること、リング投げが `C` キーであることを案内します。初回導入ダンジョンでは発火しません。

捕獲失敗とボス制限の通知は、アイテム入手と同じダンジョンログ表示へ流します。捕獲成功ログと初回入手通知は吸収演出の収納完了後に出します。収納完了時にリュックがいっぱいなら、捕獲アイテムはプレイヤー足元付近へドロップアイテムとして落ち、空きができた後に通常のドロップ拾得処理で入手できます。左上のホットリロード/デバッグ通知用 `reloadNotice_` には出しません。

`C` キーはアクティブリング投げに使います。旧仕様の `C` キー捕獲、左クリック捕獲、ゲームパッドWestボタン捕獲は使いません。

宝箱、木箱、報酬ノード、ワープ、スイッチ、地形ギミックなどを捕まえる仕様は将来拡張枠です。世界観上は「なんでも捕まえられる」余地を残しますが、Phase 1の実装には混ぜません。

捕獲確率は暫定で以下を使います。

```text
chance = clamp(0.15 + (1.0 - hpRate) * 0.75 - captureDifficulty * 0.04, 0.05, 0.90)
```

`hpRate` は現在HP / 最大HP、`captureDifficulty` は `Enemies` タブの `捕獲難度` です。HPが低いほど捕獲しやすく、捕獲難度が高いほど捕獲しにくくなります。虫とりアミ系アイテムでは、この基礎確率へ `capture_net` の `value` を掛け、最終確率を0〜0.95へ丸めます。成功時は対象敵を非アクティブ化して吸収演出へ渡し、失敗時は該当アミの再判定秒数が経過するまで同じアミでは再判定しません。

捕獲成功時は `EnemyDefinition` から `ObjectDefinition` 相当のランタイムアイテムを作ります。IDは `captured_` + 敵ID、名前は `Enemies` の `名前` をそのまま使い、捕獲後も変更しません。カテゴリは内部的に `軌道`、レア度は1、価格は0です。捕獲敵アイテムは `ItemVisualRef` を `Enemy` 参照として持ち、`Enemies` の `画像番号` から `assets/enemies/en_番号.png` を使ってインベントリ、ショートカットHUD、リング上で敵グラフィックを表示します。起動時に `Enemies` から生成する `captured_敵ID` のカタログ定義にも同じ `ItemVisualRef` を設定し、セーブデータから復元した捕獲敵アイテムでも敵グラフィックを再表示します。ダンジョン/ワールド上のリング表示では、捕獲敵アイテムはリング中心から見た外側方向へ4方向スプライトを自動で切り替え、常時歩行アニメーションします。リング画面、インベントリ、ショートカットHUDなどのUI表示では静止アイコンとして扱います。

捕獲敵アイテムへの列対応は、`捕獲後説明文` を説明文、`捕獲後通常効果` を通常効果、`捕獲後軌道効果` を軌道効果、`捕獲後攻撃力` を攻撃力、`捕獲後ダメージ種別` をダメージ種別、`捕獲後掘削力` を掘削力、`捕獲後耐久力` を耐久力、`捕獲後重さ` を重さ、`捕獲後特殊タグ` を特殊タグ、`捕獲後効果テキスト` を効果テキスト、`捕獲後挙動ID` を保持用IDとして使います。

捕獲敵アイテムは既存Objects由来アイテムと同じインベントリ経路で保持し、リングへ装着できます。リング装着時は捕獲後攻撃力、ダメージ種別、掘削力、耐久力、重さ、軌道効果、特殊タグを既存Objectパラメータと同じように反映します。捕獲後挙動IDは `SpellRingItem` に保持し、実処理は捕獲後挙動IDまたは `EffectSpec` 経由で行います。特殊タグから直接効果は発動しません。

## Captured Behavior IDs

捕獲後挙動IDは、発射物を使わない基本挙動、`ProjectileSystem` を使う発射系、報酬生成・盗み・採掘補助系、爆発・磁力・風逸らしを最小実装しています。

- `contact_slow`: 敵接触時、捕獲後軌道効果に `status_slow` がない場合は `enemy:status_slow:0.85:1.5` 相当の `EffectSpec` を生成して敵へ鈍足を付与します。
- `jump_outward`: 約2秒ごとに短時間だけリング半径外側へ跳ね、接触判定位置を外側へ広げます。
- `outward_guard`: リング中心から見た外側方向を向くガードとして扱い、その方向から来た敵Projectileを消します。反射はまだ行いません。
- `heavy_guard`: physical系または小型Projectileを広めの判定で消し、敵接触時は耐久を消費せず短いノックバックを与えます。
- `periodic_heal`: 約8秒ごとにプレイヤーを1回復します。複数装着時は回復速度を少しだけ上げ、強くなりすぎないよう上限を設けます。
- `dig_contact`: 捕獲後掘削力を使い、リング接触位置と外側の追加位置へ地形掘削判定を出します。捕獲後採掘専用として使い、`terrain_dig` などの新規挙動IDは増やしません。
- `magic_guard`: magic系Projectileを広めの判定で消します。Objects効果コードの `reflect_magic` 系反射とは別枠の捕獲後防御挙動として扱います。
- `rust_debuff`: 敵接触時、捕獲後軌道効果に `debuff_defense` がない場合は `enemy:debuff_defense:0.8:4` 相当の `EffectSpec` を生成して敵の防御倍率を下げます。
- `shoot_web`: リングアイテム位置から外側方向へ `web_thread` を発射します。敵命中時は `BehaviorDefinition::capturedDefaultEffects` を適用し、定義に鈍足がない場合も確定鈍足を付与します。
- `throw_stone`: リングアイテム位置から外側方向へ `stone_bullet` を発射します。
- `shoot_poison`: リングアイテム位置から外側方向へ `poison_spit` を発射します。
- `radial_spike`: リングアイテム位置から8方向へ `cactus_needle` を発射します。
- `shoot_water`: リングアイテム位置から外側方向へ `water_shot` を発射します。
- `shoot_fire`: リングアイテム位置から外側方向へ短射程の `fire_breath` を発射します。
- `reward_drop`: 敵または壁に当たった時、低確率で既存ワールドドロップ候補から報酬を生成します。
- `steal_or_dig`: 敵に当たると低確率でアイテムを盗み、壁・鉱石に当たると低確率でアイテムを掘り出します。実際の地形ダメージは捕獲後掘削力または軌道効果側に任せます。
- `charge_explode`: 敵または地形への接触を4回蓄積すると小爆発を起こし、約2.4秒休眠してから再蓄積します。爆発は専用爆発エフェクトと `se.explosion.boom` を使い、半径内の敵、プレイヤー、リング上アイテムを巻き込みます。敵とプレイヤーは小ダメージを受け、爆心から外側へノックバックします。リング上アイテムは同じ値だけ耐久を消費しますが、ノックバックはしません。地形には半径約28px・ダメージ1の小さめ地形破壊を行い、プレイヤーへ即死級ダメージは与えません。
- `magnet_pull`: `metal` タグを持つワールドドロップ、敵、Projectileだけをゆるく吸い寄せます。距離は約160-170px、対象数はドロップ6、敵4、Projectile6までです。`metal` タグだけでダメージや状態異常は発動しません。
- `wind_deflect`: 約1.2秒ごとに風波を出し、近くの敵Projectileを最大5発まで少し横方向へ逸らします。プレイヤー操作、リング軌道、味方側Projectileは乱しません。敵弾を完全無効化する処理ではありません。

発射方向は原則としてリング中心から見た外側方向です。`捕獲後向き制御=outward` の定義はこの外側方向処理として扱います。`radial_spike` だけは周囲8方向へ撃ちます。

発射間隔は `挙動ID一覧` の `既定間隔秒` を使います。ただし強すぎ防止として通常発射は最低0.75秒、`radial_spike` は最低1.5秒に丸めます。PlayerOrbit弾は同時48発までを上限とし、`radial_spike` は1回8発までです。上限に達した場合は短い再試行待ちを入れます。

Projectile命中時は、該当 `BehaviorDefinition::capturedDefaultEffects` を `EffectDispatcher` へ渡します。未知の捕獲後挙動IDは、安全に保持するだけです。反射の本格処理、専用アセット、発射物DB化などはまだ実装しません。

報酬生成は `WorldDropSystem` の既存Objects DBドロップ候補を再利用します。候補は `掘削`、`探索`、`回復`、`武器`、`盾`、`宝` カテゴリとドロップ系タグを基準に選び、`money` や `treasure` などの特殊タグだけで効果は発動しません。WorldDrop自体はObjectsアイテム、お金、素材4種を保持できます。お金は拾得時に所持金へ加算し、素材は `MaterialInventory` へ加算します。素材は通常アイテム枠を消費しません。DBが空、候補不足、ID不備の場合は何も生成せず警告ログだけを出します。

報酬生成には増殖防止の上限を入れます。各リングアイテムは最短0.8秒ごと、10秒あたり最大3個までです。ボスからの盗み・報酬生成は同じリングアイテムにつき最大2回までに制限します。

## Ranged Enemy Behaviors

`BehaviorDefinition` の `既定間隔秒` と `既定発射物ID` は、遠距離系の敵挙動IDに接続済みです。対応済みIDは `throw_stone`, `shoot_poison`, `shoot_web`, `shoot_fire`, `radial_spike`, `shoot_water`, `wind_blow` です。各敵はDB上の挙動IDから最初に見つかった遠距離挙動を使い、指定間隔ごとに発射します。

`throw_stone`, `shoot_poison`, `shoot_web`, `shoot_fire`, `shoot_water`, `wind_blow` はプレイヤー方向へ発射します。`radial_spike` は周囲8方向へ `cactus_needle` を発射します。`wind_blow` の `wind_wave` は命中時にプレイヤーを少し押すだけで、操作不能にはしません。

Projectileがプレイヤーへ命中した場合、Projectileに紐づく `BehaviorDefinition::enemyDefaultEffects` を `EffectDispatcher` へ渡します。現在はProjectile側の発射物DBはなく、既定発射物IDと効果は挙動ID一覧から参照します。

`EffectDispatcher` は chance 系の最小ハンドラとして `status_poison_chance`, `status_slow_chance`, `status_paralyze_chance`, `status_bleed_chance`, `status_sleep_chance`, `status_stun_chance`, `status_confuse_chance` に対応します。`value` は0から100の確率パーセントとして扱い、成功時に対応する `status_*` を付与します。未対応stateや未対応効果はクラッシュさせず無視します。

特殊タグ単体では状態異常、ガード、属性効果を発動しません。`poison` や `shield` などは分類・条件判定に使い、材質/形状/質感タグは演出SEルーティングにも使います。捕獲後アイテム化と捕獲後発射系Projectileは最小実装済みです。

## ProjectileSystem

敵弾・捕獲後弾の共通基盤として `Projectile` / `ProjectileSystem` を追加しています。発射物は `ObjectPool<Projectile, balance::MaxProjectiles>` の固定容量プールで管理し、毎フレーム `new/delete` しません。

`Projectile` は `active`, `position`, `velocity`, `radius`, `lifetime`, `ownerType`, `projectileId`, `damage`, `damageType`, `effects`, `tags` を保持します。`ownerType` は現時点では `enemy` と `player_orbit` 相当の区分です。

発射物DBタブはまだ作らず、`projectileId` ごとの仮パラメータはコード内テーブルで管理します。対応IDは `stone_bullet`, `big_stone_bullet`, `weapon_throw`, `poison_spit`, `paralyze_shot`, `mud_blob`, `cactus_needle`, `water_shot`, `fire_breath`, `web_thread`, `wind_wave`, `explosion_small`, `junk_chunk` です。

`ProjectileSystem` は毎フレーム位置更新、寿命切れ消滅、壁衝突消滅、敵Projectileのプレイヤー衝突ダメージ、PlayerOrbit Projectileの敵衝突ダメージを行います。敵Projectileがリングアイテムへ当たった時は、そのアイテムの `projectile` 効果コードを見て防御・反射を判定します。特殊タグだけでは防御効果を発動しません。

- `guard`: 小型/通常弾を防ぐ。針、水弾、糸、毒弾、小石、泥弾など、重い弾扱いではないProjectileが対象です。
- `guard_large`: 大型/重量弾を防ぐ。半径6以上、ダメージ2以上、`large` / `heavy` タグ、`big_stone_bullet`、`explosion_small` などが対象です。
- `guard` と `guard_large` の両方を持つアイテムは、小型/通常弾と大型/重量弾の両方を防ぎます。
- `guard` / `guard_large` の `value` は防御範囲の補正として扱います。1を標準値とし、1より大きいほどリングアイテム周囲の受け止め判定が少し広がります。
- `reflect_physical`, `reflect_magic`, `reflect_water` と各 `_chance` は、防御できたProjectileまたは反射効果を持つアイテムにProjectileが直接重なった時に判定します。反射確率が外れ、かつ `guard` / `guard_large` で防げない場合、そのProjectileは消えません。`reflect_magic` は `magic`, `fire`, `ice`, `thunder`, `wind`, `earth`, `water` を返す元素/魔法弾反射として扱い、`reflect_water` は水弾だけを返す下位互換の専用反射として残します。同じアイテムが `reflect_water` と `reflect_magic` の両方を持つ場合、水弾では水専用反射の発見・判定を優先します。

捕獲後挙動IDの `outward_guard`, `heavy_guard`, `magic_guard` は追加の消弾条件として扱います。属性相殺はまだ行いません。

敵からの発射処理は最小テスト用途です。`挙動ID一覧` の `既定発射物ID` を持つ敵が低頻度で対象方向へ撃つだけで、遠距離敵の本格AI、弾幕制御、発射物DB化は次段階です。捕獲後発射系はPlayerOrbit Projectileとして扱い、敵へ命中します。

## Enemy AI and behavior IDs

`Enemies` DB の `未発見AI` と `敵AI` は `EnemySystem` の移動AIへ接続済みです。
`Enemies` タブには `未発見AI`, `視野距離`, `視野角`, `見失い秒数` の4列と、新敵 `drill_hermit`, `mana_leech`, `uji_uji` が追加済みです。

新敵3体の仕様:

- `drill_hermit` / 削岩ヤドカリ
  `未発見AI=dig_wander`, `敵AI=dig_chase`, `敵挙動ID=dig_move`, `捕獲後挙動ID=dig_contact`, `視野距離=180`, `視野角=120`, `見失い秒数=3.0`
- `mana_leech` / 魔力ヒル
  `未発見AI=wander`, `敵AI=chase`, `敵挙動ID=ring_slow_bite`, `視野距離=150`, `視野角=110`, `見失い秒数=3.5`
- `uji_uji` / うじウジ
  `未発見AI=buried`, `敵AI=chase`, `敵挙動ID=swarm_alert`, `視野距離=140`, `視野角=240`, `見失い秒数=4.0`

未発見AIの対応キー:

- `idle`: その場待機
- `wander`: 緩い徘徊
- `patrol`: 一定範囲の直線寄り巡回
- `buried`: 潜伏待機
- `dig_wander`: 壁優先で掘り進む徘徊
- `item_seek`: 近傍ドロップ追跡、未検出時は徘徊
- `phase_wander`: 壁衝突を受けない浮遊徘徊

発見後AIの対応キー:

- `chase`, `jump_chase`, `keep_distance`, `hover_chase`, `hover_keep_distance`, `stationary`, `flee`
- `shield_chase`: 遅め旋回で正面を保ちながら接近
- `support`: 負傷した味方を優先、未検出時は積極追跡しない
- `dig_chase`: 壁を削りながら追跡
- `phase_chase`: 壁衝突を受けず直進追跡
- `burrow_ambush`: 潜伏系から起動する待ち伏せ追跡
- `ambush`: 待機から視認/近接起動する待ち伏せ

未知の `未発見AI` は警告ログを出し `idle`、未知の `敵AI` は警告ログを出し `chase` へフォールバックします。

## Enemy Awareness System

敵は `unaware` / `detected` の2状態を持ちます。`alert` は未実装です。

- `unaware`: `未発見AI` で行動します。
- `detected`: `敵AI` で行動し、原則プレイヤー方向を向きます。

`unaware` から `detected` への遷移条件は以下です。

- プレイヤー距離 `<= 視野距離`
- 敵の向きとの差 `<= 視野角 / 2`
- 壁で視線が遮られていない

`detected` 中の見失い判定は以下です。

- `発見後視野距離 = 視野距離 × enemy_detected_vision_multiplier`
- `発見後視野角 = 視野角 × enemy_detected_vision_multiplier`
- 上記判定で距離外 / 角度外 / 視線遮蔽のいずれかが続いた時間をカウント
- `見失い秒数` を超えたら `unaware` に戻る

`enemy_detected_vision_multiplier` は `data/game_balance.cfg` の共通設定です。既定値は `1.8` です。

`facingAngle` は敵の向きを保持します。通常移動中は移動方向を向き、`idle` / `stationary` は最後の向きを保持します。`front_guard` 判定は `facingAngle` と整合した正面判定を使います。

ゴースト系の `phase_wander` / `phase_chase` は移動時のみ壁衝突を無視します。視認判定は従来どおり壁で遮蔽し、壁越し視認は行いません。

既存敵の挙動修正:

- トゲスライムはスライム系移動を使い、差分は `spike_contact` の接触性能のみ
- ゴーストは距離維持せず `phase_wander` / `phase_chase` で壁抜け移動
- 火吹きトカゲ、逆風フクロウ、錆ナメクジは高低差や天井概念を使わない地上系処理
- 魔力ヒルは通常 `chase` より低速補正で接近

掘削・地形破壊で敵が生成または露出した場合、その敵は視野判定を待たず即 `detected` になり、発見アイコンを表示します。

発見遷移時は頭上に `!`、見失い遷移時は頭上に `?` を短時間表示します。これはUI演出のみで、ゲーム進行値には影響しません。

今回の除外項目は `光を嫌う敵`, `天井待機`, `高所/低所`, `聴覚`, `警戒状態`, `光源と敵AIの連動`, `壁越し視認`, `発見後視認倍率のEnemies列化`, `debuff_orbit_speed 効果コード追加`, `terrain_dig 新規挙動ID`, `うじウジ死体分裂`, `特殊タグからの直接効果発動` です。

`敵挙動ID` は以下を対応済みとします。

- 基本/耐性/防御: `contact_basic`, `physical_resist`, `magic_body`, `front_guard`, `spike_contact`
- 遠距離: `throw_stone`, `shoot_poison`, `shoot_web`, `shoot_fire`, `radial_spike`, `shoot_water`, `wind_blow`
- 支援/特殊: `enemy_heal`, `countdown_explode`, `dig_move`, `ring_slow_bite`, `swarm_alert`, `steal_item`, `boss_sequence`

`boss_sequence` はボス用の共通アクションシーケンスです。`params` の `pattern` でプリセットを選びます。現在の対応パターンは `stardust_mole`、`junk_crab`、`astragna` です。`stardust_mole` は小ジャンプ潜行、ランダム地中待機、土煙予告、その場大ジャンプ、着地後30f待機、主人公方向への固定突進、柔らかい土の破壊、硬い壁または鉱石壁への衝突硬直を行います。`junk_crab` はガラクタリング、ハサミ、ガラクタ飛ばし、転倒、復帰を行います。`astragna` は専用の回転外殻ブロックと封印パーツ5個を `BossActionRuntime` 内に持ち、封印パーツ全破壊でダウン、ダウン中の外殻掘削、中央守護星への接触救出を行います。ボス弱点は共通の命中円判定で扱い、リングアイテムや反射弾の命中円が弱点円に重なった場合だけ弱点倍率を適用します。

`ring_slow_bite` は EffectSpec を増やさず、特殊タグ発火も使わず、`EnemySystem` から `SpellRingSystem` へ直接適用する専用一時デバフとして実装します。プレイヤー接触時にリング実効速度へ倍率を掛け、同一デバフ再付与時は残り時間を更新します。既定値は `enemy_ring_slow_bite_multiplier=0.65`, `enemy_ring_slow_bite_duration=4.0` です。

`swarm_alert` は発見遷移時に発火し、近距離にいる同一 `enemyId` の未発見個体を `detected` へ遷移させます。遷移した個体には `!` アイコンを表示します。

`steal_item` は金食いネズミ系の敵側盗み挙動です。敵本体が現在のカメラ表示範囲内にいる場合だけ、`radius` 内かつ同じ表示範囲内にあるワールドドロップから `target` に合う最寄り対象を盗みます。`target=money` はお金、`target=money|treasure` はお金と `treasure` タグ付きObjectsドロップを対象にします。素材ドロップは盗み対象外です。盗んだドロップはワールド上から消え、敵の保持ドロップになります。保持上限は `maxCarry` で、盗んだ保持ドロップは撃破時に必ず吐き出します。盗み成功時は右側ダンジョンログへ `敵名は[アイコン]アイテム名を盗んだ` と表示します。

`countdown_explode` は準備開始時から赤く滑らかに点滅し、爆発直前へ向けて点滅間隔を短くします。赤く光るピークでは `se.explosion.tick` を鳴らし、爆発時は専用爆発エフェクトと `se.explosion.boom` を使います。爆風内の敵、プレイヤー、リング上アイテムは敵味方を問わず巻き込まれ、敵とプレイヤーはfire系ダメージを受けて爆心から外側へノックバックします。リング上アイテムは同じ値の耐久ダメージを受けますが、ノックバックはしません。爆発元の敵自身だけは二重ヒットを避けるため敵ダメージ対象から除外します。

`dig_move` は敵側の掘削移動専用です。`dig_wander` と `dig_chase` で壁に進路が塞がれた場合に既存地形掘削処理で掘り進みます。`terrain_dig` などの新規敵挙動IDは追加しません。

未知の敵挙動IDはクラッシュさせず、初回だけデバッグログに出して安全に無視します。`metal`, `shield`, `poison` などの特殊タグは分類や条件判定に使い、材質/形状/質感タグは演出SEルーティングにも使います。特殊タグから直接効果は発動しません。敵側の弾発射は最小実装済みです。捕獲と捕獲後アイテム化も最小実装済みです。

## Enemies DB runtime connection

`EnemyCatalog` で読み込んだ `Enemies` DB は、既存 `EnemySystem` の通常敵生成へ接続済みです。掘削で敵が出るタイミング、敵上限、掘削カウンタは従来の `RuntimeBalance` のまま使い、通常の掘削起因スポーンと敵ノード由来の固定配置スポーンでは、生成地点のステージIDと深度ランクに対応する `ST_E1`〜`AS_E9` の相対重みで敵種を選びます。敵出現重みはボス敵IDを指定するボス生成や、敵IDを明示したイベント敵には接続しません。イベント敵でも敵IDが空でステージIDが指定されている場合は、同じ敵出現重みを使います。

`Enemy` はDB由来の `enemyId`, `EnemyDefinition` ポインタ、`aiId`, `unawareAiId`, `facingAngle`, `awareness`, `loseSightTimer`, `visionDistance`, `visionAngle`, `loseSightSeconds` を保持します。未知の敵定義、空DB、不正値がある場合はクラッシュさせず、既定敵パラメータへフォールバックします。

反映済みの `Enemies` 列は `名前`, `HP`, `接触攻撃力`, `接触ダメージ種別`, `移動速度`, `半径`, `経験値`, `敵AI`, `未発見AI`, `視野距離`, `視野角`, `見失い秒数`, `敵挙動ID`, `敵特殊タグ`, `捕獲難度`, `捕獲後説明文`, `捕獲後効果テキスト`, `捕獲後攻撃力`, `捕獲後ダメージ種別`, `捕獲後掘削力`, `捕獲後耐久力`, `捕獲後重さ`, `捕獲後通常効果`, `捕獲後軌道効果`, `捕獲後特殊タグ`, `捕獲後挙動ID`, `ST_E1`〜`AS_E9` です。接触ダメージは敵ごとの攻撃力とダメージ種別を使い、死亡時の死因表示には敵名を使います。例: `スライムの接触で死亡`。

敵グラフィックは `Enemies` DB の画像番号から `assets/enemies/en_番号.png` を参照して描画します。画像がない敵は、敵IDから決まる色の円へフォールバックします。デバッグ表示にはアクティブ敵の名前、enemyId、HP、半径、AI、挙動IDを表示します。

敵への攻撃・捕獲・観察・プレイヤー接触ダメージの命中判定は、敵ごとの円集合当たり判定を使います。円集合は敵の中心位置からの相対オフセットと半径で定義し、敵の巨大化/縮小ステータスに合わせてオフセットと半径を同じ倍率で拡縮します。敵は現在の `facingAngle` から `down` / `left` / `right` / `up` の方向別円集合を選び、該当方向が未定義なら共通円集合、共通も未定義なら `Enemies` DBの `半径` 1円へフォールバックします。リング上アイテムも `data/hitboxes.cfg` の `object` 定義があれば円集合を使い、リング描画の `リング角度` と同じ回転を適用し、アイテムの大きさ補正に合わせてオフセットと半径を拡縮します。捕獲敵アイテムだけは `object` 判定を使わず、リング上で向いている外向き方向から元敵IDの方向別 `enemy` 判定を選びます。最初に円集合全体を包む外接半径で軽い広域判定を行い、その後に円同士の距離だけを確認します。移動・壁衝突・スポーン間隔などの物理半径は従来どおり `半径` を使います。

未対応項目: 発射物DB、種族ごとの専用AI演出/戦術、敵特殊タグからの直接効果発動、画像未割当敵の追加アセット、反射の本格処理。

## EnemyCatalog

敵DBは `ObjectCatalog` とは責務を分け、`src/data/EnemyCatalog.hpp` / `src/data/EnemyCatalog.cpp` の `EnemyCatalog` として読み込みます。Googleスプレッドシートから `Enemies` タブと `behavior_sheet` で指定した挙動タブ（現設定: `#挙動ID`）を公開CSVとして取得し、列名ベースで `EnemyDefinition` / `BehaviorDefinition` に変換して保持します。

この段階では読み込み、正規化、参照用Map作成、検証、起動ログ出力に加え、敵生成、敵側の最小挙動、捕獲処理、捕獲後のアイテム化、発射物を使わない捕獲後挙動ID、捕獲後発射系挙動ID、報酬生成・盗み・採掘補助系、爆発・磁力・風逸らしへ接続しています。特殊タグからの直接効果発動は実装しません。

### google_sheet_source.cfg 追加キー

`data/google_sheet_source.cfg` には任意キーとして以下を指定できます。未指定または空欄の場合はこの既定値を使います。

```ini
enemies_sheet=Enemies
behavior_sheet=#挙動ID
```

### Enemiesタブ

`Enemies` タブは1行目を列見出しとして扱います。列は `ID`, `名前`, `説明文`, `HP`, `接触攻撃力`, `接触ダメージ種別`, `移動速度`, `半径`, `経験値`, `お金`, `敵AI`, `未発見AI`, `視野距離`, `視野角`, `見失い秒数`, `敵挙動ID`, `敵特殊タグ`, `捕獲難度`, `捕獲後説明文`, `捕獲後効果テキスト`, `捕獲後攻撃力`, `捕獲後ダメージ種別`, `捕獲後掘削力`, `捕獲後耐久力`, `捕獲後重さ`, `捕獲後通常効果`, `捕獲後軌道効果`, `捕獲後特殊タグ`, `捕獲後挙動ID`, `ST_E1`〜`AS_E9`, `備考` です。

`お金` 列は敵撃破時の確定お金ドロップ量です。列名ベースで読み込み、空白はドロップなし、数値変換失敗は警告ログを出してドロップなし、0以下もドロップなしです。1以上の場合、通常敵・ボスの撃破時にその金額を `WorldDropSystem::spawnMoneyDrop` でワールド上へばら撒き、拾得時に所持金へ加算します。経験値、ボス撃破、ステージクリア処理は従来の撃破イベント処理を維持します。

任意の敵出現重み列は次の18列です。列が存在しない場合と空欄は0扱いです。数値変換失敗と負数は0扱いにし、`EnemyCatalog::validationWarnings` と起動ログへ警告を出します。重み0以下の敵は、その列の抽選対象外です。

```text
ST_E1 ST_E2 ST_E3
JK_E1 JK_E2 JK_E3
SC_E1 SC_E2 SC_E3
AS_E1 AS_E2 AS_E3 AS_E4 AS_E5 AS_E6 AS_E7 AS_E8 AS_E9
```

`E` は通常の掘削起因スポーン、および敵ノード由来の露出/埋没固定配置スポーンにおける敵種選択用の相対重みです。現在ステージIDと生成地点の深度ランクから `ST_E1` などの列名を解決し、その列の重みが1以上の敵だけを候補にして重み付き抽選します。敵が出る頻度やタイミングは `RuntimeBalance` の `enemy_min_dug_tiles`、`enemy_guarantee_dug_tiles`、`enemy_soft_cap`、およびダンジョンレイアウト側の敵ノード数で制御し、`ST_E1`〜`AS_E9` は頻度そのものを増減しません。

該当列に候補がない場合、または現在ステージIDから列名を解決できない場合は、警告ログを出して通常敵候補の単純ランダム選択へフォールバックします。`EnemyCatalog` が空の場合もクラッシュさせず、既定敵パラメータへフォールバックします。星くずモグラ、ジャンクラブ、アストラグナ、星脈竜などのボス系、または `boss` / `boss_only` / `no_normal_spawn` / `event_only` / `fixed_only` 相当のタグを持つ敵は、通常スポーンの重み候補から除外します。敵IDを明示した固定イベントやボス生成は、この重み抽選の対象外です。

敵撃破時は、既存のお金ドロップと経験値処理に加えて素材ドロップ判定を行います。敵ごとの個別列は追加せず、`game_balance.cfg` の共通値で制御します。通常敵は `enemy_mana_drop_chance` で `mana_drop` を1個、`enemy_moon_fragment_chance` で `moon_fragment` を1個落とします。ボスは `boss_mana_drop_chance` と `boss_moon_fragment_chance` を使い、発生時の数量は1〜3個です。`mana_drop` の主入手は敵撃破、`moon_fragment` の主入手はワープポイント周囲のままで、敵/ボスからの月のカケラはレア補助入手として扱います。素材は `WorldDropSystem::spawnMaterialDrop` で地面に出し、拾得時に `MaterialInventory` へ加算します。

### 挙動タブ

`behavior_sheet` で指定したタブは1行目を列見出しとして扱います。列は `挙動ID`, `表示名`, `使用欄`, `分類`, `起動条件`, `基本処理`, `既定間隔秒`, `既定発射物ID`, `敵側対象`, `捕獲後対象`, `敵側既定効果`, `捕獲後既定効果`, `関連特殊タグ`, `敵側向き制御`, `捕獲後向き制御`, `移動制御`, `重複ルール`, `実装状態`, `備考` です。

### 敵DB検証

起動時に読み込み件数と検証警告をログへ出します。ID空欄、ID重複、名前/表示名空欄、数値列の変換失敗、ダメージ種別不正、未定義の敵挙動ID/捕獲後挙動ID、未定義の敵特殊タグ/捕獲後特殊タグ/関連特殊タグ、`EffectSpec` 形式不正、敵出現重み列の数値変換失敗と負数を警告します。`未発見AI` / `視野距離` / `視野角` / `見失い秒数` は列欠落時に既定値で継続し、列が存在する場合は空欄・不正値で警告して既定値へフォールバックします。敵出現重み列が存在しないこと自体は警告しません。未知の `未発見AI` は実行時に警告して `idle` へフォールバックします。`お金` は既定値0、つまりドロップなしです。ダメージ種別は `none`, `physical`, `fire`, `ice`, `thunder`, `wind`, `earth`, `water`, `magic` のみを許可します。


## 捕獲後挙動コード（リング挙動）

捕獲後挙動コードは `trigger:behavior:params:interval` を使用し、Enemies DB の捕獲後列から取り込んだ `capturedBehaviorSpecs` をリングアイテム実行時に参照する。
特殊タグ単体では発動しない。

### 実装した捕獲後behavior

- `outward_guard`: 外側から来る小型Projectileを防御
- `heavy_guard`: 大型/高威力寄りProjectileを防御し、重接触側で耐久消費を抑制
- `magic_guard`: magic系Projectileを防御
- `jump_outward`: interval/paramsで外側ジャンプ、ジャンプ中は接触判定を少し拡張
- `contact_slow`: 敵接触時に短時間slow付与（params対応）
- `periodic_heal`: 共有クールダウンでプレイヤー小回復（interval/amount対応、上限あり）
- `rust_debuff`: 敵接触時に短時間defense低下（params対応）
- `reward_drop`: 敵/壁接触時の低確率報酬（interval/chance対応）
- `steal_or_dig`: 敵/壁接触時の低確率報酬系トリガ（interval/chance対応）
- `charge_explode`: 接触回数蓄積で爆発、`count` / `charges` / `radius` / `damage` / `terrainRadius` / `terrainDamage` / `rest` 対応
- `magnet_pull`: metal対象のdrop/enemy/projectileを緩く吸引（radius/strength/targetTag対応）
- `wind_deflect`: 近傍敵Projectileを軽く逸らす（interval/radius/strength対応）
- `dig_contact`: 地形接触時の掘削補助（probeDistance系params対応）
- `throw_object` / `shoot_poison` / `shoot_web` / `shoot_fire` / `shoot_water` / `radial_spike`: リング位置から外側へ発射。`interval` は捕獲後挙動コードを優先。

### バランス制限

- `periodic_heal` は共有クールダウン + 1パルス上限で過剰回復を防止
- `reward_drop` / `steal_or_dig` は既存の窓制限・クールダウン・低確率を維持
- Projectile発射は既存のプレイヤーProjectile上限を超えない

### 反映ルール

- 捕獲後攻撃力/ダメージ種別/掘削力/耐久力/重さは既存リング処理へ反映済み
- 捕獲後挙動コードが `none` の場合は通常の接触リングアイテムとして扱う
- 不正挙動コード・不正paramsは既存のvalidation warningで無効化し、クラッシュさせない

### 簡易実装・未拡張

- `magnet_pull` の `targetTag` は現状 `metal` 中心の簡易フィルタ
- 一部behaviorの細かな演出差分（種別ごとの専用VFX/SFX）は未拡張

## 敵挙動コード移行 総合検証（2026-05-12）

### 1. シート読み込み

- `Enemies` は列名ベースで読み込み（`敵挙動コード` / `捕獲後挙動コード` を優先）。
- `#敵挙動コード` は列名ベースで読み込み。
- `#挙動ID` / `挙動ID一覧` はフォールバックのみ。
- `behavior_sheet` が設定ファイルで明示された場合はそれを優先。
- 明示なし時にフォールバックを使った場合は `validationWarnings` に記録。

### 2. ダメージ種別

- Enemies接触ダメージ種別の許可値は
  `none, physical, fire, ice, thunder, wind, earth, water, magic` のみ。
- `poison` は接触ダメージ種別としては不許可（warningで `none` 扱い）。
- 毒は `status_poison` / `shoot_poison` / `shoot_mud`（毒沼）で扱う。

### 3. 特殊タグ運用

- `poison` / `metal` / `treasure` / `shield` タグ単体で直接挙動発火はしない。
- 実処理は `敵挙動コード` / `捕獲後挙動コード` / `Objects EffectSpec` からのみ発火。

### 4. 敵AIと敵挙動コード

- 敵AI: 索敵・移動・待機・逃走。
- 敵挙動コード: 攻撃・妨害・支援・ドロップ・光源反応など。
- `jump_chase`（AI）と `jump_attack`（挙動コード）は分離済み。

### 5. 捕獲後挙動コード

- `capturedBehaviorSpecs`（trigger/params/interval）をリングアイテム実行時に参照。
- 実装済み: `outward_guard`, `heavy_guard`, `magic_guard`, `jump_outward`, `contact_slow`,
  `periodic_heal`, `rust_debuff`, `reward_drop`, `steal_or_dig`, `charge_explode`,
  `magnet_pull`, `wind_deflect`, `dig_contact`, `throw_object`, `shoot_poison`, `shoot_web`,
  `shoot_fire`, `shoot_water`, `radial_spike`。
- バランス制限: periodic_heal共有クールダウンと上限、reward系クールダウン/窓制限、
  projectile上限維持。

### 6. ログ・デバッグ

- 起動ログに Enemies件数 / #敵挙動コード件数 / warning件数を出力。
- warning詳細も起動ログに列挙。
- F1デバッグで敵名・敵ID・AI・未発見AI・敵挙動コード・捕獲後挙動コードを確認可能。

### 7. 後方互換

- 旧 `敵挙動ID` / `捕獲後挙動ID` / `#挙動ID` / `挙動ID一覧` は後方互換として継続。
- 正式仕様は `敵挙動コード` / `捕獲後挙動コード` / `#敵挙動コード`。

### 8. 実装済み・簡易実装・未実装

- 実装済み: 本章および前章の敵/捕獲後behavior。
- 簡易実装: `magnet_disturb`, `magnet_pull` の `targetTag` は現状 `metal` 中心。
- 未拡張: 種族ごとの専用VFX/SFX差分、完全な実機バランスチューニング。

### 9. エンコード

- `google_sheet_source.cfg` / ソース / 仕様書は UTF-8運用。
- `仕様書.md` は UTF-8 BOM (`EF BB BF`) を維持。
- MSVCの `/utf-8` コンパイルオプションを維持。

## リング形状拡張前の接続点メモ（2026-05-12）

- 現在の実戦用軌道座標は `SpellRingSystem::update` で `getRingItemWorldPosition()` を通して算出し、`SpellRingItem::worldPosition` に保持する。
- 地形掘削は `DiggingSystem` が `item.worldPosition` と `spellRing.center()` を参照する。
- 敵接触は `EnemySystem` が `item.worldPosition` を当たり判定・効果発火の基準に使う。
- 光源は `Game::render` が `item.worldPosition` から `LightSource` を生成する。
- リング投げ中の中心更新は `SpellRingSystem` の `state` (`Normal/Thrown/Returning`) と `center_` で管理する。
- 右クリック長押しの中心ずらしは `Input::ringOffsetHeld` → `Player::spellRingShift` → `getRingCenterWorldPosition()` → `SpellRingSystem::center_` の経路で反映する。`getRingCenterWorldPosition()` はプレイヤーの足元ではなく、プレイヤー魔女の自発光と同じ体中央アンカーを基準にする。
- リング画面UI座標は `findNearestRingPathParam()` と `getRingItemWorldPosition()` の共通軌道計算を使って算出する。
- セーブ/ロードは `ring` 行で `item.localAngle` を保存/復元し、読込後に `normalizeItemPlacements()` で角度正規化する。
- デバッグ表示は `DebugOverlay` でリング半径・速度・投げクール比を表示する（個別アイテム座標は未表示）。
- セーブは `ring_shape_1..3` で `RingShape` を保存し、旧セーブや不正値は `Circle` にフォールバックする。

## リング形状拡張（2026-05-12）

- `RingShape` は `Circle` / `FigureEight` / `Comet` の3種。
- 標準形状は `Ring 1 = Circle`, `Ring 2 = FigureEight`, `Ring 3 = Comet`。
- 実戦処理では最大3リングを同時処理し、各リングが自分の `RingShape` を使う。

### 3リング同時表示の見た目方針

- `Circle` は細い基準円として描画し、主張を抑えた輪郭レイヤーに置く。
- `FigureEight` は細い8の字線で描画し、結び目がリング中心にあることを視認できるようにする。
- `FigureEight` 全体は結び目中心でゆっくり回転する（`figure8_shape_rotation_speed`）。
- `Comet` は全周線を描かず、外周の一部だけを弧状の流れ・残像・先頭強調で描画する。
- レイヤー順は `Circle`（輪郭）→`FigureEight`（内側交差）→`Comet`（外周の流れ）を基本とする。
- Comet多数アイテム時は弧内レーン配置を維持し、表示は軽い縮小と代表的トレイル中心で破綻を抑える。

### localAngle の意味

- `Circle`: 円周上の角度。
- `FigureEight`: 8の字軌道パラメータ `t`（角度として保持）。
- `Comet`: 彗星弧中心からの相対角度（`-arc/2..+arc/2` に正規化）。

### 共通軌道計算基盤

- 追加した共通関数:
  - `getRingCenterWorldPosition(...)`
  - `getRingItemLocalPosition(...)`
  - `getRingItemWorldPosition(...)`
  - `getRingItemVelocity(...)`
  - `getRingPathSamplePoints(...)`
  - `findNearestRingPathParam(...)`
- 実戦（当たり判定・掘削・光源）は `SpellRingItem::worldPosition` を共有し、`SpellRingSystem::update` で共通軌道計算を使用。
- リングUIは同じ軌道計算関数でパス描画とドラッグ吸着を行う。
- `SpellRingSystem` はリングごとのアイテム配列を持ち、`runtimeItems()` で全リングの実戦アイテムを横断する。
- 描画・掘削・敵接触・光源・リング投げ中・右クリック中心ずらしは同じ実戦座標（`SpellRingItem::worldPosition`）を参照する。
- 速度依存ダメージは `SpellRingItem::orbitMotionSpeed`（実移動速度由来）を参照する。
- `Circle` は既存互換として半径・回転挙動を維持する。

### リング画面UI

- リング画面は、選択中リングの実戦軌道をそのまま表示する（Circle / FigureEight / Comet）。
- UI座標は専用軌道式を持たず、実戦軌道サンプリング結果をUI中心へ平行移動して表示する。
- リング1〜3のタブは、リング画面ウィンドウ左端からリング1左端までの余白と、詳細ウィンドウ左端からリング3右端までの余白が同じになるように横幅を計算する。
- リング1〜3の数字キーはタブ直接選択ではなくプリセット呼び出しに使う。リング切替は `Z/X` またはタブクリックで行う。
- `localAngle` は形状ごとの軌道上位置として維持し、編集・保存に使う。
- ドラッグ中はマウス最近傍の軌道点をサンプリング探索し、対応する `localAngle` にスナップする。
- 配置不可時は近傍の配置可能位置へ補正し、補正不可なら元位置へ戻す。
- 整列と全部取るは画面内ボタンではなく入力操作として扱う。整列は `T` / ゲームパッド `X`、全部取るは `Shift+R` / ゲームパッド `LT` + 十字キー下で実行する。
- Comet は全周配置せず弧内配置を維持し、多数装着時はレーン分割と実距離ベース重なり判定で詰まりを軽減する。
- 位置調整キーは `A/D` または `←/→` で、形状ごとの `localAngle` 増減として処理する。

### game_balance.cfg 追加キー

- `figure8_width_multiplier`
- `figure8_height_multiplier`
- `figure8_shape_rotation_speed`
- `comet_radius_multiplier`
- `comet_arc_degrees`
- `comet_speed_multiplier`
- `comet_trail_length`
- `comet_lane_spacing`
- `comet_max_arc_degrees`

### セーブ互換

- `ring` 行は既存項目を維持しつつ末尾に `ringIndex` を追加（旧データで未指定時は `0` = Ring1 扱い）。
- 形状は新規 `ring_shape_1..3` で保存。
- 旧セーブ（`ring_shape_*` 未存在）と不正値は `Circle` にフォールバック。

## 効果発見仕様（2026-05-12更新）

### ダメージ種別

- 有効値は `none, slash, blunt, pierce, fire, ice, thunder, wind, earth, water, magic`。
- 表示名は `slash=斬撃, blunt=打撃, pierce=刺突, fire=火, ice=氷, thunder=雷, wind=風, earth=土, water=水, magic=魔法`。
- 旧値 `physical` は互換入力として受理し、警告ログを出したうえで `blunt` にフォールバックする。
- `isPhysicalDamageType(type)` は `slash/blunt/pierce`（および旧互換 `physical`）を物理グループとして扱う。
- `physical_resist` / `magic_body` の物理側倍率は `slash/blunt/pierce` 全てに適用する。
- `reflect_physical` / `reflect_physical_chance` は Projectile分類としての「物理弾」用名称を維持する（Objectsの `damageType` とは別扱い）。
- `reflect_magic` / `reflect_magic_chance` は Projectile分類としての「元素/魔法弾」用名称として扱い、`magic/fire/ice/thunder/wind/earth/water` を対象にする。`reflect_water` / `reflect_water_chance` は水弾専用の狭い反射として残す。

### Objectsタブ

- `ObjectDefinition` から `DiscoveryEffectLine { objectId, effectKey, text, trigger }` を生成し、`objectId + effectKey` 単位で効果行を管理する。
- 効果行の機械的根拠は以下。
- `attackPower + damageType`（`basic_attack`）
- 掘削実処理に接続される掘削要素（カテゴリ/`dig`系効果/十分な掘削力）による `dig` / `dig_hard`
- 通常効果（`normalEffects`）
- 軌道効果（`orbitEffects`）
- 特殊タグのみでは効果行を生成しない。
- 未実装/未対応の効果コードはクラッシュせずスキップし、デバッグ警告に記録する。
- 効果テキストが空でなければ `｜` 区切りで分割して表示候補化し、生成行数と一致した場合のみ順序対応で上書きする。行数不一致時は警告して自動生成文を使う。

### Enemiesタブ

- `contact_damage_type` / `captured_damage_type` は新damageType体系で検証する。
- 旧値 `physical` は警告付き `blunt` フォールバック。
- 捕獲後オブジェクト化時も同じ正規化を適用し、効果発見行を再生成する。

### 本棚と図鑑

- 効果表示は `getObjectEffectDisplayLines(objectId, revealMode)` を共通使用する。
- `revealMode` は `RevealedOnly` / `WithUnknown` / `DebugAll`。
- 通常UIは `WithUnknown` を使用し、未発見効果を `？？？` 表示する（行数は表示する）。
- 効果発見ポップアップ・本棚詳細・アイテム詳細・リング効果欄は同一効果文言ソース（`DiscoveryEffectLine.text`）を参照する。

### SaveData

- 既存の行指向テキスト形式を維持し、JSON化しない。
- ヘッダは `MAJO_SHOVEL_SAVE_V1`。
- 効果発見は `codex_effect objectId effectKey` を保存/読込する。
- `codex_effect` がない旧セーブは未発見扱いで継続する。
- セーブ内にDBから消えた `objectId/effectKey` が残っていてもクラッシュさせず保持する。

### 効果表示の共通化

- UI側で個別生成せず、図鑑システムの共通表示関数を使用する。
- これにより、効果文言の単一ソース化と未発見表示ルールの統一を行う。
- 共通関数は `getObjectEffectDisplayLines(objectId, revealMode)` を使用する。
- 通常UIは `revealMode = WithUnknown` とし、未解禁効果は `？？？` を表示する。

### アイテム画面（UI）

- 通常表示で `EffectSpec` 生文字列を主表示に使わない。
- プレイヤー向けの効果欄は共通関数の「1効果1行」を表示する。
- 効果数を表示し、未解禁効果は `？？？` とする。
- `Objects` の効果テキストに `｜` が含まれる場合も、共通関数側で行分割済みの結果を表示する（UI上に `｜` は出さない）。
- `Base` から開いた場合は、アイテムスロットのコマンドメニュー内「使用する」を半透明の無効表示にし、通常使用効果を発動しない。

### リング画面（UI）

- 選択中アイテム詳細の効果欄は共通関数の効果行を表示する。
- 未解禁効果は `？？？`。
- `通常効果/軌道効果` の生要約表示は通常UIから外し、効果欄を優先する。
- リングに載せただけでは未解禁効果は解禁しない（発動時のみ解禁）。

### 本棚と図鑑（UI）

- アイテム図鑑・宝図鑑は `objectId` ごとの効果行を表示する。
- 既存の図鑑段階（未発見/発見済み/入手済み/リング装備済み/効果発動済み/完全確認済み）は維持する。
- 効果欄は `codex_effect` を基準に本文または `？？？` を表示する。
- 宝カテゴリも同じ処理を通す。
- 敵図鑑の既存表示仕様は変更しない。

### 効果テキストの `｜` 区切り仕様

- Objectsの「効果テキスト」は実改行ではなく `｜` を改行相当の区切りとして扱う。
- 実改行が混在している場合も区切りとして扱うが、正規データは `｜` を使用する。

### 効果発見条件（実処理接続）

- 共通入口は `discoverObjectEffect(objectId, effectKey, worldPosition, optionalNote)` とする。
- 未発見の `objectId + effectKey` のみ `codex_effect` に追加し、効果判明ポップアップをキュー投入する。
- 既発見なら何もしない。DBから消えた `objectId/effectKey` や未実装効果コードは安全に無視する。
- リングに載せただけでは解禁しない。実際に効果が発動した処理地点からのみ記録する。
- 記録元は以下。
- 敵命中（リング攻撃）: `basic_attack`
- 非致死リング攻撃: `nonlethal_hit`
- 敵命中時の追加ドロップ: `coin_drop_chance`
- 地形掘削成功: `dig / dig_hard / dig_multi`
- Projectile防御成功: `guard / guard_large / guard_projectile`
- Projectile反射成功: `reflect_physical(_chance) / reflect_magic(_chance) / reflect_water(_chance)`
- 状態異常付与成功: `status_* / status_*_chance`
- 通常使用効果実発動: `heal / buff_* / debuff_*` など
- 照明有効化: `light`
- 探知成立（実際に対象を検知）: `detect_hidden / detect_treasure`
- 軌道補正適用: `orbit_speed / orbit_power / damage_speed` など

### 初回発動保証（隠し仕様）

- 対象は確率効果（`status_*_chance`, `reflect_*_chance` など）。
- 未発見効果は、発動条件成立時の初回のみ抽選前に成功扱いにする。
- 保証単位は `objectId + effectKey`。
- この保証は図鑑補助の内部仕様で、通常UIでは説明しない。
- デバッグログには保証発動を出してよい。

### 無効対象と判明

- 無効対象（例: ボスの状態異常無効）には効果本体を通さない。
- ただし効果内容の判明は行う。
- この場合のポップアップには注記を付ける。
- 注記例: `※ただし、この敵には効かなかった`

### 効果判明ポップアップ

- 表示位置は効果発動地点の近くとし、従来と同じ基準位置を第一候補にする。
- 画面外にはみ出す場合は画面内へ補正する。
- 上部情報バー、ミニマップ、リングHUD、ダンジョンログ、ステータスHUD、リロード通知など既存HUDと重なる場合は、効果発動地点の周辺候補から重なりが少ない位置へ補正する。
- ウィンドウ枠は表示せず、背景は左右端が透明で中央が黒半透明になる横方向グラデーション矩形にする。
- 出現時は0.2秒でフェードインする。フェードイン中は本文の文字送りを開始しない。
- 本文はメッセージウィンドウと同様に、フェードイン完了後から一文字ずつ表示する。`{item:...}` / `{world:...}` のインラインアイコン制御文字は1表示単位として扱い、途中で分割しない。
- 全文表示後は2.5秒保持し、その後0.2秒でフェードアウトする。
- 画面上に同時表示する効果判明ポップアップは最大3個までとし、4個目以降はキューに残して順次表示する。
- 効果が `none` / `なし` の場合は効果判明ポップアップを出さない。図鑑やアイテム詳細の効果欄には、未発見扱いの `？？？` ではなく最初から `なし` と表示する。
- 表示形式は以下。

```text
効果判明：{アイコン} {アイテム名}
・{効果行テキスト}
・{効果行テキスト}
{任意注記}
```

- 同一 `objectId + effectKey` は一度だけ表示する。
- 同じタイミング（1回の発見イベント処理）で同一アイテムIDの未発見効果が複数判明した場合は、1つのポップアップにまとめ、効果行を複数の `・` 行で表示する。
- 異なるアイテムIDの複数同時発生時はキューで順次表示する。
- アイテム名の前には既存のインラインアイコン制御文字でアイコンを描画する。

## デバッグ: 起動モード

- デバッグコンソールの `ゲーム進行` グループに `起動モード` ドロップダウンを置きます。
- 選択肢は `タイトル前から`、`拠点から`、`ダンジョンから`、`敵テスト` です。
- `タイトル前から` は実際の通常起動と同じ挙動です。起動時に `OpeningKamishibai` から表示し、紙芝居終了後に `Title`、決定入力後に `intro_tutorial_completed` 未完了なら導入ダンジョン、完了済みなら `Base` へ進みます。
- `拠点から` はデバッグ用の短絡起動です。タイトル前オープニングと仮 `Title` を踏まず、直接 `Base` を表示します。
- `ダンジョンから` はデバッグ用の短絡起動です。タイトル前オープニングと仮 `Title` を踏まず、採掘開始状態へ直接進めます。
- `敵テスト` はデバッグ用の短絡起動です。タイトル前オープニングと仮 `Title` を踏まず、敵テスト用ダンジョンへ直接進めます。
- 保存値は `%LOCALAPPDATA%/MajoShovel/dev_launch_mode.txt` に記録し、未設定または不正値の場合は `タイトル前から` を既定値にします。

## デバッグ: ステージ解放状態

- デバッグコンソールの `ゲーム進行` グループに `ステージ解放状態` ドロップダウンを置きます。
- 選択肢は `初期状態`、`ステージ2解放`、`ステージ3解放` です。
- `初期状態` はステージ1のみ、かつ導入チュートリアル未完了状態に戻します。`ステージ2解放` はステージ1・ステージ2・ローグライク、`ステージ3解放` はステージ1・ステージ2・ステージ3・ローグライクをダンジョン入口の行き先に表示し、導入チュートリアルは完了済み扱いにします。
- ローグライクダンジョン「不可思議の迷宮」は、ステージ2解放以降で選べます。

## デバッグ: 会話テスト

- デバッグコンソールの3つ目のタブに `テスト` グループを置き、`イベントテスト` と `チュートリアルテスト` ボタンを配置します。
- どちらもゲーム画面上にテスト選択ウィンドウを開き、読み込み済みの `StoryEvent` 一覧から選んで共通会話オーバーレイで再生します。
- テスト選択ウィンドウの左側はコンパクトな縦リスト表示にし、タイトルを主表示、`eventId` と `trigger` を補助表示、`ONCE` / `SEEN` / `REPEAT` を状態表示として右寄せで出します。右側の詳細パネルには選択中イベントのID、Trigger、Once、既読状態を表示します。
- テスト再生で開いた会話が終了したら、同じテスト選択ウィンドウへ戻ります。選択位置とスクロール位置は再生前の状態を維持します。
- 一覧の表示名は `.story` の `@title` を使います。`@title` が空の場合は `@event` IDを表示します。
- `チュートリアルテスト` は `@trigger` が `tutorial:` で始まるイベントだけを表示します。`イベントテスト` はそれ以外のイベントを表示します。
- テスト再生は `@once` の既読フラグを追加せず、既読済みイベントでも再生できます。通常進行用の会話キューとは分離し、テスト再生開始時は選択した会話だけを再生します。
- `.story` のホットリロード後は読み込み済みイベントの更新番号を見て一覧を作り直し、今後追加したイベントも自動でテスト対象に入ります。

## デバッグ: エフェクトテスト

- デバッグコンソールの3つ目のタブの `テスト` グループに `エフェクトテスト` ボタンを置く。コマンドは `game effect-test`。
- 実行するとダンジョン上ではなく、専用のプレビュー画面を開く。左側にエフェクト一覧、右側に対象プレビュー、下部に選択中項目の概要と終了ボタンを表示する。ポップアップ表示はテスト対象にしない。このプレビュー画面のレイアウト、スクロール一覧、選択更新、フッター、終了操作は弾テストと共通のDevTools用UIを使う。
- 共通プレビューUIのフッターには背景色スウォッチを置き、暗色、灰色、明色、洞窟色、青系からプレビュー背景を選べる。背景選択はエフェクトテストと弾テストで共有し、背景変更だけでは再生中のテスト項目をリセットしない。
- エフェクト数が多いため、一覧上部に `すべて` と `EffectPreviewEntry::group` 由来の分類タブを表示する。初期分類は粒子、API、魔法常駐、魔法単発、状態異常で、今後プレビュー項目に新しいgroupを追加した場合はタブにも自動で入る。
- 一覧の項目を選択すると、その項目を自動再生する。単発系は20フレーム間隔で再発火し、オーラやループEmitterは選択中だけ常駐再生する。選択を変更した時点で古い粒子・Emitterをクリアし、新しい項目を先頭から再生する。
- 対象がプレイヤーの場合は実際の魔女スプライトを通常のプレイヤー描画スケールで表示する。対象が敵の場合は `slime` の敵画像を通常の敵画像描画経路で表示する。対象が壁の場合は `assets/tiles/tile_1.png` のステージ1壁タイルを通常タイル描画経路で実寸表示する。
- エフェクトの座標と半径はプレビュー画面内のスクリーン座標をゲーム内1px相当として扱い、UI拡大用の別スケールを掛けない。発生位置は対象タイプごとに固定し、プレイヤーは足元、敵は中心、壁はタイル中心を基準にする。
- テスト項目は `EffectSystem`、`MagicFxSystem`、`EntityStatusVisuals` がそれぞれ公開するプレビューカタログを合成して作る。DevTools側は一覧の合成、選択、20フレーム再生、対象描画だけを担当し、個別エフェクト名の追加先にはしない。
- `EffectSystem` の粒子プリセットは `ParticlePresets` から一覧化する。高水準API系の発火項目は `EffectSystem` 側のプレビュー定義に置く。`MagicFxSystem` の追加FXは `MagicFx` 側のプレビュー定義と再生ヘルパーへ追加する。状態異常は `StatusVisualDefinitions` から一覧化するため、新しい状態異常表示を追加した場合はテスト一覧にも自動で入る。

## デバッグ: 弾テスト

- デバッグコンソールの3つ目のタブの `テスト` グループに `弾テスト` ボタンを置く。コマンドは `game projectile-test`。`game bullet-test` も別名として扱う。
- 実行するとダンジョン上ではなく、エフェクトテストと同じ共通プレビューUIを使う専用画面を開く。左側に弾一覧、右側にスライム発射プレビュー、下部に選択中項目の概要と終了ボタンを表示する。
- 一覧の項目を選択すると、その弾を20フレーム間隔で自動発射する。選択を変更した時点で既存Projectileをクリアし、新しい項目を先頭から再生する。
- 一覧とフッターの選択中項目名は `ProjectileDefinition::displayName` の日本語名で表示し、内部IDは詳細行に表示する。
- 発射元は `slime` の敵画像を通常の敵画像描画経路で実寸表示する。Projectileは `ProjectileSystem::spawn()` で生成し、描画は通常のProjectile描画をスクリーン座標の実寸で使う。プレビュー更新は衝突、ダメージ、反射、状態異常付与を行わず、位置更新と寿命消滅だけを行う。
- テスト項目は `ProjectileSystem` のコード内発射物プロトタイプから公開される一覧を使う。DevTools側に個別の弾IDを重複定義しないため、`ProjectileSystem` に新しいプロトタイプを追加すると弾テスト一覧にも自動で入る。

## デバッグ: 敵テスト

- デバッグコンソールの `戦闘データ` グループに `レベルアップ` ボタンを置く。押すと採掘中のプレイヤーを1レベル上げ、通常のレベルアップと同じ強化カード選択へ入ります。
- デバッグコンソールの `戦闘データ` グループに `敵テスト` ボタンを置く。
- コマンドは `game enemy-test`。
- 実行すると通常の `Playing` 進行のまま `enemyTestActive` を有効化し、敵テスト用の固定ダンジョンへ移動する。
- 敵テスト用ダンジョンは暫定実装として固定seedの小型ダンジョンを使う。専用の平坦アリーナ地形は後続実装対象。
- 敵テスト中は通常ダンジョン状態を保存対象にしない。通常ステージの `DungeonState` へ敵テスト用マップや敵を混ぜない。
- 敵テストUIは画面左上の小型ツールバーとして表示する。
- ツールバーには敵選択ドロップダウン、`召喚`、`全消去`、`UI非表示`、`終了` を置く。
- 敵選択ドロップダウンを開いた時だけ `Enemies` DBの敵一覧を表示する。
- 敵選択ドロップダウンは共通UI部品 `UiDropdown` を使い、画像ボタンではなく矩形のリストとして描画する。
- ドロップダウン内のマウスONはハイライトのみ行い、選択確定はクリック、決定操作、または召喚ボタンで行う。
- ドロップダウンを開いている間は方向入力でハイライト行を移動できる。
- マウスがドロップダウンのリスト上にある場合は、リスト内のどこでもマウスホイールで表示範囲をスクロールできる。
- 表示行数を超える項目がある場合は、ドロップダウン右側にスクロールバーを表示する。
- 敵を選択して `召喚` を押すと、プレイヤー前方付近にその敵IDを指定して召喚する。
- `UI非表示` を押すとテストUIを隠し、復帰用の小ボタンだけを残す。
- 召喚敵は出現直後から発見済み状態にして、AI・挙動・弾・接触・リング攻撃を通常どおり確認できる。
- `全消去` で敵と弾を消去する。
- `拠点へ戻る` またはキャンセル操作で敵テストを終了し、拠点へ戻る。
- 敵テスト中は自動敵スポーン、ボススポーン、ワープ、報酬ノード、宝箱、木箱の通常更新を止める。
- 敵AI、Projectile、プレイヤー移動、リング、掘削、ダメージ処理は通常どおり動かす。

## ダンジョン描画順

- 地形タイル、ワープポイント、リング軌道線などの背景系を先に描画する。
- 地形タイルの照明対象判定は、タイル中心点ではなくタイル矩形と各照明円の交差で行う。円に一部でもかかるタイルは描画対象にし、実際の塗りは照明円でクリップする。
- 鉱石/お金/月片ノード、木箱、宝箱、地面ドロップアイテム、プレイヤー、敵は同じ深度グループとして扱い、ワールドY座標が小さいものから順に描画する。つまりY座標が下にあるものほど手前に表示する。
- プレイヤーの影と足跡は、この深度グループより後ろに描画する。
- 弾は上記の深度グループより手前に描画する。弾同士もワールドY座標が小さいものから順に描画し、Y座標が下にあるものほど手前に表示する。
- リング軌道線、リング投げ中の接続線、リング上のアイテム、およびリング上アイテム由来のパーティクルは暗闇オーバーレイより手前に描画し、暗闇による非表示判定を受けない。
- エフェクトとUIは従来どおり、ワールド内オブジェクトおよび弾より後に描画する。

## リング追加演出

- 通常ダンジョン中に下部アイテム欄の選択アイテムを `R` でリングへ追加できた場合、下部アイテム欄の該当スロット中心から、追加されたリングアイテムの現在位置へ光が走る演出を表示する。
- 演出はスクリーンスペースで扱い、始点はHUDスロット中心、終点は `SpellRingItem::worldPosition` をカメラ変換した画面座標とする。
- 終点の追跡にはリング追加時の `ringIndex`、`itemIndex`、`localAngle`、`objectId`、`instanceId` を使う。対象アイテムが見つからない場合は、同じリング軌道計算から推定位置を使う。
- 光は短いベジェ曲線の残光として描画し、約0.26秒で終点へ吸い込まれる。終点到達時は小さなソフトリングを出す。
- 描画はHUD表示後、ポップアップやデバッグ表示の前に行う。
- 光の線、発光点、到達リングは `Renderer` の `SDL_RenderGeometry` ベースのソフトプリミティブを使い、透明フリンジ付きの頂点カラーでアンチエイリアス風に描画する。

## リング半径

- `spell_ring_radius` はリング1の基準半径として扱う。
- リング別の初期半径倍率は、リング1が1.2倍、リング2が1.8倍、リング3が2.4倍。
- 半径強化や工房の初期半径強化は基準半径に反映し、その後にリング別倍率を適用する。
- リング3のアイテム軌道速度は、形状や装備重量などの既存補正を反映した後に0.5倍する。

## リング速度

- 全リングの初期基準速度 `spell_ring_speed` は 2.176 とする。旧値2.72の0.8倍。
- 速度強化や工房の初期速度強化は、この基準速度に反映する。

## 敵ジャンプ

- 敵は共通ジャンプ状態として、開始位置、着地位置、経過時間、ジャンプ時間、ジャンプ高さを持つ。
- ジャンプ中の実座標は開始位置から着地位置へ線形補間し、`altitude` に `sin(t * pi)` の高さを加えて放物線状に見せる。
- `jump_attack` は瞬間移動ではなく共通ジャンプを開始する。着地可能地点は指定距離から短い距離へ順に探し、壁や地形に埋まる地点には着地しない。
- `jump_attack` の任意パラメータとして `jumpDuration` と `jumpHeight` を使える。未指定時は既定値を使う。
- 着地時に既存の着地強化時間を開始し、`landingRadius` と `landingDamageMultiplier` を接触処理へ反映する。

## 敵高度

- 敵は共通の高度状態 `altitude` を持ち、ジャンプ中の高さと浮遊の高さは同じ値として扱う。
- 描画位置は実座標のYから `altitude` を引いた位置にする。移動、接触、壁判定は従来どおり実座標を使う。
- 浮遊対象は敵AIで判定し、ID別補正は行わない。`hover_chase`、`hover_keep_distance`、`phase_wander`、`phase_chase` はAIに応じた基準高度と上下揺れを持つ。
- `jump_attack` などでジャンプしている間は、浮遊高度にジャンプ高度を加算した値を `altitude` として扱う。
- 敵の影はプレイヤーと同じ `drawActorShadow` を使い、実座標の地面位置に描画する。`altitude` が高いほど影のサイズだけ小さくし、濃さは変えない。
